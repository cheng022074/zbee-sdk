
<?xml?>
<classes>

    <class standard="yes" motify="1556513301684" signature="<void> src::array.clear(<array> data)" entry-type="array"  name="src::array.clear">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 清除数组
 * 
 * @param {array} data 数组
 * 
 * 
 */

data.splice(0 , data.length) ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559021557789" signature="<void> src::array.clone(<array> data)" entry-type="array"  name="src::array.clone">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 对于数组进行拷贝
 * 
 * @param {array} data 数组
 * 
 * 
 */

 return data.slice(0) ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529942" signature="<boolean> src::is.type(<mixed> data , <string> type)" entry-type="mixed string"  name="src::is.type">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , type){

        /**
 * 
 * 对于 typeof 的简单封装
 * 
 * @param {mixed} data 检验数据
 * 
 * @param {string} type 检验数据类型
 * 
 * @return {boolean} 如果检验数据的数据类型与检验数据类型一致，则返回 true，否则返回 false 
 * 
 */

 return typeof data === type ;

    }

                return function(data , type){

                    

                    

                    return main.call(this , data , type) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529931" signature="<boolean> src::is.array(<mixed> data)" entry-type="mixed"  name="src::is.array" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952819759;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为数组类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为数组类型则返回 true , 否则返回 false 
 * 
 */

 return Array.isArray(data) ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819759){

            isType = include('is.type');

            var_init_locked_1561952819759 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529935" signature="<mixed> src::is.empty(<mixed> data , <boolean> allowEmptyString)" entry-type="mixed boolean"  name="src::is.empty" imports="src::is.type src::is.array">
        <![CDATA[(() =>{

                let isArray;

                let var_init_locked_1561952819762;

                

                

                function main(data , allowEmptyString){

        /**
 * 
 * 判定数据是否为空
 * 
 * @import is.array
 * 
 * @param {mixed} data 检验数据
 * 
 * @param {boolean} [allowEmptyString = false] 是否视空符串不为空，默认空符串为空
 * 
 * @return {mixed} 如果给定值为空则返回 true , 否则返回 false  
 * 
 */

return (data == null) || (!allowEmptyString ? data === '' : false) || (isArray(data) && data.length === 0);

    }

                return function(data , allowEmptyString = false){

                    
        if(!var_init_locked_1561952819762){

            isArray = include('is.array');

            var_init_locked_1561952819762 = true ;
        }
        

                    

                    return main.call(this , data , allowEmptyString) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529941" signature="<boolean> src::is.string(<mixed> data)" entry-type="mixed"  name="src::is.string" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952819765;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为字符串类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为字符串类型则返回 true , 否则返回 false 
 * 
 */

return isType(data , 'string') ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819765){

            isType = include('is.type');

            var_init_locked_1561952819765 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529796" signature="<array> src::array.from(<mixed> data)" entry-type="mixed"  name="src::array.from" imports="src::is.type src::is.array src::is.empty src::is.string">
        <![CDATA[(() =>{

                let isEmpty,isString;

                let var_init_locked_1561952819769;

                

                

                function main(data){

        /**
 * 
 * 将非数组数据打包成数组数据
 * 
 * @import is.empty
 * 
 * @import is.string
 * 
 * @param {mixed} data 数据
 * 
 * @return {array} 数组数据
 * 
 */

if(isEmpty(data)){

    return [];
}

if (data && data.length !== undefined && !isString(data)) {

    return Array.from(data);

}

return [
    data
];

    }

                return function(data){

                    
        if(!var_init_locked_1561952819769){

            isEmpty = include('is.empty');
isString = include('is.string');

            var_init_locked_1561952819769 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302555" signature="<object> src::date.get.properties(<Date> date , <string[]> names)" entry-type="Date string[]"  name="src::date.get.properties" imports="src::is.type src::is.array src::is.empty src::is.string src::array.from">
        <![CDATA[(() =>{

                let from;

                let var_init_locked_1561952819776;

                

                

                function main(date , names){

        
/**
 * 
 * 获得指定日期的属性值
 * 
 * @import from from array.from
 * 
 * @param {Date} date 日期对象
 * 
 * @param {string[]} [names] 属性名称集合
 * 
 * @return {object} 日期描述
 * 
 */

 names = from(names) ;

 let result = {} ;

 for(name of names){

    let value ;

    switch(name){

        case 'year':

            value = date.getFullYear() ;

            break ;

        case 'month':

            value = date.getMonth() + 1 ;

            break ;

        case 'day':

            value = date.getDate() ;
    }

    result[name] = value ;
 }

 return result ;

    }

                return function(date , names){

                    
        if(!var_init_locked_1561952819776){

            from = include('array.from');

            var_init_locked_1561952819776 = true ;
        }
        

                    

                    return main.call(this , date , names) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301692" signature="<boolean> src::array.dates.includes(<Date[]> dates , <Date> date , <array> fields)" entry-type="Date[] Date array"  name="src::array.dates.includes" imports="src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952819781;

                

                

                function main(dates , date , fields){

        
/**
 * 
 * 基于日历数组进行包含性检测
 * 
 * @import get from date.get.properties
 * 
 * @param {Date[]} dates 日历数组
 * 
 * @param {Date} date 校验匹配数据项
 * 
 * @param {array} [fields = ['year' , 'month' , 'day']] 校验字段项
 * 
 * @return {boolean} 如果日历数组中包含校验项则返回 true , 否则返回 false 
 * 
 */

 let {
    year,
    month,
    day
 } = get(date , fields) ;

 for(let date of dates){

    let {
        year:itemYear,
        month:itemMonth,
        day:itemDay
    } = get(date , fields) ;

    if(itemYear === year && itemMonth === month && itemDay === day){

        return true ;
    }
 }

 return false ;
 



 

    }

                return function(dates , date , fields = ['year' , 'month' , 'day']){

                    
        if(!var_init_locked_1561952819781){

            get = include('date.get.properties');

            var_init_locked_1561952819781 = true ;
        }
        

                    

                    return main.call(this , dates , date , fields) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301706" signature="<void> src::array.insert.after(<array> data , <mixed> insertItem , <mixed> existItem)" entry-type="array mixed mixed"  name="src::array.insert.after">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , insertItem , existItem){

        
/**
 * 
 * 在数组中指定项之后添加
 * 
 * @param {array} data 目标数组
 * 
 * @param {mixed} insertItem 需要添加的项
 * 
 * @param {mixed} existItem 指定项
 * 
 */

 let index = data.indexOf(existItem) ;

 if(index !== -1){

    data.splice(index + 1 , 0 , insertItem) ;
 }

    }

                return function(data , insertItem , existItem){

                    

                    

                    return main.call(this , data , insertItem , existItem) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301711" signature="<void> src::array.insert.before(<array> data , <mixed> insertItem , <mixed> existItem)" entry-type="array mixed mixed"  name="src::array.insert.before">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , insertItem , existItem){

        
/**
 * 
 * 在数组中指定项之前添加
 * 
 * @param {array} data 目标数组
 * 
 * @param {mixed} insertItem 需要添加的项
 * 
 * @param {mixed} existItem 指定项
 * 
 */

 let index = data.indexOf(existItem) ;

 if(index !== -1){

    data.splice(index , 0 , insertItem) ;
 }

    }

                return function(data , insertItem , existItem){

                    

                    

                    return main.call(this , data , insertItem , existItem) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301697" signature="<void> src::array.insert(<array> data , <number> index , <mixed> ...items)" entry-type="array number mixed"  name="src::array.insert">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , index , ...items){

        
/**
 * 
 * 在指定下标处插入项
 * 
 * @param {array} data 数组
 * 
 * @param {number} index 数组下标
 * 
 * @param {mixed} [...items] 项
 * 
 */

 data.splice(index , 0 , ...items) ;

    }

                return function(data , index , ...items){

                    

                    

                    return main.call(this , data , index , ...items) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301719" signature="<boolean> src::array.remove.index(<array> data , <number> index)" entry-type="array number"  name="src::array.remove.index">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , index){

        
/**
 * 
 * 根据数组下标删除对应项
 * 
 * @param {array} data 作用数组
 * 
 * @param {number} index 数组项的下标
 * 
 * @return {boolean} 如果删除成功则返回 true , 否则返回　false 
 * 
 */

if(index >= 0 && index < data.length){

    data.splice(index , 1) ;

    return true ;
}

return false ;

    }

                return function(data , index){

                    

                    

                    return main.call(this , data , index) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301715" signature="<void> src::array.remove(<array> data , <mixed> ...items)" entry-type="array mixed"  name="src::array.remove" imports="src::array.remove.index">
        <![CDATA[(() =>{

                let remove;

                let var_init_locked_1561952819794;

                

                

                function main(data , ...items){

        
/**
 * 
 * 在数组中去除项目
 * 
 * @import remove from array.remove.index
 * 
 * @param {array} data 数组
 * 
 * @param {mixed} [...items] 项目
 * 
 */

 for(let item of items){

    remove(data , data.indexOf(item)) ;
 }

    }

                return function(data , ...items){

                    
        if(!var_init_locked_1561952819794){

            remove = include('array.remove.index');

            var_init_locked_1561952819794 = true ;
        }
        

                    

                    return main.call(this , data , ...items) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301717" signature="<void> src::array.remove.all(<array> data , <mixed> item)" entry-type="array mixed"  name="src::array.remove.all" imports="src::array.remove.index src::array.remove">
        <![CDATA[(() =>{

                let remove;

                let var_init_locked_1561952819797;

                

                

                function main(data , item){

        
/**
 * 
 * 在数组中去除所有指定项目
 * 
 * @import remove from array.remove
 * 
 * @param {array} data 数组
 * 
 * @param {mixed} item 项目
 * 
 */

while(true){

    if(remove(data , item) === false){

        break ;
    }
}

    }

                return function(data , item){

                    
        if(!var_init_locked_1561952819797){

            remove = include('array.remove');

            var_init_locked_1561952819797 = true ;
        }
        

                    

                    return main.call(this , data , item) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560409074007" signature="<object> src::data.connection.accept(<mixed> ...args)" entry-type="mixed"  name="src::data.connection.accept">
        <![CDATA[(() =>{

                

                

                

                

                function main(...args){

        
/**
 * 
 * 接收消息数据
 * 
 * @param {mixed} [...args] 可选参数
 * 
 * @return {object} 消息体 
 * 
 */

let me = this,
    message = me.processMessage(...args),
    {
        subscribers
    } = me;

subscribers.forEach(subscriber => {

    if(me.validateMessage(subscriber , message)){

        subscriber.accept(me.processData(subscriber , message)) ;
    }

}) ;

    }

                return function(...args){

                    

                    

                    return main.call(this , ...args) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513303394" signature="<mixed> src::url.append(<string> url , <mixed> data)" entry-type="string mixed"  name="src::url.append" imports="src::is.type src::is.string">
        <![CDATA[(() =>{

                let isString;

                let var_init_locked_1561952819805;

                

                

                function main(url , data){

        
/**
 * 
 * 基于已有链接附加查询信息
 * 
 * @import is.string
 * 
 * @param {string} url 链接
 * 
 * @param {mixed} data 附加查询信息
 * 
 * @return {mixed} 拼接了查询信息的链接 
 * 
 */

let querystring ;

if(isString(data)){

    querystring = data ;

}else{

    querystring = [];

    let names = Object.keys(data) ;

    for(let name of names){

        querystring.push(`${name}=${encodeURIComponent(data[name])}`) ;
    }

    querystring = querystring.join('&') ;

}

if(querystring){

    if(url.includes('?')){

        return `${url}&${querystring}` ;
    }
    
    return `${url}?${querystring}` ;

}

return url ;


    }

                return function(url , data){

                    
        if(!var_init_locked_1561952819805){

            isString = include('is.string');

            var_init_locked_1561952819805 = true ;
        }
        

                    

                    return main.call(this , url , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302998" signature="<boolean> src::regexp.int(<string> data)" entry-type="string"  name="src::regexp.int">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 匹配整数
 * 
 * @param {string} data 参数说明
 * 
 * @return {boolean} 如果匹配成功则返回 true , 否则返回 false 
 * 
 */

return /^\d+$/.test(data) ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513303401" signature="<string> src::url.template.apply(<string> url , <object> data)" entry-type="string object"  name="src::url.template.apply" imports="src::regexp.int">
        <![CDATA[(() =>{

                let isInt;

                let var_init_locked_1561952819811;

                

                

                function main(url , data){

        
/**
 * 
 * 路径模板应用
 * 
 * @import isInt from regexp.int
 * 
 * @param {string} url 带有参数定义的URL
 * 
 * @param {object} data 模板参数定义数据集合
 * 
 * @return {string} 应用数据后的URL链接
 * 
 */

return url.replace(/\:(\w+)/g , (match , name) =>{

    if(isInt(name)){

        return `:${name}` ;
    }

    return data[name] || '' ;

}) ;


    }

                return function(url , data){

                    
        if(!var_init_locked_1561952819811){

            isInt = include('regexp.int');

            var_init_locked_1561952819811 = true ;
        }
        

                    

                    return main.call(this , url , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529940" signature="<boolean> src::is.object.simple(<mixed> data)" entry-type="mixed"  name="src::is.object.simple">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为简单对象类型
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为简单对象类型则返回 true , 否则返回 false 
 * 
 */

return data instanceof Object && data.constructor === Object;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561690069620" signature="<void> src::data.connection.ajax.request(<string> url , {<string> method , <object> query , <object> params , <object> path , <boolean> requestJSON , <boolean> responseHeaders})"  name="src::data.connection.ajax.request" imports="src::is.type src::is.string src::url.append src::regexp.int src::url.template.apply src::is.object.simple">
        <![CDATA[(() =>{

                let append,apply,isObject;

                let var_init_locked_1561952819818;

                

                

                function main(url , {method , query , params , path , requestJSON , responseHeaders}){

        
/**
 * 
 * 基于 AJAX 进行数据交互
 * 
 * @import append from url.append
 * 
 * @import apply from url.template.apply
 * 
 * @import isObject from is.object.simple
 * 
 * @param {string} url 请求路径
 * 
 * @param {object} [config] 请求配置
 * 
 * @param {string} [config.method = 'GET'] 请求方式，默认是 GET 请求
 * 
 * @param {object} [config.query] GET请求的参数集合
 * 
 * @param {object} [config.params = {}] 请求主体的参数集合
 * 
 * @param {object} [config.path] 以路径参数形式提交的参数集合
 * 
 * @param {boolean} [config.requestJSON = true] 是否以 JSON方式提交数据
 * 
 * @param {boolean} [config.responseHeaders = false] 是否返回头部信息
 * 
 */

if(query){

    url = append(url , query) ;
}

if(path){

    url = apply(url , path) ;
}

let form = false ;

method = method.toUpperCase() ;

switch(method){

    case 'GET':
    case 'DELETE':

        url = append(url , params) ;

        break ;

    case 'POST':
    case 'PUT':

        if(requestJSON === false){

            form = true ;
        }
}

return {
    url,
    method,
    data:params,
    form,
    responseHeaders
} ;

    }

                return function(url , {method = 'GET' , query , params = {} , path , requestJSON = true , responseHeaders = false}){

                    
        if(!var_init_locked_1561952819818){

            append = include('url.append');
apply = include('url.template.apply');
isObject = include('is.object.simple');

            var_init_locked_1561952819818 = true ;
        }
        

                    

                    return main.call(this , url , {method , query , params , path , requestJSON , responseHeaders}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561690858114" signature="<void> src::data.connection.ajax.request.browser(<string> url , <object> config)" entry-type="string object"  name="src::data.connection.ajax.request.browser" imports="src::is.type src::is.string src::url.append src::regexp.int src::url.template.apply src::is.object.simple src::data.connection.ajax.request" dependent-modules="axios:^0.18.0;qs:^6.7.0">
        <![CDATA[(() =>{

                let request;

                let var_init_locked_1561952819823;

                

                

                function main(url , config){

        
/**
 * 
 * 基于浏览器的 AJAX 请求实现
 * 
 * @import request from ....request
 * 
 * @require axios
 * 
 * @require qs
 * 
 * @param {string} url 请求路径
 * 
 * @param {object} [config] 请求配置
 * 
 */

let {
    url:requestURL,
    method,
    data,
    form,
    responseHeaders
} = request(url , config);

const axios = require('axios'),
{
    stringify
} = require('qs');


if(form === true){

    data = stringify(data) ;
}

let result = axios[method.toLowerCase()](requestURL , data) ;

if(responseHeaders){

    return result.then(({
        data,
        headers
    }) => {
        data,
        headers
    }) ;
}

return result.then(({
    data
}) => data) ;

    }

                return function(url , config){

                    
        if(!var_init_locked_1561952819823){

            request = include('src::data.connection.ajax.request');

            var_init_locked_1561952819823 = true ;
        }
        

                    

                    return main.call(this , url , config) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561690886707" signature="<void> src::data.connection.ajax.request.miniprogram(<string> url , <object> config)" entry-type="string object"  name="src::data.connection.ajax.request.miniprogram" imports="src::is.type src::is.string src::url.append src::regexp.int src::url.template.apply src::is.object.simple src::data.connection.ajax.request">
        <![CDATA[(() =>{

                let request;

                let var_init_locked_1561952819831;

                

                

                function main(url , config){

        
/**
 * 
 * 基于小程序的 AJAX 请求实现
 * 
 * @import request from ....request
 * 
 * @param {string} url 请求路径
 * 
 * @param {object} [config] 请求配置
 * 
 */

let {
    url:requestURL,
    method,
    data,
    form,
    responseHeaders
} = request(url , config),
header = {};

if(form === true){

    header['content-type'] = 'application/x-www-form-urlencoded' ;
}

return new Promise(callback =>{

    wx.request({
        header,
        url:requestURL,
        method,
        data,
        success({
            data,
            header
        }){
    
            if(responseHeaders){

                callback({
                    data,
                    header
                }) ;
            
            }else{

                callback(data) ;
            }
        }
    }) ;

}) ;




    }

                return function(url , config){

                    
        if(!var_init_locked_1561952819831){

            request = include('src::data.connection.ajax.request');

            var_init_locked_1561952819831 = true ;
        }
        

                    

                    return main.call(this , url , config) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348019070" signature="<mixed> src::is.function(<mixed> data)" entry-type="mixed"  name="src::is.function" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952819834;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为函数类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {mixed} 如果给定值为函数类型则返回 true , 否则返回 false
 * 
 */

return isType(data , 'function') && data.ZBEE_CLASS !== true;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819834){

            isType = include('is.type');

            var_init_locked_1561952819834 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529932" signature="<boolean> src::is.boolean(<mixed> data)" entry-type="mixed"  name="src::is.boolean" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952819836;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为布尔类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为布尔类型则返回 true , 否则返回 false 
 * 
 */

return isType(data , 'boolean') ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819836){

            isType = include('is.type');

            var_init_locked_1561952819836 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557476977736" signature="<function> src::class.empty()"  name="src::class.empty">
        <![CDATA[(() =>{

                

                

                

                let var_once_value_1561952819839;

                function main(){

        
/**
 * 
 * 返回一个空类
 * 
 * @once
 * 
 * @return {function} 类引用 
 * 
 */

 return class {

 } ;

    }

                return function(){

                    

                    

                    
        if(var_once_value_1561952819839){

            return var_once_value_1561952819839 ;

        }
        return var_once_value_1561952819839 = main.call(this ) ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302720" signature="<void> src::map.constructor()"  name="src::map.constructor">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 初始化 Map 对象
 * 
 */

this.map = new Map() ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302757" signature="<number> src::map.size.get()"  name="src::map.size.get">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 获得当前 Map 的键值对数量
 * 
 * @return {number} 数量 
 * 
 */

return this.map.size ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302729" signature="<object> src::map.find(<array> keys)" entry-type="array"  name="src::map.find">
        <![CDATA[(() =>{

                

                

                

                

                function main(keys){

        
/**
 * 
 * 判断指定组合键是否存在
 * 
 * @param {array} keys 组合键
 * 
 * @return {object} 返回查询结果 
 * 
 */

let me = this,
{
    map
} = me,
currentKeys = map.keys(),
{
    length
} = keys;

for(let groupKeys  of currentKeys){

   if(length === groupKeys.length){

       let isMatch = true ;

       for(let i = 0 ; i < length ; i ++){

           if(groupKeys[i] !== keys[i]){

               isMatch = false ;

               break ;
           }
       }

       if(isMatch){

           return {
               match:true,
               key:groupKeys
           } ;
       }
   }
}

return {
    match:false
} ;

    }

                return function(keys){

                    

                    

                    return main.call(this , keys) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302747" signature="<Map> src::map.set(<array> ...values)" entry-type="array"  name="src::map.set" imports="src::map.find">
        <![CDATA[(() =>{

                let find;

                let var_init_locked_1561952819853;

                let var_current_scope_1561952819853;

                

                function main(...values){

        
/**
 * 
 * 设置一个值
 * 
 * @import find from .find scoped
 * 
 * @param {array} [...values] 包含多维键，以及相应值
 * 
 * @return {Map} 返回当前对象 
 * 
 */

 let me = this,
 {
    map
 } = me,
 {
     length
 } = values;

 if(length >= 2){

    let keys = values.slice(0 , length - 1),
        value = values[length - 1],
        {
            match,
            key
        } = find(keys);

    if(match){

        map.set(key ,value) ;
    
    }else{

        map.set(keys , value) ;
    }
 }

 return me ;

    }

                return function(...values){

                    

                    
        if(!var_current_scope_1561952819853 !== this){

            find = include('src::map.find').bind(this);

            var_current_scope_1561952819853 = this ;
        }
        

                    return main.call(this , ...values) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302738" signature="<boolean> src::map.get(<array> ...keys)" entry-type="array"  name="src::map.get" imports="src::map.find">
        <![CDATA[(() =>{

                let find;

                let var_init_locked_1561952819857;

                let var_current_scope_1561952819857;

                

                function main(...keys){

        
/**
 * 
 * 判断指定组合键是否存在
 * 
 * @import find from .find scoped
 * 
 * @param {array} [...keys] 组合键
 * 
 * @return {boolean} 如果组合键存在，则返回 true , 否则返回 false 
 * 
 */

let me = this,
{
    map
} = me ;

let {
    match,
    key
} = find(keys) ;

if(match){

    return map.get(key) ;
}

    }

                return function(...keys){

                    

                    
        if(!var_current_scope_1561952819857 !== this){

            find = include('src::map.find').bind(this);

            var_current_scope_1561952819857 = this ;
        }
        

                    return main.call(this , ...keys) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302743" signature="<boolean> src::map.has(<array> ...keys)" entry-type="array"  name="src::map.has" imports="src::map.find">
        <![CDATA[(() =>{

                let find;

                let var_init_locked_1561952819860;

                let var_current_scope_1561952819860;

                

                function main(...keys){

        
/**
 * 
 * 判断指定组合键是否存在
 * 
 * @import find from .find scoped
 * 
 * @param {array} [...keys] 组合键
 * 
 * @return {boolean} 如果组合键存在，则返回 true , 否则返回 false 
 * 
 */

 let {
    match
 } = find(keys) ;

 return match ;

    }

                return function(...keys){

                    

                    
        if(!var_current_scope_1561952819860 !== this){

            find = include('src::map.find').bind(this);

            var_current_scope_1561952819860 = this ;
        }
        

                    return main.call(this , ...keys) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302724" signature="<boolean> src::map.delete(<array> ...keys)" entry-type="array"  name="src::map.delete" imports="src::map.find">
        <![CDATA[(() =>{

                let find;

                let var_init_locked_1561952819866;

                let var_current_scope_1561952819866;

                

                function main(...keys){

        

/**
 * 
 * 删除指定组合键
 * 
 * @import find from .find scoped
 * 
 * @param {array} [...keys] 组合键
 * 
 * @return {boolean} 如果组合键存在，则返回 true , 否则返回 false 
 * 
 */

let me = this,
{
    map
} = me ;

let {
    match,
    key
} = find(keys) ;

if(match){

    return map.delete(key) ;
}

return false ;

    }

                return function(...keys){

                    

                    
        if(!var_current_scope_1561952819866 !== this){

            find = include('src::map.find').bind(this);

            var_current_scope_1561952819866 = this ;
        }
        

                    return main.call(this , ...keys) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302734" signature="<void> src::map.forEach(<function> fn , <mixed> scope)" entry-type="function mixed"  name="src::map.forEach">
        <![CDATA[(() =>{

                

                

                

                

                function main(fn , scope){

        
/**
 * 
 * 实现迭代
 * 
 * @param {function} fn 函数引用
 * 
 * @param {mixed} scope 函数作用域
 * 
 */

 let {
    map
 } = this ;

 map.forEach(fn , scope) ;

    }

                return function(fn , scope){

                    

                    

                    return main.call(this , fn , scope) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557191505875" signature="<void> src::map.clear()"  name="src::map.clear">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 清空
 * 
 */

 this.map.clear() ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557109445628" signature="<void> src::map()"  name="src::map" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear">
        <![CDATA[(() =>{

                let extend,constructor,get_size,method_set,method_get,method_has,method_delete,method_forEach,method_clear;

                let var_init_locked_1561952819875;

                let var_class_1561952819875;

                

                return function(){

                    
        if(!var_init_locked_1561952819875){

            extend = include('class.empty')();
constructor = include('src::map.constructor');
get_size = include('src::map.size.get');
method_set = include('src::map.set');
method_get = include('src::map.get');
method_has = include('src::map.has');
method_delete = include('src::map.delete');
method_forEach = include('src::map.forEach');
method_clear = include('src::map.clear');

            var_init_locked_1561952819875 = true ;
        }
        

                    
        if(!var_class_1561952819875){

            class main {

            

            

            constructor(...args){

            

            constructor.apply(this , args) ;

        }

            set(...args){

            return method_set.apply(this , args) ;

        }
get(...args){

            return method_get.apply(this , args) ;

        }
has(...args){

            return method_has.apply(this , args) ;

        }
delete(...args){

            return method_delete.apply(this , args) ;

        }
forEach(...args){

            return method_forEach.apply(this , args) ;

        }
clear(...args){

            return method_clear.apply(this , args) ;

        }

            get size(){

                return get_size.call(this) ;
    
            }

        }

            var_class_1561952819875 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952819875 ;
                }

            } ;
        }
        

                    return new var_class_1561952819875();
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558404985000" signature="<void> src::mixin.observable.constructor()"  name="src::mixin.observable.constructor" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map">
        <![CDATA[(() =>{

                let createMap;

                let var_init_locked_1561952819880;

                

                

                
/**
 * 
 * 初始化观察者
 * 
 * @import createMap from map
 * 
 */

const EventEmitter = require('events') ;

function main(){

    let me = this ;

    me.emitter = new EventEmitter() ;

    me.listeners = createMap() ;

    me.cacheFireEventDataList = [] ;
}



                return function(){

                    
        if(!var_init_locked_1561952819880){

            createMap = include('map');

            var_init_locked_1561952819880 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558406649686" signature="<mixed> src::mixin.observable.fire.data.last()"  name="src::mixin.observable.fire.data.last">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 获得最近的触发事件数据
 * 
 * @return {mixed} 如果有的话则返回一个参数数组，如果没有则返回 false 
 * 
 */

 let {
    cacheFireEventDataList
 } = this,
 {
    length
 } = cacheFireEventDataList;

 if(length){

    return cacheFireEventDataList[length - 1] ;
 }

 return false ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556603892887" signature="<void> src::mixin.observable.listeners.clear.all()"  name="src::mixin.observable.listeners.clear.all">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 清除所有事件监听
 * 
 */

 this.emitter.removeAllListeners() ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556603892882" signature="<void> src::mixin.observable.listeners.clear(<string> event)" entry-type="string"  name="src::mixin.observable.listeners.clear">
        <![CDATA[(() =>{

                

                

                

                

                function main(event){

        
/**
 * 
 * 清除指定事件上的所有事件监听
 * 
 * @param {string} event 事件名称
 * 
 */

this.emitter.removeAllListeners(event) ;

    }

                return function(event){

                    

                    

                    return main.call(this , event) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557469986815" signature="<void> src::mixin.observable.listeners.add(<object> listeners)" entry-type="object"  name="src::mixin.observable.listeners.add" imports="src::is.object.simple src::is.type src::is.function">
        <![CDATA[(() =>{

                let isObject,isFunction;

                let var_init_locked_1561952819893;

                

                

                function main(listeners){

        
/**
 * 
 * 添加一组事件监听
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.function
 * 
 * @param {object} listeners 事件监听配置
 * 
 */

let me = this,
    events = Object.keys(listeners),
    {
        scope,
        ...realListeners
    } = listeners;

listeners = realListeners ;

for(let event of events){

    let listener = listeners[event] ;

    if(isObject(listener)){

        let {
            fn,
            scope:listenerScope,
            ...options
        } = listener ;

        me.addListener(event , fn , listenerScope || scope , options) ;

    }else{

        me.addListener(event , listener , scope) ;
    }
}

    }

                return function(listeners){

                    
        if(!var_init_locked_1561952819893){

            isObject = include('is.object.simple');
isFunction = include('is.function');

            var_init_locked_1561952819893 = true ;
        }
        

                    

                    return main.call(this , listeners) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556603892892" signature="<void> src::mixin.observable.listeners.remove(<object> listeners)" entry-type="object"  name="src::mixin.observable.listeners.remove">
        <![CDATA[(() =>{

                

                

                

                

                function main(listeners){

        
/**
 * 
 * 去除一组事件监听
 * 
 * @param {object} listeners 事件监听
 * 
 */

let me = this,
    events = Object.keys(listeners) ;

for(let event of events){

    let listener = listeners[event] ;

    if(isObject(listener)){

        let {
            fn
        } = listener ;

        me.removeListener(event , fn) ;

    }else if(isFunction(listener)){

        me.removeListener(event , listener) ;
    }
}

    }

                return function(listeners){

                    

                    

                    return main.call(this , listeners) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529913" signature="<void> src::function.empty()"  name="src::function.empty">
        <![CDATA[(() =>{

                

                

                

                

                /**
 * 
 * 返回一个空函数
 * 
 * @scoped
 * 
 */

const emptyFn = () =>{
} ;

function main(){

    return emptyFn ;
}

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557381709000" signature="<function> src::function.get(<string|function> fn , <mixed> scope)" entry-type="string mixed"  name="src::function.get" imports="src::is.type src::is.string src::is.function src::function.empty">
        <![CDATA[(() =>{

                let isString,isFunction,empty;

                let var_init_locked_1561952819902;

                

                

                function main(fn , scope){

        /**
 * 
 * 获得一个函数引用
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import empty from function.empty
 * 
 * @param {string | function} fn 函数描述
 * 
 * @param {mixed} [scope] 函数作用域
 * 
 * @return {function} 函数引用本身 
 * 
 */

if(isString(fn)){

    if(scope && fn in scope){

        fn = scope[fn] ;

    }else{

        fn = include(fn) ;
    }
}

if(isFunction(fn)){

    if(scope){

        return fn.bind(scope) ;
    }

    return fn ;
}

return empty() ;

    }

                return function(fn , scope){

                    
        if(!var_init_locked_1561952819902){

            isString = include('is.string');
isFunction = include('is.function');
empty = include('function.empty');

            var_init_locked_1561952819902 = true ;
        }
        

                    

                    return main.call(this , fn , scope) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558513515480" signature="<void> src::mixin.observable.listener.add(<string> event , <mixed> fn , <mixed> scope , {<boolean> once , <boolean> getOldFireEventData})"  name="src::mixin.observable.listener.add" imports="src::is.type src::is.string src::is.function src::function.empty src::function.get">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952819907;

                

                

                function main(event , fn , scope , {once , getOldFireEventData}){

        
/**
 * 
 * 添加事件监听
 * 
 * @import get from function.get
 * 
 * @param {string} event 事件名称
 * 
 * @param {mixed} fn 事件回调函数
 * 
 * @param {mixed} [scope] 事件作用域
 * 
 * @param {object} [options = {}] 配置
 * 
 * @param {boolean} [options.once = false] 是否只监听一次
 * 
 * @param {boolean} [options.getOldFireEventData] 是否获取以前的触发事件数据
 * 
 */

let me = this,
{
    emitter,
    listeners
} = me,
listener = get(fn , scope);

listeners.set(event , fn , scope , listener) ;

switch(getOldFireEventData){

    case 'last':

        let {
            lastFireEventData
        } = me ;


        if(lastFireEventData){

            listener(...lastFireEventData) ;
        }

        break ;

    case 'all':

        let {
            cacheFireEventDataList
        } = me ;

        for(let cacheFireEventData of cacheFireEventDataList){

            listener(...cacheFireEventData) ;
        }
}

if(once){
    
     emitter.once(event , listener) ;

 }else{

     emitter.addListener(event , listener) ;
}

    }

                return function(event , fn , scope , {once = false , getOldFireEventData} = {}){

                    
        if(!var_init_locked_1561952819907){

            get = include('function.get');

            var_init_locked_1561952819907 = true ;
        }
        

                    

                    return main.call(this , event , fn , scope , {once , getOldFireEventData}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556603892870" signature="<void> src::mixin.observable.listener.remove(<string> event , <function> fn , <mixed> scope)" entry-type="string function mixed"  name="src::mixin.observable.listener.remove">
        <![CDATA[(() =>{

                

                

                

                

                function main(event , fn , scope){

        
/**
 * 
 * 移除事件监听
 * 
 * @param {string} event 事件名称
 * 
 * @param {function} fn 事件回调函数
 * 
 * @param {mixed} [scope] 事件作用域
 * 
 * 
 */

let me = this,
{
    listeners,
    emitter
} = me,
listener = listeners.get(event , fn , scope) ;

if(listener){

    emitter.removeListener(event , listener) ;

    listeners.delete(event , fn , scope) ;
}

    }

                return function(event , fn , scope){

                    

                    

                    return main.call(this , event , fn , scope) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558401820267" signature="<boolean> src::mixin.observable.listener.has(<string> name)" entry-type="string"  name="src::mixin.observable.listener.has">
        <![CDATA[(() =>{

                

                

                

                

                function main(name){

        
/**
 * 
 * 查看指定事件名称上有无事件监听
 * 
 * @param {string} name 事件名称
 * 
 * @return {boolean} 如果事件上有监听则返回 true , 否则返回 false 
 * 
 */

 let {
    emitter
 } = this ;

 return emitter.listenerCount(name) !== 0 ;

    }

                return function(name){

                    

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560481458584" signature="<void> src::mixin.observable.fire(<string> event , <mixed> ...args)" entry-type="string mixed"  name="src::mixin.observable.fire" imports="src::is.type src::is.array">
        <![CDATA[(() =>{

                let isArray;

                let var_init_locked_1561952819915;

                

                

                
/**
 *
 * 触发事件
 * 
 * @import is.array
 *
 * @param {string} event 事件名称
 *
 * @param {mixed} [...args] 事件参数
 *
 */

 function main(event , ...args){

    let me = this ;

    if(me.$suspendEvents === true){

        return ;
    }

    doFireBubbleEvent.call(me , event , me , ...args) ;
 }

 function doFireBubbleEvent(event , target , ...args){

    let me = this,{
        bubbleTarget,
        emitter,
        fireEventDataCacheCount = 0,
        cacheFireEventDataList
    } = me ;

    if(fireEventDataCacheCount !== 0){

        cacheFireEventDataList.push([
            target,
            ...args
        ]) ;

        let deleteCount =  cacheFireEventDataList.length - fireEventDataCacheCount;

        if(deleteCount > 0){

            cacheFireEventDataList.splice(0 , deleteCount) ;
        }
    }

    emitter.emit(event , target ,  ...args) ;

    if(bubbleTarget){

        doFireBubbleEvent.call(bubbleTarget , event , target , ...args) ;
    }
 }

                return function(event , ...args){

                    
        if(!var_init_locked_1561952819915){

            isArray = include('is.array');

            var_init_locked_1561952819915 = true ;
        }
        

                    

                    return main.call(this , event , ...args) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560481372948" signature="<void> src::mixin.observable.events.suspend()"  name="src::mixin.observable.events.suspend">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 暂停事件监听
 * 
 */

this.$suspendEvents = true ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560481383341" signature="<void> src::mixin.observable.events.resume()"  name="src::mixin.observable.events.resume">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 恢复事件监听
 * 
 */

 this.$suspendEvents = false ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559192268469" signature="<void> src::mixin.observable(<mixed> extend)"  name="src::mixin.observable" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume">
        <![CDATA[(() =>{

                let constructor,get_lastFireEventData,method_clearAllListeners,method_clearListeners,method_addListeners,method_removeListeners,method_addListener,method_removeListener,method_hasListener,method_fireEvent,method_suspendEvents,method_resumeEvents;

                let var_init_locked_1561952819927;

                

                

                function main(extend){

                        return class extends extend{

                            
                
                            
                
                            constructor(...args){

            super(...args) ;

            constructor.apply(this , args) ;

        }
                
                            clearAllListeners(...args){

            return method_clearAllListeners.apply(this , args) ;

        }
clearListeners(...args){

            return method_clearListeners.apply(this , args) ;

        }
addListeners(...args){

            return method_addListeners.apply(this , args) ;

        }
removeListeners(...args){

            return method_removeListeners.apply(this , args) ;

        }
addListener(...args){

            return method_addListener.apply(this , args) ;

        }
on(...args){

                return this.addListener(...args) ;
    
            }
removeListener(...args){

            return method_removeListener.apply(this , args) ;

        }
un(...args){

                return this.removeListener(...args) ;
    
            }
hasListener(...args){

            return method_hasListener.apply(this , args) ;

        }
fireEvent(...args){

            return method_fireEvent.apply(this , args) ;

        }
suspendEvents(...args){

            return method_suspendEvents.apply(this , args) ;

        }
resumeEvents(...args){

            return method_resumeEvents.apply(this , args) ;

        }
                
                            get lastFireEventData(){

                return get_lastFireEventData.call(this) ;
    
            }
                
                        }

                }

                return function(extend){

                    
        if(!var_init_locked_1561952819927){

            constructor = include('src::mixin.observable.constructor');
get_lastFireEventData = include('src::mixin.observable.fire.data.last');
method_clearAllListeners = include('src::mixin.observable.listeners.clear.all');
method_clearListeners = include('src::mixin.observable.listeners.clear');
method_addListeners = include('src::mixin.observable.listeners.add');
method_removeListeners = include('src::mixin.observable.listeners.remove');
method_addListener = include('src::mixin.observable.listener.add');
method_removeListener = include('src::mixin.observable.listener.remove');
method_hasListener = include('src::mixin.observable.listener.has');
method_fireEvent = include('src::mixin.observable.fire');
method_suspendEvents = include('src::mixin.observable.events.suspend');
method_resumeEvents = include('src::mixin.observable.events.resume');

            var_init_locked_1561952819927 = true ;
        }
        

                    

                    return main.call(this , extend) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560158091516" signature="<void> src::data.subscriber.constructor(<string> name , {<boolean> accumulationMode , <object> extraParams , <object> defaultParams})"  name="src::data.subscriber.constructor" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map">
        <![CDATA[(() =>{

                let createMap;

                let var_init_locked_1561952819936;

                

                

                function main(name , {accumulationMode , extraParams , defaultParams}){

        
/**
 * 
 * 初始化订阅器
 * 
 * @import createMap from map
 * 
 * @param {string} name 订阅名称
 * 
 * @param {object} config 订阅器配置
 * 
 * @param {boolean} [config.accumulationMode = false] 是否启动累积模型，当启动累积模型后则所有的接收消息都会被缓存，默认为 false
 * 
 * @param {object} [config.extraParams = {}] 附加参数
 * 
 * @param {object} [config.defaultParams = {}] 默认参数
 * 
 */

let me = this ;

me.name = name ;

me.extraParams = extraParams ;

me.defaultParams = defaultParams ;

me.accumulationMode = accumulationMode ;

me.cache = [] ;

me.bindCallbacks = createMap() ;

me.closed = true ;

    }

                return function(name , {accumulationMode = false , extraParams = {} , defaultParams = {}}){

                    
        if(!var_init_locked_1561952819936){

            createMap = include('map');

            var_init_locked_1561952819936 = true ;
        }
        

                    

                    return main.call(this , name , {accumulationMode , extraParams , defaultParams}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302675" signature="<boolean> src::is.defined(<mixed> data)" entry-type="mixed"  name="src::is.defined">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        /**
 * 
 * 判断给定数据是否定义
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果数据定义则返回 true , 否则返回 false
 * 
 */

return data !== undefined ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559716967440" signature="<void> src::data.subscriber.accept(<mixed> data)" entry-type="mixed"  name="src::data.subscriber.accept" imports="src::is.defined">
        <![CDATA[(() =>{

                let isDefined;

                let var_init_locked_1561952819945;

                

                

                function main(data){

        
/**
 * 
 * 接收数据
 * 
 * @import is.defined
 * 
 * @param {mixed} data 数据
 * 
 * 
 */

let {
    closed,
    bindCallbacks,
    accumulationMode,
    cache,
    params
} = this,
results = [];

if(closed){

    return results;
}

bindCallbacks.forEach(callback => {

    let result = callback(data , params) ;

    if(isDefined(result)){

        results.push(result) ;
    }

}) ;

if(accumulationMode === false){

    cache.length = 0 ;
}

cache.push({
    params,
    data
}) ;

return results ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819945){

            isDefined = include('is.defined');

            var_init_locked_1561952819945 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717839544" signature="<void> src::data.subscriber.bind(<fucntion> fn , <mixed> scope)" entry-type="fucntion mixed"  name="src::data.subscriber.bind" imports="src::is.type src::is.string src::is.function src::function.empty src::function.get">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952819948;

                

                

                function main(fn , scope){

        
/**
 * 
 * 绑定函数
 * 
 * @import get from function.get
 * 
 * @param {fucntion} fn 绑定函数
 * 
 * @param {mixed} scope 绑定函数作用域
 * 
 */

let me = this,{
    bindCallbacks,
    cache
} = me;

fn = get(fn , scope) ;

for(let {
    data,
    params
} of cache){

    fn(data , params) ;
}

bindCallbacks.set(fn , scope , fn) ;

return me;

    }

                return function(fn , scope){

                    
        if(!var_init_locked_1561952819948){

            get = include('function.get');

            var_init_locked_1561952819948 = true ;
        }
        

                    

                    return main.call(this , fn , scope) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717646420" signature="<void> src::data.subscriber.reopen()"  name="src::data.subscriber.reopen">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 重新打开订阅器
 * 
 */

let me = this,
{
    closed,
    params
} = me ;

if(!closed){

    me.close() ;

    me.open(params) ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302910" signature="<array> src::object.keys(<object> data)" entry-type="object"  name="src::object.keys" imports="src::is.object.simple">
        <![CDATA[(() =>{

                let isObject;

                let var_init_locked_1561952819954;

                

                

                /**
 * 
 * 获取对象的键值组合
 * 
 * @import isObject from is.object.simple
 * 
 * @param {object} data 对象
 * 
 * @return {array} 键值数组
 * 
 * @scoped
 * 
 */

function main(data){

    return get_keys(data) ;
}

function get_keys(data , rootKey = ''){

    let keys = Object.keys(data),
        result = [];

    for(let key of keys){

        let value = data[key] ;

        if(isObject(value)){

            result.push(...get_keys(value , `${rootKey}${key}.`)) ;
        
        }else{

            result.push(`${rootKey}${key}`) ;
        }
    }

    return result ;
}


                return function(data){

                    
        if(!var_init_locked_1561952819954){

            isObject = include('is.object.simple');

            var_init_locked_1561952819954 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529939" signature="<boolean> src::is.object(<mixed> data)" entry-type="mixed"  name="src::is.object" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952819956;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为对象类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为对象类型则返回 true , 否则返回 false 
 * 
 */

return Object.prototype.toString.call(data) === '[object Object]' ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952819956){

            isType = include('is.type');

            var_init_locked_1561952819956 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302906" signature="<string> src::object.key.join(<array> ...keys)" entry-type="array"  name="src::object.key.join">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 将多个键值连接起来
 * 
 * @param {array} [...keys] 一组键值
 * 
 * @return {string} 连接后的键值 
 * 
 */

const suffixRe = /(?:^\.+)|(?:\.+$)/g ;

function main(...keys){

    let result = [] ;

    for(let key of keys){

        key = key.replace(suffixRe , '') ;

        if(key){

            result.push(key) ;
        }
    }

    return result.join('.') ;
}



                return function(...keys){

                    

                    

                    return main.call(this , ...keys) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557285054070" signature="<mixed> src::object.value.get(<object> data , <string> key)" entry-type="object string"  name="src::object.value.get" imports="src::is.type src::is.array src::is.object src::is.defined src::object.key.join">
        <![CDATA[(() =>{

                let isArray,isObject,isDefined,join;

                let var_init_locked_1561952819962;

                

                

                
/**
 * 
 * 获得一个对象的键值
 * 
 * @import is.array
 * 
 * @import is.object
 * 
 * @import is.defined
 * 
 * @import join from object.key.join
 * 
 * @param {object} data 对象数据
 * 
 * @param {string} [key = '.'] 对象键值
 * 
 * @return {mixed} 对应对象数据的键值的数据 
 * 
 * @scoped
 * 
 */

const firstKeyRe = /^([^\.]+)\./;

function main(data , key , prefixKey = ''){

    if(key === '.'){

        return data ;
    }

    if(isObject(data)){

        let firstKey,
            lastKey = key.replace(firstKeyRe , function(){

                firstKey = arguments[1] ;
                
                return '' ;

            }) ;

        if(firstKey){

            firstKey = join(prefixKey , firstKey) ;

            let result ;

            if(data.hasOwnProperty(firstKey)){

                result = data[firstKey] ;

                prefixKey = '' ;

            }else{

                result = data ;

                prefixKey = firstKey ;
            }

            if(lastKey){

                return main(result , lastKey , prefixKey) ;
            }
        
            return result ;
        
        }else{

            return data[join(prefixKey , lastKey)] ;
        }

    }else if(isArray(data)){

        let result = [] ;

        for(let item of data){

            let itemResult = main(item , key) ;

            if(isArray(itemResult)){

                result.push(...itemResult) ;
            
            }else if(!isDefined(itemResult)){

                result.push(itemResult) ;

            }
        }

        return result ;
    }
}

                return function(data , key = '.'){

                    
        if(!var_init_locked_1561952819962){

            isArray = include('is.array');
isObject = include('is.object');
isDefined = include('is.defined');
join = include('object.key.join');

            var_init_locked_1561952819962 = true ;
        }
        

                    

                    return main.call(this , data , key) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557284779712" signature="<boolean> src::object.equals(<object> value1 , <object> value2)" entry-type="object object"  name="src::object.equals" imports="src::is.object.simple src::object.keys src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get">
        <![CDATA[(() =>{

                let keys,get;

                let var_init_locked_1561952819966;

                

                

                function main(value1 , value2){

        /**
 * 
 * 匹配两个对象数据是否匹配
 * 
 * @import keys from object.keys
 * 
 * @import get from object.value.get
 * 
 * @param {object} value1 第一个对象数据
 * 
 * @param {object} value2 第二个对象数据
 * 
 * @return {boolean} 如果对象数据匹配则返回 true ， 否则返回 false
 * 
 */

let keys1 = keys(value1),
    keys2 = keys(value2) ;

if(keys1.length !== keys2.length){

    return false ;
}

for(let key of keys1){

    if(!keys2.includes(key) || get(value1 , key) !== get(value2 , key)){

        return false ;
    }
}

return true ;

    }

                return function(value1 , value2){

                    
        if(!var_init_locked_1561952819966){

            keys = include('object.keys');
get = include('object.value.get');

            var_init_locked_1561952819966 = true ;
        }
        

                    

                    return main.call(this , value1 , value2) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529996" signature="<void> src::string.split(<String> target , <RegEx> splitRe)" entry-type="String RegEx"  name="src::string.split" imports="src::is.type src::is.array src::is.empty">
        <![CDATA[(() =>{

                let isEmpty;

                let var_init_locked_1561952819970;

                

                

                
/**
 * 
 * 将字符串通过间隔符分隔成数组
 * 
 * @import is.empty
 * 
 * @param {String} target 字符串
 * 
 * @param {RegEx} splitRe 字符串分隔符
 * 
 * @scoped
 * 
 */

function main(target , splitRe){

    return target.split(splitRe).filter(filter) ;
 }

 function filter(target){

    return !isEmpty(target) ;
 }

                return function(target , splitRe){

                    
        if(!var_init_locked_1561952819970){

            isEmpty = include('is.empty');

            var_init_locked_1561952819970 = true ;
        }
        

                    

                    return main.call(this , target , splitRe) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557285048407" signature="<void> src::object.value.set(<object> target , <string> key , <mixed> value)" entry-type="object string mixed"  name="src::object.value.set" imports="src::is.type src::is.object src::is.array src::is.empty src::string.split">
        <![CDATA[(() =>{

                let isObject,split;

                let var_init_locked_1561952819975;

                

                

                /**
 * 
 * 设置对象的属性值
 * 
 * @import is.object
 * 
 * @import split from string.split
 * 
 * @param {object} target 目标对象
 * 
 * @param {string} key 属性名称
 * 
 * @param {mixed} value 属性值
 * 
 * @scoped
 * 
 */

const splitRe = /\./;

function main(target , key , value){

    if(splitRe.test(key)){

        let keys = split(key , splitRe) ;
    
        key = keys.pop();
    
        for(let key of keys){
    
            let data = target[key] ;
    
            if(!isObject(data)){
    
                data = target[key] = {} ;
            }
    
            target = data ;
        }
    
        target[key] = value ;
    
    }else{
    
        target[key] = value ;
    }
}   

                return function(target , key , value){

                    
        if(!var_init_locked_1561952819975){

            isObject = include('is.object');
split = include('string.split');

            var_init_locked_1561952819975 = true ;
        }
        

                    

                    return main.call(this , target , key , value) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557285029502" signature="<object> src::object.assign(<object> dest , <object> ...sources)" entry-type="object object"  name="src::object.assign" imports="src::is.object.simple src::object.keys src::is.type src::is.object src::is.array src::is.empty src::string.split src::object.value.set src::is.defined src::object.key.join src::object.value.get">
        <![CDATA[(() =>{

                let getKeys,set,get;

                let var_init_locked_1561952819979;

                

                

                
/**
 * 
 * 深度合并
 * 
 * @import getKeys from object.keys
 * 
 * @import set from object.value.set
 * 
 * @import get from object.value.get
 * 
 * @param {object} dest 目标数据
 * 
 * @param {object} [...sources] 来源数据
 * 
 * @return {object} 合并后数据
 * 
 * @scoped
 * 
 */

function assign(dest , source){

    let keys = getKeys(source) ;

    for(let key of keys){

        set(dest , key , get(source , key)) ;
    }

}

function main(dest , ...sources){

    for(let source of sources){

        assign(dest , source) ;
    }

    return dest ;

}

                return function(dest , ...sources){

                    
        if(!var_init_locked_1561952819979){

            getKeys = include('object.keys');
set = include('object.value.set');
get = include('object.value.get');

            var_init_locked_1561952819979 = true ;
        }
        

                    

                    return main.call(this , dest , ...sources) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560157843606" signature="<void> src::data.subscriber.open(<object> params)" entry-type="object"  name="src::data.subscriber.open" imports="src::is.object.simple src::object.keys src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign">
        <![CDATA[(() =>{

                let equals,assign;

                let var_init_locked_1561952819986;

                

                

                function main(params){

        
/**
 * 
 * 打开订阅器
 * 
 * @import equals from object.equals
 * 
 * @import assign from object.assign
 * 
 * @param {object} [params = {}] 订阅参数
 * 
 */

let me = this,
{
    extraParams,
    defaultParams,
    params:oldParams
} = me ;

params = assign({} , defaultParams , params , extraParams) ;

if(!oldParams || !equals(params , oldParams)){

    me.close() ;

    me.params = params ;

    me.closed = false ;
    
    me.fireEvent('open' , params , oldParams) ;
}

    }

                return function(params = {}){

                    
        if(!var_init_locked_1561952819986){

            equals = include('object.equals');
assign = include('object.assign');

            var_init_locked_1561952819986 = true ;
        }
        

                    

                    return main.call(this , params) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560158075765" signature="<void> src::data.subscriber.close()"  name="src::data.subscriber.close">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 关闭订阅器
 * 
 */

let me = this,
{
    closed,
    params,
    cache
} = me;

if(closed){

    return ;
}

delete me.params ;

cache.length = 0 ;

me.closed = true ;

me.fireEvent('close' , params) ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717802351" signature="<void> src::data.subscriber.destroy()"  name="src::data.subscriber.destroy">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 销毁
 * 
 */

let me = this ;

me.close() ;

let {
     bindCallbacks
} = this ;

bindCallbacks.clear() ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560136555092" signature="<void> src::data.subscriber()"  name="src::data.subscriber" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy">
        <![CDATA[(() =>{

                let mixin_1561952819323__1,extend,constructor,method_accept,method_bind,method_reopen,method_open,method_close,method_destroy;

                let var_init_locked_1561952819997;

                let var_class_1561952819997;

                

                return function(){

                    
        if(!var_init_locked_1561952819997){

            mixin_1561952819323__1 = include('mixin.observable');
extend = include('class.empty')();
constructor = include('src::data.subscriber.constructor');
method_accept = include('src::data.subscriber.accept');
method_bind = include('src::data.subscriber.bind');
method_reopen = include('src::data.subscriber.reopen');
method_open = include('src::data.subscriber.open');
method_close = include('src::data.subscriber.close');
method_destroy = include('src::data.subscriber.destroy');

            var_init_locked_1561952819997 = true ;
        }
        

                    
        if(!var_class_1561952819997){

            class main extends mixins({extend , mixins:[include('mixin.observable')]}){

            

            

            constructor(...args){

            super(...args) ;

            constructor.apply(this , args) ;

        }

            accept(...args){

            return method_accept.apply(this , args) ;

        }
bind(...args){

            return method_bind.apply(this , args) ;

        }
reopen(...args){

            return method_reopen.apply(this , args) ;

        }
open(...args){

            return method_open.apply(this , args) ;

        }
close(...args){

            return method_close.apply(this , args) ;

        }
destroy(...args){

            return method_destroy.apply(this , args) ;

        }

            

        }

            var_class_1561952819997 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952819997 ;
                }

            } ;
        }
        

                    return var_class_1561952819997;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348016117" signature="<boolean> src::is.class(<mixed> data)" entry-type="mixed"  name="src::is.class" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952820013;

                

                

                function main(data){

        
/**
 * 
 * 判断数据是否为类
 * 
 * @import is.type
 * 
 * @param {mixed} data 检测数据
 * 
 * @return {boolean} 如果指定数据是类则返回 true ，否则返回 false 
 * 
 */

return isType(data , 'function') && data.ZBEE_CLASS === true ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952820013){

            isType = include('is.type');

            var_init_locked_1561952820013 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558417859298" signature="<void> src::class.create(<mixed> baseClass , <mixed> ...args)" entry-type="mixed mixed"  name="src::class.create" imports="src::is.type src::is.string src::is.function src::is.class">
        <![CDATA[(() =>{

                let isString,isFunction,isClass;

                let var_init_locked_1561952820015;

                

                

                function main(baseClass , ...args){

        
/**
 * 
 * 实例基于指定类对象
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import is.class
 * 
 * @param {mixed} baseClass 类
 * 
 * @param {mixed} [...args] 类参数
 * 
 */

if(isString(baseClass)){

    baseClass = include(baseClass) ;
}

if(isFunction(baseClass)){

    baseClass = baseClass(...args) ;

    if(!isClass(baseClass)){

        return baseClass ;
    }
}

if(isClass(baseClass)){

    return new baseClass(...args) ;
}

    }

                return function(baseClass , ...args){

                    
        if(!var_init_locked_1561952820015){

            isString = include('is.string');
isFunction = include('is.function');
isClass = include('is.class');

            var_init_locked_1561952820015 = true ;
        }
        

                    

                    return main.call(this , baseClass , ...args) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560408116778" signature="<void> src::data.connection.base()"  name="src::data.connection.base" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create" dependent-modules="regex-parser:^2.2.10">
        <![CDATA[(() =>{

                let isObject,isString,isFunction,isBoolean,isArray,Subscriber,get,create;

                let var_init_locked_1561952820020;

                let var_class_1561952820020;

                

                return function(){

                    
        if(!var_init_locked_1561952820020){

            isObject = include('is.object.simple');
isString = include('is.string');
isFunction = include('is.function');
isBoolean = include('is.boolean');
isArray = include('is.array');
Subscriber = include('data.subscriber')();
get = include('function.get');
create = include('class.create');

            var_init_locked_1561952820020 = true ;
        }
        

                    
        if(!var_class_1561952820020){

            /**
 * 
 * 数据连接基础类
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import is.boolean
 * 
 * @import is.array
 * 
 * @import Subscriber from data.subscriber value
 * 
 * @import get from function.get
 * 
 * @import create from class.create
 * 
 * @require regex-parser
 * 
 * @class
 * 
 */

 const createRegex = require('regex-parser'); 

 function createRules(rules){

    let result = [] ;

    for(let {
        test,
        use
    } of rules){

        if(isFunction(use)){

            result.push({
                test:createRegex(test),
                use
            }) ;
        }
    }

    return result ;
 }

 class main{

    constructor({
        subscriber = (name , options) =>{

            return new Subscriber(name , options) ;
        },
        rules = []
    }){

        let me = this ;

        me.subscriber = subscriber ;

        me.subscribers = new Map() ;

        me.rules = createRules(rules) ;
    }

    processMessage(...args){

        return {} ;
    }

    processData(subscriber , {
        data
    }){

        return data ;
    }

    validateMessage(subscriber , message){

        return true ;
    }


    processSubscribeParams(subscriber , params){

        return [
            params
        ] ;
    }

    /**
     * 
     * 构建订阅器
     * 
     * @param {string} name  订阅名称 
     * 
     * @param {mixed} options 订阅器参数
     * 
     * @return {data.Subscriber} 订阅器
     * 
     */
    createSubscriber(name , options){

        let me = this,
        {
            subscriber
        } = me ;

        return create(subscriber , name , options) ;
    }

    get subscriberListeners(){

        return {
            open:'onSubscriberOpen',
            close:'onSubscriberClose',
            scope:this
        } ;
    }

    onSubscriberOpen(subscriber , params){

        let me = this;

        params = me.processSubscribeParams(subscriber , params , 'open') ;

        if(isArray(params)){

            me.doSubscriberOpen(subscriber , ...params) ;
        }
    }

    doSubscriberOpen(subscriber , ...args){


    }

    onSubscriberClose(subscriber , params){

        let me = this;

        params = me.processSubscribeParams(subscriber , params , 'close') ;

        if(isArray(params)){

            me.doSubscriberClose(subscriber , ...params) ;
        }

    }

    doSubscriberClose(subscriber , ...args){


    }

    /**
     * 
     * @param {string} name 订阅器名称
     *  
     * @param {string} method 订阅器方法名称
     *  
     * @param  {mixed} ...args 订阅器方法参数
     */
    doSubscriberMethod(name , method , ...args){

        let me = this,
        {
            subscribers
        } = me,
        subscriber = subscribers.get(name);

        if(subscriber){

            subscriber[method](...args) ;

            return subscriber ;
        }
    }
 }

            var_class_1561952820020 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820020 ;
                }

            } ;
        }
        

                    return var_class_1561952820020;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560408205924" signature="<void> src::data.connection.subscribe(<string> name , <object> options)" entry-type="string object"  name="src::data.connection.subscribe" imports="src::is.object.simple src::object.keys src::is.type src::is.object src::is.array src::is.empty src::string.split src::object.value.set src::is.defined src::object.key.join src::object.value.get src::object.assign">
        <![CDATA[(() =>{

                let assign;

                let var_init_locked_1561952820037;

                

                

                
/**
 * 
 * 单次订阅
 * 
 * @import assign from object.assign
 * 
 * @param {string} name 订阅名称
 * 
 * @param {object} [options = {}] 订阅配置
 * 
 */

function main(name , options){

    let me = this ;

    options = assign({} , convertNameToSubscriberOptions.call(me , name) , options) ;

    let {
        subscribers,
        subscriberListeners
    } = me,
    {
        fn,
        scope,
        params,
        listeners = {},
        autoOpen = true,
        ...currentOptions
    } = options;

    if(subscribers.has(name)){

        return subscribers.get(name) ;
    }

    let subscriber = me.createSubscriber(name , currentOptions) ;

    subscriber.addListeners(subscriberListeners) ;

    subscriber.addListeners(listeners) ;

    subscribers.set(name , subscriber) ;

    if(fn){

        subscriber.bind(fn , scope) ;
    }

    if(autoOpen){

        subscriber.open(params) ;
    }

    return subscriber ;
}

function convertNameToSubscriberOptions(name){

    let {
        rules
    } = this;

    for(let {
        test,
        use
    } of rules){

        let args = name.match(test) ;

        if(args){

            return use(...args) ;
        }
    }

    return {} ;
 }

                return function(name , options = {}){

                    
        if(!var_init_locked_1561952820037){

            assign = include('object.assign');

            var_init_locked_1561952820037 = true ;
        }
        

                    

                    return main.call(this , name , options) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559720437004" signature="<void> src::data.connection.unsubscribe(<string> name)" entry-type="string"  name="src::data.connection.unsubscribe">
        <![CDATA[(() =>{

                

                

                

                

                function main(name){

        
/**
 * 
 * 取消单次订阅
 * 
 * @param {string} name 订阅名称
 * 
 */

let me = this,
    {
        subscribers
    } = me,
    subscriber = me.doSubscriberMethod(name , 'destroy') ;

if(subscriber){

    subscribers.delete(name) ;
}

    }

                return function(name){

                    

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560329035374" signature="<mixed> src::data.connection.subscribes(<object> config)" entry-type="object"  name="src::data.connection.subscribes" imports="src::is.type src::is.string src::is.function src::is.object.simple src::function.empty src::function.get">
        <![CDATA[(() =>{

                let isString,isFunction,isObject,get;

                let var_init_locked_1561952820047;

                

                

                function main(config){

        
/**
 * 
 * 批量订阅
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import isObject from is.object.simple
 * 
 * @import get from function.get
 * 
 * @param {object} config 批量订阅配置
 * 
 * @return {mixed} 返回说明 
 * 
 */

let {
    scope,
    ...subscribers
} = config,
me = this,
names = Object.keys(subscribers),
result = {};

for(let name of names){

    let target = subscribers[name],
        subscriber;

    if(isString(target) || isFunction(target)){

        subscriber = me.subscribe(name).bind(get(subscribers[name] , scope) , scope) ;

    }else if(isObject(target)){

        let {
            fn,
            scope:currentScope,
            listeners = {},
            ...options
        } = target ;

        currentScope = currentScope || scope ;

        listeners.scope = listeners.scope || currentScope ;

        subscriber = me.subscribe(name , {
            fn,
            scope:currentScope,
            listeners,
            ...options
        }) ;
    }

    if(subscriber){

        result[name] = subscriber ;
    }
}

return result ;

    }

                return function(config){

                    
        if(!var_init_locked_1561952820047){

            isString = include('is.string');
isFunction = include('is.function');
isObject = include('is.object.simple');
get = include('function.get');

            var_init_locked_1561952820047 = true ;
        }
        

                    

                    return main.call(this , config) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560132184951" signature="<void> src::data.connection.unsubscribes(<string[]> names)" entry-type="string[]"  name="src::data.connection.unsubscribes">
        <![CDATA[(() =>{

                

                

                

                

                function main(names){

        
/**
 * 
 * 批量取消订阅
 * 
 * @param {string[]} names 订阅名称
 * 
 */

let me = this;

for(let name of names){

   me.unsubscribe(name) ;
}

    }

                return function(names){

                    

                    

                    return main.call(this , names) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560147254532" signature="<void> src::data.connection.resubscribes()"  name="src::data.connection.resubscribes">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 重新订阅
 * 
 */

let {
    subscribers
} = this;

subscribers.forEach(subscriber => subscriber.reopen()) ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560148434618" signature="<void> src::data.connection()"  name="src::data.connection" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create src::data.connection.base src::data.connection.accept src::data.connection.subscribe src::data.connection.unsubscribe src::data.connection.subscribes src::data.connection.unsubscribes src::data.connection.resubscribes">
        <![CDATA[(() =>{

                let extend,method_acceptMessage,method_subscribe,method_unsubscribe,method_subscribes,method_unsubscribes,method_resubscribes;

                let var_init_locked_1561952820056;

                let var_class_1561952820056;

                

                return function(){

                    
        if(!var_init_locked_1561952820056){

            extend = include('src::data.connection.base')();
method_acceptMessage = include('src::data.connection.accept');
method_subscribe = include('src::data.connection.subscribe');
method_unsubscribe = include('src::data.connection.unsubscribe');
method_subscribes = include('src::data.connection.subscribes');
method_unsubscribes = include('src::data.connection.unsubscribes');
method_resubscribes = include('src::data.connection.resubscribes');

            var_init_locked_1561952820056 = true ;
        }
        

                    
        if(!var_class_1561952820056){

            class main extends mixins({extend , mixins:[]}){

            

            

            

            acceptMessage(...args){

            return method_acceptMessage.apply(this , args) ;

        }
subscribe(...args){

            return method_subscribe.apply(this , args) ;

        }
unsubscribe(...args){

            return method_unsubscribe.apply(this , args) ;

        }
subscribes(...args){

            return method_subscribes.apply(this , args) ;

        }
unsubscribes(...args){

            return method_unsubscribes.apply(this , args) ;

        }
resubscribes(...args){

            return method_resubscribes.apply(this , args) ;

        }

            

        }

            var_class_1561952820056 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820056 ;
                }

            } ;
        }
        

                    return var_class_1561952820056;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561690280923" signature="<void> src::data.connection.ajax(<object> options)" entry-type="object"  name="src::data.connection.ajax" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create src::data.connection.base src::data.connection.accept src::data.connection.subscribe src::data.connection.unsubscribe src::data.connection.subscribes src::data.connection.unsubscribes src::data.connection.resubscribes src::data.connection" dependent-modules="url-join:^4.0.0">
        <![CDATA[(() =>{

                let Connection;

                let var_init_locked_1561952820069;

                let var_class_1561952820069;

                

                return function(options){

                    
        if(!var_init_locked_1561952820069){

            Connection = include('data.connection')();

            var_init_locked_1561952820069 = true ;
        }
        

                    
        if(!var_class_1561952820069){

            
/**
 * 
 * 基于 AJAX 进行数据交互
 * 
 * @import Connection from data.connection value
 * 
 * @require url-join
 * 
 * @param {object} options 配置
 * 
 */

 const join = require('url-join') ;

 class main extends Connection{

    constructor({
        ajax
    }){

        super() ;

        let me = this,
        {
            url:ajaxURL,
            request
        } = ajax ;

        me.ajaxURL = ajaxURL ;

        me.request = request ;
    }

    processSubscribeParams(subscriber , params){

        let {
            ajaxURL
        } = this ;

        return [
            join(ajaxURL , subscriber.name),
            params
        ] ;
    }

    validateMessage(subscriber , {
        subscriber:messageSubscriber
    }){

        return subscriber === messageSubscriber ;
    }

    processMessage(subscriber , data){

        return {
            subscriber,
            data
        } ;
    }

    async doSubscriberOpen(subscriber , url , params){

        this.acceptMessage(subscriber , await request(url , params)) ;

    }
 }


            var_class_1561952820069 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820069 ;
                }

            } ;
        }
        

                    return new var_class_1561952820069(options);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560158091516" signature="<void> src::data.Subscriber.constructor(<string> name , {<boolean> accumulationMode , <object> extraParams , <object> defaultParams})"  name="src::data.Subscriber.constructor" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map">
        <![CDATA[(() =>{

                let createMap;

                let var_init_locked_1561952820083;

                

                

                function main(name , {accumulationMode , extraParams , defaultParams}){

        
/**
 * 
 * 初始化订阅器
 * 
 * @import createMap from map
 * 
 * @param {string} name 订阅名称
 * 
 * @param {object} config 订阅器配置
 * 
 * @param {boolean} [config.accumulationMode = false] 是否启动累积模型，当启动累积模型后则所有的接收消息都会被缓存，默认为 false
 * 
 * @param {object} [config.extraParams = {}] 附加参数
 * 
 * @param {object} [config.defaultParams = {}] 默认参数
 * 
 */

let me = this ;

me.name = name ;

me.extraParams = extraParams ;

me.defaultParams = defaultParams ;

me.accumulationMode = accumulationMode ;

me.cache = [] ;

me.bindCallbacks = createMap() ;

me.closed = true ;

    }

                return function(name , {accumulationMode = false , extraParams = {} , defaultParams = {}}){

                    
        if(!var_init_locked_1561952820083){

            createMap = include('map');

            var_init_locked_1561952820083 = true ;
        }
        

                    

                    return main.call(this , name , {accumulationMode , extraParams , defaultParams}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559716967440" signature="<void> src::data.Subscriber.accept(<mixed> data)" entry-type="mixed"  name="src::data.Subscriber.accept" imports="src::is.defined">
        <![CDATA[(() =>{

                let isDefined;

                let var_init_locked_1561952820088;

                

                

                function main(data){

        
/**
 * 
 * 接收数据
 * 
 * @import is.defined
 * 
 * @param {mixed} data 数据
 * 
 * 
 */

let {
    closed,
    bindCallbacks,
    accumulationMode,
    cache,
    params
} = this,
results = [];

if(closed){

    return results;
}

bindCallbacks.forEach(callback => {

    let result = callback(data , params) ;

    if(isDefined(result)){

        results.push(result) ;
    }

}) ;

if(accumulationMode === false){

    cache.length = 0 ;
}

cache.push({
    params,
    data
}) ;

return results ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952820088){

            isDefined = include('is.defined');

            var_init_locked_1561952820088 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717839544" signature="<void> src::data.Subscriber.bind(<fucntion> fn , <mixed> scope)" entry-type="fucntion mixed"  name="src::data.Subscriber.bind" imports="src::is.type src::is.string src::is.function src::function.empty src::function.get">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952820091;

                

                

                function main(fn , scope){

        
/**
 * 
 * 绑定函数
 * 
 * @import get from function.get
 * 
 * @param {fucntion} fn 绑定函数
 * 
 * @param {mixed} scope 绑定函数作用域
 * 
 */

let me = this,{
    bindCallbacks,
    cache
} = me;

fn = get(fn , scope) ;

for(let {
    data,
    params
} of cache){

    fn(data , params) ;
}

bindCallbacks.set(fn , scope , fn) ;

return me;

    }

                return function(fn , scope){

                    
        if(!var_init_locked_1561952820091){

            get = include('function.get');

            var_init_locked_1561952820091 = true ;
        }
        

                    

                    return main.call(this , fn , scope) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717646420" signature="<void> src::data.Subscriber.reopen()"  name="src::data.Subscriber.reopen">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 重新打开订阅器
 * 
 */

let me = this,
{
    closed,
    params
} = me ;

if(!closed){

    me.close() ;

    me.open(params) ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560157843606" signature="<void> src::data.Subscriber.open(<object> params)" entry-type="object"  name="src::data.Subscriber.open" imports="src::is.object.simple src::object.keys src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign">
        <![CDATA[(() =>{

                let equals,assign;

                let var_init_locked_1561952820097;

                

                

                function main(params){

        
/**
 * 
 * 打开订阅器
 * 
 * @import equals from object.equals
 * 
 * @import assign from object.assign
 * 
 * @param {object} [params = {}] 订阅参数
 * 
 */

let me = this,
{
    extraParams,
    defaultParams,
    params:oldParams
} = me ;

params = assign({} , defaultParams , params , extraParams) ;

if(!oldParams || !equals(params , oldParams)){

    me.close() ;

    me.params = params ;

    me.closed = false ;
    
    me.fireEvent('open' , params , oldParams) ;
}

    }

                return function(params = {}){

                    
        if(!var_init_locked_1561952820097){

            equals = include('object.equals');
assign = include('object.assign');

            var_init_locked_1561952820097 = true ;
        }
        

                    

                    return main.call(this , params) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560158075765" signature="<void> src::data.Subscriber.close()"  name="src::data.Subscriber.close">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 关闭订阅器
 * 
 */

let me = this,
{
    closed,
    params,
    cache
} = me;

if(closed){

    return ;
}

delete me.params ;

cache.length = 0 ;

me.closed = true ;

me.fireEvent('close' , params) ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559717802351" signature="<void> src::data.Subscriber.destroy()"  name="src::data.Subscriber.destroy">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 销毁
 * 
 */

let me = this ;

me.close() ;

let {
     bindCallbacks
} = this ;

bindCallbacks.clear() ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560136555092" signature="<void> src::data.Subscriber()"  name="src::data.Subscriber" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.Subscriber.constructor src::is.defined src::data.Subscriber.accept src::data.Subscriber.bind src::data.Subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.Subscriber.open src::data.Subscriber.close src::data.Subscriber.destroy">
        <![CDATA[(() =>{

                let mixin_1561952819382__1,extend,constructor,method_accept,method_bind,method_reopen,method_open,method_close,method_destroy;

                let var_init_locked_1561952820109;

                let var_class_1561952820109;

                

                return function(){

                    
        if(!var_init_locked_1561952820109){

            mixin_1561952819382__1 = include('mixin.observable');
extend = include('class.empty')();
constructor = include('src::data.Subscriber.constructor');
method_accept = include('src::data.Subscriber.accept');
method_bind = include('src::data.Subscriber.bind');
method_reopen = include('src::data.Subscriber.reopen');
method_open = include('src::data.Subscriber.open');
method_close = include('src::data.Subscriber.close');
method_destroy = include('src::data.Subscriber.destroy');

            var_init_locked_1561952820109 = true ;
        }
        

                    
        if(!var_class_1561952820109){

            class main extends mixins({extend , mixins:[include('mixin.observable')]}){

            

            

            constructor(...args){

            super(...args) ;

            constructor.apply(this , args) ;

        }

            accept(...args){

            return method_accept.apply(this , args) ;

        }
bind(...args){

            return method_bind.apply(this , args) ;

        }
reopen(...args){

            return method_reopen.apply(this , args) ;

        }
open(...args){

            return method_open.apply(this , args) ;

        }
close(...args){

            return method_close.apply(this , args) ;

        }
destroy(...args){

            return method_destroy.apply(this , args) ;

        }

            

        }

            var_class_1561952820109 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820109 ;
                }

            } ;
        }
        

                    return var_class_1561952820109;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560146137621" signature="<void> src::data.connection.message.address(<string> name , <object> options)" entry-type="string object"  name="src::data.connection.message.address" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.Subscriber.constructor src::is.defined src::data.Subscriber.accept src::data.Subscriber.bind src::data.Subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.Subscriber.open src::data.Subscriber.close src::data.Subscriber.destroy src::data.Subscriber">
        <![CDATA[(() =>{

                let Subscriber;

                let var_init_locked_1561952820120;

                let var_class_1561952820120;

                

                return function(name , options){

                    
        if(!var_init_locked_1561952820120){

            Subscriber = include('data.Subscriber')();

            var_init_locked_1561952820120 = true ;
        }
        

                    
        if(!var_class_1561952820120){

            
/**
 * 
 * 消息地址
 * 
 * @import Subscriber from data.Subscriber value
 * 
 * @param {string} name 消息地址
 * 
 * @param {object} options 消息配置
 * 
 */

 class main extends Subscriber{

    send(to , data){

        let me = this ;

        me.fireEvent('send' , {
            from:me.name,
            to,
            data
        }) ;
    }

    accept(data){

        let me = this,
            results = super.accept(data),
            {
                from
            } = data;

        if(from){

            for(let result of results){

                if(result instanceof Promise){

                    result.then(data => me.send(from , data)) ;
                
                }else{

                    me.send(from , result) ;
                }
            }
        }
    }
 }

 

            var_class_1561952820120 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820120 ;
                }

            } ;
        }
        

                    return new var_class_1561952820120(name , options);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561620937494" signature="<void> src::data.connection.socket()"  name="src::data.connection.socket" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create src::data.connection.base src::data.connection.accept src::data.connection.subscribe src::data.connection.unsubscribe src::data.connection.subscribes src::data.connection.unsubscribes src::data.connection.resubscribes src::data.connection">
        <![CDATA[(() =>{

                let Connection,isObject,getKeys,getValue,equals;

                let var_init_locked_1561952820130;

                let var_class_1561952820130;

                

                return function(){

                    
        if(!var_init_locked_1561952820130){

            Connection = include('data.connection')();
isObject = include('is.object.simple');
getKeys = include('object.keys');
getValue = include('object.value.get');
equals = include('object.equals');

            var_init_locked_1561952820130 = true ;
        }
        

                    
        if(!var_class_1561952820130){

            
/**
 * 
 * Socket 通信
 * 
 * @import Connection from data.connection value
 * 
 * @import isObject from is.object.simple
 * 
 * @import getKeys from object.keys
 * 
 * @import getValue from object.value.get
 * 
 * @import equals from object.equals
 * 
 * @class
 * 
 */

 class main extends Connection{

    validateMessage({
        params:baseParams
    },{
        params:equalParams
    }){

        if(isObject(baseParams) && isObject(equalParams)){

            let names = Object.keys(equalParams) ;

            for(let name of names){

                if(baseParams[name] !== equalParams[name]){

                    return false ;
                }
            }

            return true ;
        }

        return false ;
    }
 }

            var_class_1561952820130 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820130 ;
                }

            } ;
        }
        

                    return var_class_1561952820130;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560146723970" signature="<void> src::data.connection.message()"  name="src::data.connection.message" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create src::data.connection.base src::data.connection.accept src::data.connection.subscribe src::data.connection.unsubscribe src::data.connection.subscribes src::data.connection.unsubscribes src::data.connection.resubscribes src::data.connection src::data.connection.socket src::data.Subscriber.constructor src::data.Subscriber.accept src::data.Subscriber.bind src::data.Subscriber.reopen src::data.Subscriber.open src::data.Subscriber.close src::data.Subscriber.destroy src::data.Subscriber src::data.connection.message.address">
        <![CDATA[(() =>{

                let Socket,isObject,isString,createAddress,isDefined;

                let var_init_locked_1561952820143;

                let var_class_1561952820143;

                

                return function(){

                    
        if(!var_init_locked_1561952820143){

            Socket = include('data.connection.socket')();
isObject = include('is.object.simple');
isString = include('is.string');
createAddress = include('data.connection.message.address');
isDefined = include('is.defined');

            var_init_locked_1561952820143 = true ;
        }
        

                    
        if(!var_class_1561952820143){

            /**
 * 
 * 消息订阅
 * 
 * @import Socket from data.connection.socket value
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.string
 * 
 * @import createAddress from data.connection.message.address
 * 
 * @import is.defined
 * 
 * @singleton
 * 
 */

 class main extends Socket{

    constructor(){

        super({
            subscriber:createAddress
        }) ;
    }

    processMessage({
        from,
        to,
        data
    }){

        return {
            params:{
                address:to
            },
            data:{
                from,
                to,
                data
            }
        } ;
    }

    get subscriberListeners(){

        return {
            ...super.subscriberListeners,
            send:'onMessageSend'
        } ;
    }

    subscribe(name){

        return super.subscribe(name , {
            params:{
                address:name
            }
        }) ;
    }

    onMessageSend(address , message){

        this.acceptMessage(message) ;
    }

    send(address , data){

        if(isString(address)){

            address = {
                to:address
            } ;
        }

        if(isObject(address)){

            if(!address.hasOwnProperty('data')){

                address = {
                    ...address,
                    data
                } ;
            }

            this.acceptMessage(address) ;
        }
    }
 }

            var_class_1561952820143 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820143 ;
                }

            } ;
        }
        

                    return new var_class_1561952820143();
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561617535593" signature="<mixed> src::data.connection.miniprogram(<object> mixins , <mixed> page)" entry-type="object mixed"  name="src::data.connection.miniprogram" imports="src::function.empty">
        <![CDATA[(() =>{

                let empty;

                let var_init_locked_1561952820160;

                

                

                function main(mixins , page){

        
/**
 * 
 * 基于数据连接的 VUE 封装
 * 
 * @import empty from function.empty value
 * 
 * @param {object} mixins Socket 对象
 * 
 * @param {mixed} page 组件定义对象
 * 
 * @return {mixed} 组件定义对象
 * 
 */

 const {
    socket,
    ajax
 } = mixins,{
    onLoad = empty,
    onUnload = empty,
    subscribers = {},
    loaders = {},
    ...options
 } = page,
 {
    keys
 } = Object;

 return {
    onLoad(){

        let me = this;

        if(socket){
            
            me.$subscribers = socket.subscribes({
                ...subscribers,
                scope:me
            }) ;
        }

        if(ajax){

            me.$loaders = ajax.subscribes({
                ...loaders,
                scope:me
            }) ;
        }

        onLoad.call(me) ;
    },

    onUnload(){

        let me = this ;

        onUnload.call(me) ;

        if(socket){

            socket.unsubscribes(keys(subscribers)) ;
        }

        if(ajax){

            ajax.unsubscribes(keys(loaders)) ;
        }
    },

    ...options
 } ;

    }

                return function(mixins , page){

                    
        if(!var_init_locked_1561952820160){

            empty = include('function.empty')();

            var_init_locked_1561952820160 = true ;
        }
        

                    

                    return main.call(this , mixins , page) ;
                } ;

            })()]]>
    </class>

    <class standard="no" motify="1560332229758" signature="<mixed> src::data.connection.react(<object> mixins , <mixed> componentClass)" entry-type="object mixed"  name="src::data.connection.react" imports="src::is.type src::is.string src::is.object.simple">
        <![CDATA[(() =>{

                let isString,isObject;

                let var_init_locked_1561952820167;

                

                

                function main(mixins , componentClass){

        /**
 * 
 * 基于数据连接的 React 封装
 * 
 * @import is.string
 * 
 * @import isObject from is.object.simple
 * 
 * @param {object} mixins Socket 对象
 * 
 * @param {mixed} componentClass 组件类引用
 * 
 * @return {mixed} 组件类引用
 * 
 * @non-standard
 * 
 */

 const {
     socket,
     ajax
 } = mixins;

class Component extends componentClass{

    constructor(props){

        super(props) ;

        this.connection_subscribed = false ;
    }

    componentDidMount() {

        let me = this,
        {
            connection_subscribed
        } = me;

        if(!connection_subscribed){

            if(socket){
                
                let {
                    subscribers = {}
                } = me ;

                me.subscribers = socket.subscribes({
                    ...subscribers,
                    scope:me
                }) ;
            }

            if(ajax){
        
                let {
                    loaders = {}
                } = me ;

                me.loaders = ajax.subscribes({
                    ...loaders,
                    scope:me
                }) ;
            }

            me.connection_subscribed = true ;
        }

        if (super.componentDidMount) {

            super.componentDidMount();
        }
    }

    componentWillUnmount(){

        if (super.componentWillUnmount) {

            super.componentWillUnmount();
        }

        let me = this,
        {
            connection_subscribed
        } = me;

        if(connection_subscribed){

            let {
                subscribers = {},
                loaders = {}
            } = me,
            {
                keys
            } = Object;

            if(socket){

                socket.unsubscribes(keys(subscribers)) ;
            }

            if(ajax){

                ajax.unsubscribes(keys(loaders)) ;
            }

            me.connection_subscribed = false ;
        }
    }
    
} ;

return props => <Component {...props} socket={socket} ajax={ajax}/> ;

    }

                return function(mixins , componentClass){

                    
        if(!var_init_locked_1561952820167){

            isString = include('is.string');
isObject = include('is.object.simple');

            var_init_locked_1561952820167 = true ;
        }
        

                    

                    return main.call(this , mixins , componentClass) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561618827692" signature="<void> src::data.connection.socket.io()"  name="src::data.connection.socket.io" imports="src::is.object.simple src::is.type src::is.string src::is.function src::is.boolean src::is.array src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::data.subscriber.constructor src::is.defined src::data.subscriber.accept src::data.subscriber.bind src::data.subscriber.reopen src::object.keys src::is.object src::object.key.join src::object.value.get src::object.equals src::is.empty src::string.split src::object.value.set src::object.assign src::data.subscriber.open src::data.subscriber.close src::data.subscriber.destroy src::data.subscriber src::is.class src::class.create src::data.connection.base src::data.connection.accept src::data.connection.subscribe src::data.connection.unsubscribe src::data.connection.subscribes src::data.connection.unsubscribes src::data.connection.resubscribes src::data.connection src::data.connection.socket" dependent-modules="socket.io-client:1.3.3">
        <![CDATA[(() =>{

                let Connection;

                let var_init_locked_1561952820171;

                let var_class_1561952820171;

                

                return function(){

                    
        if(!var_init_locked_1561952820171){

            Connection = include('data.connection.socket')();

            var_init_locked_1561952820171 = true ;
        }
        

                    
        if(!var_class_1561952820171){

            /**
 * 
 * 基于 socket.io 标准进行开发
 * 
 * @import Connection from data.connection.socket value
 * 
 * @require socket.io-client
 * 
 * @class
 * 
 */

 const IO = require('socket.io-client') ;

 class main extends Connection{

    constructor({
        socket,
        ...options
    }){

        super(options) ;

        let {
            url:socketURL,
            options:socketOptions
        } = socket ;

        let me = this,
            {
                messageEventName,
                acceptMessage,
                resubscribes,
                onConnect
            } = me ;

        socket = me.socket = IO(socketURL , {
            'force new connection': true,
            transports: [
                'websocket',
                'polling'
            ],
            ...socketOptions
        }) ;

        socket.on(messageEventName , acceptMessage.bind(me)) ;

        socket.on('reconnect' , resubscribes.bind(me)) ;

        socket.once('connect' , onConnect.bind(me)) ;

        me.firstConnected = false ;
    }

    onConnect(){

        this.firstConnected = true ;
    }

    get messageEventName(){

        return 'msg' ;
    }


    get subscribeEventName(){

        return 'sub' ;
    }

    get unsubscribeEventName(){

        return 'unsub' ;
    }


    emit(event , ...params){

        let me = this,
        {
            firstConnected,
            socket
        } = me;

        if(firstConnected){

            if(socket.connected){

                socket.emit(event , ...params) ;
            }

        }else{

            socket.once('connect' , () => me.emit(event , ...params)) ;
        }
    }

    doSubscriberOpen(subscriber , ...args){

        let me = this,
        {
            subscribeEventName
        } = me ;

        me.emit(subscribeEventName , ...args) ;
    }

    doSubscriberClose(subscriber , ...args){

        let me = this,
        {
            unsubscribeEventName
        } = me ;

        me.emit(unsubscribeEventName , ...args) ;
    }
 }

            var_class_1561952820171 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820171 ;
                }

            } ;
        }
        

                    return var_class_1561952820171;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302097" signature="<void> src::data.connection.socket.standard(<string> url , <object> config)" entry-type="string object"  name="src::data.connection.socket.standard" imports="src::timer">
        <![CDATA[(() =>{

                let createTimer;

                let var_init_locked_1561952820187;

                

                

                /**
 * 
 * 基于标准 WebSocket 进行开发
 * 
 * @import createTimer from timer
 * 
 * @param {string} url socket 连接地址
 * 
 * @param {object} config socket 连接配置
 * 
 */

function main(url , options = {}){

    return new Socket(url , options) ;
}

const EventEmitter = require('events') ;

class Socket extends EventEmitter{

    constructor(url , options){

        super() ;

        let me = this ;

        me.url = url;

        me.options = options ;

        let {
            timeout = 20000,
            parse
        } = options ;

        me.timer = createTimer(timeout) ;

        createSocket.call(me) ;

        let {
            socket
        } = me ;

        socket.addEventListener('message' , ({
            data
        }) =>{

            let {
                event,
                params
            } = parse(data);

            me.emit(event , params) ;

        }) ;

        let onException =() => reconnect.call(me) ;

        me.once('connect' , () => {

            socket.addEventListener('error' , onException) ;

            socket.addEventListener('close' , onException) ;

        }) ;
    }

    get connected(){

        let {
            socket
        } = this ;

        return socket.readyState === 1 ;
    }

    emit(event , ...args){

        let me = this,
        {
            connected,
            options,
            socket
        } = me,
        {
            stringify
        } = options;

        if(connected){

            socket.send(stringify(event , args)) ;
        
        }else{

            me.on('connect' , () => me.emit(event , ...args)) ;
        }
    }

    on(eventName , fn){

        this.addListener(eventName , fn) ;
    }
}

function reconnect(){

    let me = this;

    if(me.connected){

        me.socket.close() ;
    }

    return createSocket.call(me) ;
}

function createSocket(){

    let me = this,
        socket = me.socket = new WebSocket(url),
        {
            timer
        } = me,
        onConnectError = () => me.emit('connect_error');

    timer.start() ;

    timer.on('timeout' , () => me.emit('connect_timeout')) ;

    socket.addEventListener('open' , () =>{

        socket.removeEventListener('error' , onConnectError) ;

        timer.end() ;

        me.emit('connect') ;

    }) ;

    socket.addEventListener('error' , onConnectError) ;

    return me.socket ;
}

                return function(url , config){

                    
        if(!var_init_locked_1561952820187){

            createTimer = include('timer');

            var_init_locked_1561952820187 = true ;
        }
        

                    

                    return main.call(this , url , config) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560410065076" signature="<mixed> src::data.connection.vue(<object> mixins , <mixed> component)" entry-type="object mixed"  name="src::data.connection.vue" imports="src::function.empty">
        <![CDATA[(() =>{

                let empty;

                let var_init_locked_1561952820191;

                

                

                function main(mixins , component){

        
/**
 * 
 * 基于数据连接的 VUE 封装
 * 
 * @import empty from function.empty value
 * 
 * @param {object} mixins Socket 对象
 * 
 * @param {mixed} component 组件定义对象
 * 
 * @return {mixed} 组件定义对象
 * 
 */

 const {
    socket,
    ajax
 } = mixins,{
    mounted = empty,
    destroyed = empty,
    subscribers = {},
    loaders = {},
    ...options
 } = component,
 {
    keys
 } = Object;

 return {
    mounted(){

        let me = this;

        if(socket){
            
            me.$subscribers = socket.subscribes({
                ...subscribers,
                scope:me
            }) ;
        }

        if(ajax){

            me.$loaders = ajax.subscribes({
                ...loaders,
                scope:me
            }) ;
        }

        mounted.call(me) ;
    },

    destroyed(){

        let me = this ;

        destroyed.call(me) ;

        if(socket){

            socket.unsubscribes(keys(subscribers)) ;
        }

        if(ajax){

            ajax.unsubscribes(keys(loaders)) ;
        }
    },

    ...options
 } ;

    }

                return function(mixins , component){

                    
        if(!var_init_locked_1561952820191){

            empty = include('function.empty')();

            var_init_locked_1561952820191 = true ;
        }
        

                    

                    return main.call(this , mixins , component) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558502542731" signature="<mixed> src::class.create.option(<string> namespace , <mixed> option)" entry-type="string mixed"  name="src::class.create.option" imports="src::is.type src::is.string src::is.object.simple src::is.class src::is.function src::class.create src::is.defined">
        <![CDATA[(() =>{

                let isString,isObject,isClass,create,isDefined;

                let var_init_locked_1561952820194;

                

                

                function main(namespace , option){

        /**
 * 
 * 基于配置的实例
 * 
 * @import is.string
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.class
 * 
 * @import create from class.create
 * 
 * @import is.defined
 * 
 * @param {string} namespace 命名空间
 * 
 * @param {mixed}  [option] 配置
 * 
 * @return {mixed} 类实例
 * 
 */

 if(isString(option)){

    return create(`${namespace}.${option}`) ;

 }else if(isObject(option)){

    let {
        type,
        ...currentOptions
    } = option,
    name;

    if(type){

        name = `${namespace}.${type}` ;
    
    }else{

        name = namespace ;
    }

    return create(name , currentOptions) ;
 
}else if(!isDefined(options)){

    return create(namespace) ;
}

    }

                return function(namespace , option){

                    
        if(!var_init_locked_1561952820194){

            isString = include('is.string');
isObject = include('is.object.simple');
isClass = include('is.class');
create = include('class.create');
isDefined = include('is.defined');

            var_init_locked_1561952820194 = true ;
        }
        

                    

                    return main.call(this , namespace , option) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302930" signature="<object.Proxy> src::object.proxy(<mixed> target)" entry-type="mixed"  name="src::object.proxy">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 对象代理，如果对象没有需要的方法或者属性时，则会抛出异常
 * 
 * @param {mixed} target 需要代理的对象
 * 
 * @return {object.Proxy} 代理对象引用 
 * 
 */

 function main(target){

    return new Proxy(target) ;
 }

 class Proxy{

    constructor(target){

        this.target = target ;
    }

    call(method , ...args){

        let {
            target
        } = this ;

        if(method in target){

            return target[method](...args) ;
        
        }else{

            throw new ProxyMethodNotFoundError(target , method) ;
        }
    }

    callIf(method , ...args){

        let {
            target
        } = this ;

        if(method in target){

            return target[method](...args) ;
        }
    }

    set(name , value){

        let {
            target
        } = this ;

        if(name in target){

            target[name] = value ;
        
        }else{

            throw new ProxyPropertyNotFoundError(target , name , 'set') ;
        }
    }

    setIf(name , value){

        let {
            target
        } = this ;

        if(name in target){

            target[name] = value ;
        
        }
    }

    get(name){

        let {
            target
        } = this ;

        if(name in target){

            return target[name] ;
        
        }else{

            throw new ProxyPropertyNotFoundError(target , name , 'get') ;
        }
    }

    getIf(name){

        let {
            target
        } = this ;

        if(name in target){

            return target[name] ;
        
        }
    }

    fireEvent(name , ...args){

        let {
            target
        } = this ;

        if('fireEvent' in target){

            target.fireEvent(name , ...args) ;
        }
    }
 }

 class ProxyMethodNotFoundError extends Error{

    constructor(target , method){

        super(`无法访问名称为 ${method} 的方法`) ;

        let me = this ;

        me.proxyTarget = target ;

        me.proxyMethod = method ;

    }
 }

 class ProxyPropertyNotFoundError extends Error{

    constructor(target , property , mode){

        let modeMessage ;

        switch(mode){

            case 'set':

                modeMessage = '设置' ;

                break ;

            case 'get':

                modeMessage = '获取' ;
        }

        super(`无法${modeMessage}名称为 ${property} 的属性`) ;

        let me = this ;

        me.proxyTarget = target ;

        me.proxyProperty = property ;

    }
 }

                return function(target){

                    

                    

                    return main.call(this , target) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558672651143" signature="<function> src::data.reader.json({<string> rootProperty , <function> model , <boolean> isModelData})"  name="src::data.reader.json" imports="src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get src::is.string src::is.function src::is.empty src::array.from src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::class.create.option src::object.proxy src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get">
        <![CDATA[(() =>{

                let objectValueGet,isString,isFunction,arrayFrom,isEmpty,getModel;

                let var_init_locked_1561952820201;

                

                

                /**
 * 
 * JSON 数据读取器
 * 
 * @import object.value.get
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import array.from
 * 
 * @import is.empty
 * 
 * @import getModel from data.model.get
 * 
 * @param {object} [config = {}] 读取参数设置
 * 
 * @param {string} [config.rootProperty = '.'] 读取数据的根
 * 
 * @param {function} [config.model] 数据模型类
 * 
 * @param {boolean} [config.isModelData = true] 是否解析成模型化的数据 
 * 
 * @return {function} 读取器所生成的解析函数
 * 
 */

 function main({
   rootProperty,
   model,
   isModelData
 }){

   const Model = getModel(model) ;

   let {
      fields
   } = Model ;

   return  (new Function('data' , `

      var me = this,
         include = me.include,
         converts = me.converts,
         createModel = me.createModel,
         get = include('object.value.get'),
         from = include('array.from'),
         isEmpty = include('is.empty');

      ${generate_get_root_data(rootProperty)}

      data = from(data) ;

      var result = [],
          len = data.length;

      for(var i = 0 ; i < len ; i ++){

         var item = {},
             currentItem = data[i];

         ${generate_get_field_data(fields)}

         result.push(createModel(item)) ;
      }

      return result;

    `)).bind({
       include,
       converts:fields.converts,
       createModel:data => isModelData ? new Model({
         innerData:data
       }) : data
    });
 }

 function generate_get_root_data(rootProperty){

   if(rootProperty !== '.'){

      return `data = get(data , '${rootProperty}');` ;
   }

   return '' ;
 }

 function generate_get_field_data({
   names
 }){

   let result = [] ;

   for(let name of names){

      result.push(`item['${name}'] = converts['${name}'](currentItem);`) ;

      result.push(`if(isEmpty(item['${name}'])){

         delete item['${name}'];
      
      }`) ;
   }

   return result.join('') ;
 }

                return function({rootProperty = '.' , model , isModelData = true} = {}){

                    
        if(!var_init_locked_1561952820201){

            objectValueGet = include('object.value.get');
isString = include('is.string');
isFunction = include('is.function');
arrayFrom = include('array.from');
isEmpty = include('is.empty');
getModel = include('data.model.get');

            var_init_locked_1561952820201 = true ;
        }
        

                    

                    return main.call(this , {rootProperty , model , isModelData}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558343966157" signature="<void> src::data.proxy.constructor({<mixed> reader , <mixed> model})"  name="src::data.proxy.constructor" imports="src::object.proxy src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get src::is.string src::is.function src::is.empty src::array.from src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::class.create.option src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json">
        <![CDATA[(() =>{

                let createProxy,createReader;

                let var_init_locked_1561952820220;

                

                

                function main({reader , model}){

        
/**
 * 
 * 初始化数据代理
 * 
 * @import createProxy from object.proxy
 * 
 * @import createReader from data.reader.json
 * 
 * @param {object} [options = {}] 配置
 * 
 * @param {mixed} [options.reader = {}] 配置读取器
 * 
 * @param {mixed} [options.model] 数据模型
 * 
 */

 let me = this ;

 me.proxy = createProxy(me) ;

 me.reader = createReader({
     ...reader,
     model
 }) ;

    }

                return function({reader = {} , model} = {}){

                    
        if(!var_init_locked_1561952820220){

            createProxy = include('object.proxy');
createReader = include('data.reader.json');

            var_init_locked_1561952820220 = true ;
        }
        

                    

                    return main.call(this , {reader , model}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558414707902" signature="<boolean> src::is.promise(<mixed> data)" entry-type="mixed"  name="src::is.promise">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 判断数据是否为 Promise 对象
 * 
 * @param {mixed} data 测试数据
 * 
 * @return {boolean} 如果是 Promise 则返回 true , 否则返回 false
 * 
 */

 return data instanceof Promise ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558419408154" signature="<void> src::data.proxy.read(<mixed> options)" entry-type="mixed"  name="src::data.proxy.read" imports="src::is.promise">
        <![CDATA[(() =>{

                let isPromise;

                let var_init_locked_1561952820238;

                

                

                
/**
 * 
 * 读取数据
 * 
 * @import is.promise
 * 
 * @param {mixed} options 读取数据配置
 * 
 */

function fireReadEvent(data){

    let me = this,
    {
        reader
    } = me ;

    me.fireEvent('read' , reader(data)) ;
}

function main(options){

    let me = this,
    {
        proxy
    } = me,
    data = proxy.call('doRead' , options);
   
    if(isPromise(data)){
   
       data.then(data => fireReadEvent.call(me , data)) ;
    
    }else{

        fireReadEvent.call(me , data) ;
    }
}




                return function(options){

                    
        if(!var_init_locked_1561952820238){

            isPromise = include('is.promise');

            var_init_locked_1561952820238 = true ;
        }
        

                    

                    return main.call(this , options) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558418019884" signature="<void> src::data.proxy()"  name="src::data.proxy" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::is.defined src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class src::class.create src::class.create.option src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read">
        <![CDATA[(() =>{

                let mixin_1561952819468__1,extend,constructor,method_read;

                let var_init_locked_1561952820240;

                let var_class_1561952820240;

                

                return function(){

                    
        if(!var_init_locked_1561952820240){

            mixin_1561952819468__1 = include('mixin.observable');
extend = include('class.empty')();
constructor = include('src::data.proxy.constructor');
method_read = include('src::data.proxy.read');

            var_init_locked_1561952820240 = true ;
        }
        

                    
        if(!var_class_1561952820240){

            class main extends mixins({extend , mixins:[include('mixin.observable')]}){

            

            

            constructor(...args){

            super(...args) ;

            constructor.apply(this , args) ;

        }

            read(...args){

            return method_read.apply(this , args) ;

        }

            

        }

            var_class_1561952820240 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820240 ;
                }

            } ;
        }
        

                    return var_class_1561952820240;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558417694274" signature="<void> src::data.proxy.memory(<object> options)" entry-type="object"  name="src::data.proxy.memory" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::is.defined src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class src::class.create src::class.create.option src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy">
        <![CDATA[(() =>{

                let Proxy;

                let var_init_locked_1561952820257;

                let var_class_1561952820257;

                

                return function(options){

                    
        if(!var_init_locked_1561952820257){

            Proxy = include('data.proxy')();

            var_init_locked_1561952820257 = true ;
        }
        

                    
        if(!var_class_1561952820257){

            
/**
 * 
 * 内存数据代理
 * 
 * @param {object} options 配置
 * 
 * @import Proxy from data.proxy value
 * 
 */

 class main extends Proxy{

    get proxyType(){

        return 'memory' ;
    }

    doRead(data){

        return data; 
    }
 }

            var_class_1561952820257 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820257 ;
                }

            } ;
        }
        

                    return new var_class_1561952820257(options);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558343632208" signature="<mixed> src::data.proxy.create(<mixed> proxy)" entry-type="mixed"  name="src::data.proxy.create" imports="src::is.type src::is.string src::is.object.simple src::is.class src::is.function src::class.create src::is.defined src::class.create.option src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory">
        <![CDATA[(() =>{

                let create,srcDataProxyMemory;

                let var_init_locked_1561952820277;

                

                

                function main(proxy){

        
/**
 * 
 * 创建新的代理
 * 
 * @import create from class.create.option
 * 
 * @import ..memory
 * 
 * @param {mixed} [proxy = 'memory'] 数据代理
 * 
 * @return {mixed} 数据代理 
 * 
 */

 return create('data.proxy' , proxy) ;

    }

                return function(proxy = 'memory'){

                    
        if(!var_init_locked_1561952820277){

            create = include('class.create.option');
srcDataProxyMemory = include('src::data.proxy.memory');

            var_init_locked_1561952820277 = true ;
        }
        

                    

                    return main.call(this , proxy) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558417920835" signature="<boolean> src::is.proxy.memory(<mixed> data)" entry-type="mixed"  name="src::is.proxy.memory" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::is.defined src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class src::class.create src::class.create.option src::data.proxy.memory src::data.proxy.create src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy">
        <![CDATA[(() =>{

                let Proxy;

                let var_init_locked_1561952820294;

                

                

                function main(data){

        
/**
 * 
 * 判断是否内存代理
 * 
 * @import Proxy from data.proxy value
 * 
 * @param {mixed} data 测试数据
 * 
 * @return {boolean} 如果为内存代理则返回 true , 否则返回 false
 * 
 */


 return data instanceof Proxy && data.proxyType === 'memory' ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952820294){

            Proxy = include('data.proxy')();

            var_init_locked_1561952820294 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557285029502" signature="<object> src::object.assign.if(<object> dest , <object> ...sources)" entry-type="object object"  name="src::object.assign.if" imports="src::is.object.simple src::object.keys src::is.type src::is.object src::is.array src::is.empty src::string.split src::object.value.set src::is.defined src::object.key.join src::object.value.get">
        <![CDATA[(() =>{

                let getKeys,set,get;

                let var_init_locked_1561952820312;

                

                

                
/**
 * 
 * 如果目标存在来源的字段，则不覆盖
 * 
 * @import getKeys from object.keys
 * 
 * @import set from object.value.set
 * 
 * @import get from object.value.get
 * 
 * @param {object} dest 目标数据
 * 
 * @param {object} [...sources] 来源数据
 * 
 * @return {object} 合并后数据
 * 
 * @scoped
 * 
 */

function assign(dest , source){

    let keys = getKeys(source),
        destKeys = getKeys(dest);

    for(let key of keys){

        if(!destKeys.includes(key)){

            set(dest , key , get(source , key)) ;
        }
    }

}

function main(dest , ...sources){

    for(let source of sources){

        assign(dest , source) ;
    }

    return dest ;

}

                return function(dest , ...sources){

                    
        if(!var_init_locked_1561952820312){

            getKeys = include('object.keys');
set = include('object.value.set');
get = include('object.value.get');

            var_init_locked_1561952820312 = true ;
        }
        

                    

                    return main.call(this , dest , ...sources) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559108613503" signature="<void> src::data.model.base()"  name="src::data.model.base" imports="src::is.type src::is.string src::is.object.simple src::is.class src::is.function src::class.create src::is.defined src::class.create.option src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::class.define src::data.model.create src::object.keys src::string.split src::object.value.set src::object.assign src::is.proxy.memory src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::object.assign.if">
        <![CDATA[(() =>{

                let isString,isObject,createProxy,isMemoryProxy,assign2;

                let var_init_locked_1561952820317;

                let var_class_1561952820317;

                

                return function(){

                    
        if(!var_init_locked_1561952820317){

            isString = include('is.string');
isObject = include('is.object.simple');
createProxy = include('data.proxy.create');
isMemoryProxy = include('is.proxy.memory');
assign2 = include('object.assign.if');

            var_init_locked_1561952820317 = true ;
        }
        

                    
        if(!var_class_1561952820317){

            
/**
 * 
 * 数据模型
 * 
 * @import is.string
 * 
 * @import isObject from is.object.simple
 * 
 * @import createProxy from data.proxy.create
 * 
 * @import isMemoryProxy from is.proxy.memory
 * 
 * @import assign2 from object.assign.if
 * 
 * @class
 * 
 */

 class main{

    constructor({
        proxy = {},
        innerData = {},
        data,
        autoLoad
    } = {}){

        let me = this,
        {
            ZBEE_CURRENT_CLASS
        } = me;

        me.fireEventDataCacheCount = 1 ;

        (me.proxy = createProxy(assign2({
            type:'memory',
            model:ZBEE_CURRENT_CLASS,
            reader:{
                type:'json',
                isModelData:false
            }
        } , proxy))).addListeners({
            read:'onProxyRead',
            scope:me
        }) ;

        me.data = innerData || {} ;

        if(data){
            
            let {
                proxy
            } = me ;

            if(isMemoryProxy(proxy)){

                proxy.read(data) ;
            }

        }else if(autoLoad){

            me.load() ;
        }
    }

    get idProperty(){

        return 'id' ;
    }

    get bubbleTarget(){

        return this.store ;
    }

    static get fieldConfigurations(){

        return [] ;
    }

    onProxyRead(proxy , records){

        if(records.length){

            let me = this ;

            Object.assign(me.data , records[0].data) ;

            me.fireEvent('load') ;
        }
    }
 }

            var_class_1561952820317 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820317 ;
                }

            } ;
        }
        

                    return var_class_1561952820317;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559101686204" signature="<data.Fields> src::data.model.fields()"  name="src::data.model.fields" imports="src::is.type src::is.string src::is.array src::is.empty src::array.from src::is.object.simple src::is.defined src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store">
        <![CDATA[(() =>{

                let isString,from,isObject,isDefined,getModel,Store;

                let var_init_locked_1561952820337;

                

                

                
/**
 * 
 * 获取数据字段定义集合
 * 
 * @import is.string
 * 
 * @import from from array.from
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.defined
 * 
 * @import getModel from data.model.get
 * 
 * @import Store from data.store value
 * 
 * @return {data.Fields} 字段定义集合 
 * 
 */

const createConvertFn = mapping => data => data[mapping],
    defaultGetFn = data => data,
    defaultEqualsFn = (value , oldValue) => value !== oldValue,
    defaultSetFn = function(value , name){

        this.data[name] = value ;
    },
    {
        assign,
        keys
    } = Object;

 function main(){

    let me = this,
    {
        $fields
    } = me;

    if($fields){

        return $fields ;
    }

    let {
        fieldConfigurations
    } = me,
    fields = [];

    for(let fieldConfig of fieldConfigurations){

        if(isString(fieldConfig)){

            fieldConfig = {
                name:fieldConfig,
                convert:createConvertFn(fieldConfig),
                get:defaultGetFn,
                equals:defaultEqualsFn,
                set:defaultSetFn
            } ;

        }else if(isObject(fieldConfig)){

            let {
                name,
                convert,
                mapping,
                persistent = false,
                get = defaultGetFn,
                equals = defaultEqualsFn,
                set = defaultSetFn,
                defaultValue,
                ...otherFieldConfig
            } = fieldConfig ;

            if(persistent){

                convert = () => defaultValue ;

            }else{

                if(mapping){

                    convert = createConvertFn(mapping) ;
                }

                convert = convert || createConvertFn(name) ;

                if(isDefined(defaultValue)){

                    let oldConvert = convert;

                    convert = (data) => {

                        let value = oldConvert(data) ;
                        
                        return isDefined(value) ? value : defaultValue ;

                    } ;
                }
            }

            fieldConfig = {
                name,
                convert,
                get,
                set,
                equals,
                ...otherFieldConfig
            } ;
        }

        if(isObject(fieldConfig)){

            let {
                hasMany,
                hasOne,
                belongsTo
            } = fieldConfig ;

            let association ;

            if(hasMany){

                association = {
                    type:'hasMany',
                    ...hasMany
                } ;

                delete fieldConfig.hasMany ;
            }

            if(hasOne){

                association = {
                    type:'hasOne',
                    ...hasOne
                } ;
                
                delete fieldConfig.hasOne ;
            }


            if(belongsTo){

                association = {
                    type:'belongsTo',
                    ...belongsTo
                } ;

            }

            if(association){

                assign(fieldConfig , getAssociationConfig(me , association)) ;
            }
        }

        fields.push(fieldConfig) ;
    }

    return me.$fields = new Fields(fields) ;
 }

 function getAssociationConfig(model , {
    model:AssociationModel,
    autoLoad,
    type,
    associationKey,
    assocationMode = 'local-data'
}){

   if(AssociationModel){

       AssociationModel = getModel(associationModel) ;
   
   }else{

       AssociationModel = model ;
   }

   switch(type){

       case 'hasMany':

           switch(assocationMode){

               case 'local-data':

                   return {
                       convert:data => new Store({
                           model:AssociationModel,
                           autoLoad,
                           data,
                           proxy:{
                               reader:{
                                   rootProperty:associationKey
                               }
                           }
                       })
                   } ;

               case 'local-key':

                   return {
                       convert:data => from(createConvertFn(associationKey)(data)),
                       get:keys => this.store.getByIds(keys)
                   } ;

               case 'remote-key':

                   // 等待远程 AJAX 代理实现

               break ;
           }

           break ;

       case 'hasOne':
       case 'belongsTo':

               switch(assocationMode){

                   case 'local-data':
   
                       return {
                           convert:data => new AssociationModel({
                               autoLoad,
                               data,
                               proxy:{
                                   reader:{
                                       rootProperty:associationKey
                                   }
                               }
                           })
                       } ;
   
                   case 'local-key':

                       return {
                           convert:createConvertFn(associationKey),
                           get(key){

                               let {
                                   store
                               } = this ;

                               if(store){

                                   return store.getById(key) ;
                               }

                               
                           }
                       } ;
   
                   case 'remote-key':

                       // 等待远程 AJAX 代理实现
   
                   break ;
               }
   }
}

class Fields{

    constructor(fields){

        let me = this,
            names = [],
            innerFields = [] ;

        for(let field of fields){

            let {
                name
            } = field ;

            if(!names.includes(name)){

                innerFields.push(field) ;

                names.push(name) ;
            
            }else{

                innerFields[names.indexOf(name)] = field ;
            }
        }

        me.fields = innerFields ;

        me.names = names ;
    }

    getField(name){

        let me = this,
            index = getFieldIndex.call(me , name);

        if(index !== -1){

           return me.fields[index] ;
        }
    }

    hasField(name){

        return getFieldIndex.call(this , name) !== -1 ;
    }

    get converts(){

        let {
            fields
        } = this,
        converts = {};
    
        for(let {
            name,
            convert
        } of fields){
    
            converts[name] = convert ;
        }

        return converts ;
    }
 }

 function getFieldIndex(name){

    let {
        names
    } = this ;

    return names.indexOf(name) ;
 }

                return function(){

                    
        if(!var_init_locked_1561952820337){

            isString = include('is.string');
from = include('array.from');
isObject = include('is.object.simple');
isDefined = include('is.defined');
getModel = include('data.model.get');
Store = include('data.store')();

            var_init_locked_1561952820337 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561016218120" signature="<boolean> src::data.model.store.bind.is()"  name="src::data.model.store.bind.is">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 检测当前记录是否绑定存储器
 * 
 * @return {boolean} 如果记录绑定存储器则返回 true , 否则返回 false 
 * 
 */

 return this.hasOwnProperty('store') ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302669" signature="<string> src::id.generate(<string> prefix)" entry-type="string"  name="src::id.generate">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 生成唯一的编号
 * 
 * @param {string} prefix 编号前缀
 * 
 * @return {string} 生成后的唯一编号 
 * 
 */

 let count = 1 ; 

 function main(prefix = 'zb-'){

    return `${prefix}${count ++}` ;

 }

                return function(prefix){

                    

                    

                    return main.call(this , prefix) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559106394371" signature="<mixed> src::data.model.id()"  name="src::data.model.id" imports="src::id.generate">
        <![CDATA[(() =>{

                let getId;

                let var_init_locked_1561952820358;

                

                

                function main(){

        
/**
 * 
 * 返回数据记录编号
 * 
 * @import getId from id.generate
 * 
 * @return {mixed} 编号 
 * 
 */

let me = this,
{
    $id
} = me;

if($id){

    return $id ;
}

let {
    idProperty
} = me ;

if(me.has(idProperty)){

    return me.$id =  me.get(idProperty) ;

}

return me.$id = getId('model-') ;

    }

                return function(){

                    
        if(!var_init_locked_1561952820358){

            getId = include('id.generate');

            var_init_locked_1561952820358 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559093849700" signature="<mixed> src::data.model.store.bind(<data.Store> store)" entry-type="data.Store"  name="src::data.model.store.bind">
        <![CDATA[(() =>{

                

                

                

                

                function main(store){

        
/**
 * 
 * 绑定存储器
 * 
 * @param {data.Store} store 数据存储器
 * 
 * @return {mixed} 返回说明 
 * 
 */

let me = this ;

if(!me.isBindStore){

    me.store = store ;
}

    }

                return function(store){

                    

                    

                    return main.call(this , store) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559093939560" signature="<void> src::data.model.store.unbind()"  name="src::data.model.store.unbind">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 解绑存储器
 * 
 */

 delete this.store ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559107132508" signature="<boolean> src::data.model.has(<string> name)" entry-type="string"  name="src::data.model.has">
        <![CDATA[(() =>{

                

                

                

                

                function main(name){

        
/**
 * 
 * 判断是否拥有此字段
 * 
 * @param {string} name 字段名称
 * 
 * @return {boolean} 如果拥有指定字段则返回 true , 否则返回 false
 * 
 */

let {
    fields
} = this.ZBEE_CURRENT_CLASS ;

return fields.hasField(name) ;

    }

                return function(name){

                    

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559532205399" signature="<void> src::data.model.value.set(<mixed> name , <mixed> value)" entry-type="mixed mixed"  name="src::data.model.value.set" imports="src::is.type src::is.string src::is.object.simple">
        <![CDATA[(() =>{

                let isString,isObject;

                let var_init_locked_1561952820368;

                

                

                function main(name , value){

        
/**
 * 
 * 设置属性
 * 
 * @import is.string
 * 
 * @import isObject from is.object.simple
 * 
 * @param {mixed} name 属性名称
 * 
 * @param {mixed} value 属性值
 * 
 */

let values ;

if(isString(name)){

    values = {
        [name]:value
    } ;

}else{

    values = name ;
}

if(isObject(values)){

    let me = this,
    {
        ZBEE_CURRENT_CLASS
    } = me,
    {
        fields
    } = ZBEE_CURRENT_CLASS,
    names = Object.keys(values),
    updateValues = {};

    for(let name of names){

        let field = fields.getField(name);

        if(field){

            let value = values[name],
                oldValue = me.get(name);

            if(field.equals.call(me , value , oldValue)){

                field.set.call(me , value , name) ;

                updateValues[name] = value ;

                me.fireEvent('update' , name , value , oldValue) ;
            }
        }
    }

    return updateValues ;
}

    }

                return function(name , value){

                    
        if(!var_init_locked_1561952820368){

            isString = include('is.string');
isObject = include('is.object.simple');

            var_init_locked_1561952820368 = true ;
        }
        

                    

                    return main.call(this , name , value) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559107520750" signature="<mixed> src::data.model.value.get(<mixed> name)" entry-type="mixed"  name="src::data.model.value.get">
        <![CDATA[(() =>{

                

                

                

                

                function main(name){

        
/**
 * 
 * 获取属性
 * 
 * @param {mixed} name 属性名称
 * 
 * @return {mixed} 返回说明 
 * 
 */

let me = this,
{
    data,
    ZBEE_CURRENT_CLASS
} = me,
{
    fields
} = ZBEE_CURRENT_CLASS,
field = fields.getField(name);

if(field){

    return field.get.call(me , data[name]) ;
}

    }

                return function(name){

                    

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559108080291" signature="<void> src::data.model.load(<mixed> options)" entry-type="mixed"  name="src::data.model.load">
        <![CDATA[(() =>{

                

                

                

                

                function main(options){

        
/**
 * 
 * 加载数据
 * 
 * @param {mixed} options 加载配置
 * 
 * 
 */

this.proxy.read(options) ;

    }

                return function(options){

                    

                    

                    return main.call(this , options) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556617357251" signature="<mixed> src::data.model.destroy(<mixed> data)" entry-type="mixed"  name="src::data.model.destroy">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 函数实现说明
 * 
 * @param {mixed} data 参数说明
 * 
 * @return {mixed} 返回说明 
 * 
 */

 // 代码实现

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561011653155" signature="<void> src::data.model()"  name="src::data.model" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy">
        <![CDATA[(() =>{

                let mixin_1561952819465__1,extend,static_get_fields,get_isBindStore,get_id,method_bindStore,method_unbindStore,method_has,method_set,method_get,method_load,method_destroy;

                let var_init_locked_1561952820377;

                let var_class_1561952820377;

                

                return function(){

                    
        if(!var_init_locked_1561952820377){

            mixin_1561952819465__1 = include('mixin.observable');
extend = include('src::data.model.base')();
static_get_fields = include('src::data.model.fields');
get_isBindStore = include('src::data.model.store.bind.is');
get_id = include('src::data.model.id');
method_bindStore = include('src::data.model.store.bind');
method_unbindStore = include('src::data.model.store.unbind');
method_has = include('src::data.model.has');
method_set = include('src::data.model.value.set');
method_get = include('src::data.model.value.get');
method_load = include('src::data.model.load');
method_destroy = include('src::data.model.destroy');

            var_init_locked_1561952820377 = true ;
        }
        

                    
        if(!var_class_1561952820377){

            class main extends mixins({extend , mixins:[include('mixin.observable')]}){

            

            static get fields(){

                return static_get_fields.call(this) ;
    
            }

            

            bindStore(...args){

            return method_bindStore.apply(this , args) ;

        }
unbindStore(...args){

            return method_unbindStore.apply(this , args) ;

        }
has(...args){

            return method_has.apply(this , args) ;

        }
set(...args){

            return method_set.apply(this , args) ;

        }
get(...args){

            return method_get.apply(this , args) ;

        }
load(...args){

            return method_load.apply(this , args) ;

        }
destroy(...args){

            return method_destroy.apply(this , args) ;

        }

            get isBindStore(){

                return get_isBindStore.call(this) ;
    
            }
get id(){

                return get_id.call(this) ;
    
            }

        }

            var_class_1561952820377 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820377 ;
                }

            } ;
        }
        

                    return var_class_1561952820377;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348188349" signature="<boolean> src::is.class.from(<mixed> data , <mixed> baseClass)" entry-type="mixed mixed"  name="src::is.class.from" imports="src::is.type src::is.class src::is.string">
        <![CDATA[(() =>{

                let isClass,isString;

                let var_init_locked_1561952820396;

                

                

                function main(data , baseClass){

        
/**
 * 
 * 判定数据是否为指定类
 * 
 * @import is.class
 * 
 * @import is.string
 * 
 * @param {mixed} data 测试数据
 * 
 * @param {mixed} baseClass 基准类
 * 
 * @return {boolean} 如果数据为基准类时则返回 true ,  否则返回 false
 * 
 */

 if(isString(baseClass)){

    baseClass = include(baseClass)() ;
 }

 return isClass(data) && isClass(baseClass) && (data === baseClass || data.prototype instanceof baseClass) ;

    }

                return function(data , baseClass){

                    
        if(!var_init_locked_1561952820396){

            isClass = include('is.class');
isString = include('is.string');

            var_init_locked_1561952820396 = true ;
        }
        

                    

                    return main.call(this , data , baseClass) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348224813" signature="<boolean> src::is.data.model.class(<mixed> data)" entry-type="mixed"  name="src::is.data.model.class" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from">
        <![CDATA[(() =>{

                let dataModel,isClass;

                let var_init_locked_1561952820398;

                

                

                function main(data){

        
/**
 * 
 * 判断是否为数据模型类
 * 
 * 
 * @import data.model
 * 
 * @import isClass from is.class.from
 * 
 * @param {mixed} data 参照数据
 * 
 * @return {boolean} 如果是数据模型类则返回 true , 否则返回 false 
 * 
 */

 return isClass(data , 'data.model') ;



    }

                return function(data){

                    
        if(!var_init_locked_1561952820398){

            dataModel = include('data.model');
isClass = include('is.class.from');

            var_init_locked_1561952820398 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558581661959" signature="<data.Model> src::data.model.get(<mixed> model)" entry-type="mixed"  name="src::data.model.get" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class">
        <![CDATA[(() =>{

                let Model,isModelClass,isString,isFunction;

                let var_init_locked_1561952820415;

                

                

                function main(model){

        /**
* 
* 获取数据模型类引用
* 
* @import Model from data.model value
* 
* @import isModelClass from is.data.model.class
* 
* @import is.string
* 
* @import is.function
* 
* @param {mixed} model 数据模型名称
* 
* @return {data.Model} 模型类引用
* 
*/

if(isString(model)){

   model = include(`data.model.${model}`) ;
}

if(isModelClass(model)){

   return model ;
}

return Model ;

    }

                return function(model){

                    
        if(!var_init_locked_1561952820415){

            Model = include('data.model')();
isModelClass = include('is.data.model.class');
isString = include('is.string');
isFunction = include('is.function');

            var_init_locked_1561952820415 = true ;
        }
        

                    

                    return main.call(this , model) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348501920" signature="<mixed> src::class.define(<mixed> BaseClass)" entry-type="mixed"  name="src::class.define">
        <![CDATA[(() =>{

                

                

                

                

                function main(BaseClass){

        
/**
 * 
 * 类定义
 * 
 * @param {mixed} BaseClass 基准类
 * 
 * @return {mixed} 类
 * 
 */

 return class extends BaseClass{

    get ZBEE_CURRENT_CLASS(){

        return BaseClass ;
    }

 } ;

    }

                return function(BaseClass){

                    

                    

                    return main.call(this , BaseClass) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558348440797" signature="<data.Model> src::data.model.create({<mixed> fields , <string> idProperty})"  name="src::data.model.create" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::class.define">
        <![CDATA[(() =>{

                let Model,get,define;

                let var_init_locked_1561952820433;

                

                

                
/**
 * 
 * 根据字段创建一个数据模型类
 * 
 * @import Model from data.model value
 * 
 * @import get from data.model.get
 * 
 * @import define from class.define
 *
 * @param {object} [config = {}] 配置
 * 
 * @param {mixed} [config.fields = []] 字段定义
 * 
 * @param {string} [config.idProperty = 'id'] 编号字段名称
 * 
 * @return {data.Model} 数据模型类 
 * 
 */

 function main({
     fields,
     idProperty
 }){


    return define(class extends Model{

        static get fieldConfigurations(){

            return fields;
        }

        get idProperty(){

            return idProperty;
        }
    }) ;
 }

                return function({fields = [] , idProperty = 'id'} = {}){

                    
        if(!var_init_locked_1561952820433){

            Model = include('data.model')();
get = include('data.model.get');
define = include('class.define');

            var_init_locked_1561952820433 = true ;
        }
        

                    

                    return main.call(this , {fields , idProperty}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557018819735" signature="<boolean> src::is.data.model(<mixed> data)" entry-type="mixed"  name="src::is.data.model" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model">
        <![CDATA[(() =>{

                let Model;

                let var_init_locked_1561952820451;

                

                

                function main(data){

        
/**
 * 
 * 判定数据是否为数据模型对象
 * 
 * @import Model from data.model value
 * 
 * @param {mixed} data 检测数据
 * 
 * @return {boolean} 如果检测为数据模型则返回 true , 否则返回 false 
 * 
 */

 return data instanceof Model ;

    }

                return function(data){

                    
        if(!var_init_locked_1561952820451){

            Model = include('data.model')();

            var_init_locked_1561952820451 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559021416707" signature="<void> src::data.recordset(<data.Store> store)" entry-type="data.Store"  name="src::data.recordset" imports="src::array.remove.index src::array.remove src::array.insert src::is.type src::is.array src::is.empty src::is.string src::array.from src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.data.model src::array.clone">
        <![CDATA[(() =>{

                let remove,removeIndex,insert,from,isModel,isObject,isDefined,isString,isFunction,clone;

                let var_init_locked_1561952820472;

                let var_class_1561952820472;

                

                return function(store){

                    
        if(!var_init_locked_1561952820472){

            remove = include('array.remove');
removeIndex = include('array.remove.index');
insert = include('array.insert');
from = include('array.from');
isModel = include('is.data.model');
isObject = include('is.object.simple');
isDefined = include('is.defined');
isString = include('is.string');
isFunction = include('is.function');
clone = include('array.clone');

            var_init_locked_1561952820472 = true ;
        }
        

                    
        if(!var_class_1561952820472){

            /**
 * 
 * 数据记录集合
 * 
 * @import remove from array.remove
 * 
 * @import removeIndex from array.remove.index
 * 
 * @import insert from array.insert
 * 
 * @import from from array.from
 * 
 * @import isModel from is.data.model
 * 
 * @import isObject from is.object.simple
 * 
 * @import is.defined
 * 
 * @import is.string
 * 
 * @import is.function
 * 
 * @import clone from array.clone
 * 
 * @param {data.Store} store 存储器
 * 
 */

 class main{

    constructor(store){

        let me = this ;

        me.recordMap = new Map() ;

        me.recordData = [] ;

        me.store = store ;
    }

    /**
     * 
     * 转换成数组输出
     * 
     * @return {array}
     * 
     */
    toArray(){

        return clone(this.recordData) ;
    }

    /**
     * 
     * 根据编号获得数据记录
     * 
     * @param {mixed} id 数据记录编号
     * 
     * @return {data.Model} 
     * 
     */
    getById(id){

        let {
            recordMap
        } = this ;

        return recordMap.get(id) ;
    }

    /**
     * 
     * 根据数据记录获得在存储器中的位置
     * 
     * @param {data.Model} record 数据记录
     * 
     * @return {number} 数据记录位置
     * 
     */
    indexOf(record){

        let {
            recordData
        } = this ;

        return recordData.indexOf(record) ;
    }

    /**
     * 
     * 查寻数据记录
     * 
     * @param {mixed} property 属性名称
     *  
     * @param {mixed} [value] 属性值
     * 
     * @return {array} 数据记录集合
     *  
     */
    findRecords(property , value){

        let {
            recordData:records
        } = this ;

        if(isString(property)){

            let result = [] ;

            for(let record of records){

                if(record.get(property) === value){

                    result.push(record) ;
                }
            }

            return result ;

        }else if(isFunction(property)){

            let result = [] ;

            for(let record of records){

                if(property.call(value , record) === true){

                    result.push(record) ;
                }
            }

            return result ;

        }
    }

    /**
     * 
     * 清理
     * 
     */
    clear(){

        let {
            recordMap,
            recordData
        } = this ;

        recordMap.clear() ;

        recordData.length = 0 ;
    }

    /**
     * 
     * @param {function} fn 循环执行的函数
     *  
     * @param {mixed} scope 
     */
    each(fn , scope){

        let {
            recordData
        } = this ;

        for(let record of recordData){

            fn.call(scope , record) ;
        }
    }

    /**
     * 
     * 添加数据记录
     * 
     * @param {mixed} records 数据记录
     * 
     * 
     */
    add(records){

        let me = this,
        {
            recordData
        } = me ;

        records = getRecords.call(me , records) ;

        recordData.push(...records) ;

        resetRecordData(recordData) ;

        return records ;
    }
     /**
     * 
     * 插入数据记录
     * 
     * @param {mixed} records 数据记录
     * 
     * 
     */
    insert(index , records){

        let me = this,
        {
            recordData
        } = me ;

        records = getRecords.call(me , records) ;

        insert(recordData , index , ...records) ;

        resetRecordData(recordData) ;

        return records ;
    }
     /**
     * 
     * 删除数据记录
     * 
     * @param {mixed} records 数据记录
     * 
     * 
     */
    remove(records){

        records = from(records) ;

        let {
            recordMap,
            recordData
        } = this,
        removeRecords = [];

        for(let record of records){

            let {
                id
            } = record ;

            if(recordMap.has(id)){

                recordMap.delete(id) ;

                remove(recordData , record) ;

                record.unbindStore() ;

                removeRecords.push(record) ;
            }
        }

        return removeRecords ;
    }
 }

 /**
  * 
  * 待处理的数据记录
  * 
  * @param {mixed} records 数据记录
  * 
  */
 function getRecords(records){

    records = from(records) ;

    let {
        recordMap,
        recordData,
        store
    } = this,
    result = [];

    for(let record of records){

        if(isObject(record)){

            record = store.createRecord(record) ;
        }

        if(isModel(record)){

            let {
                id
            } = record ;

            if(recordMap.has(id)){

                record = recordMap.get(id) ;
    
                removeRecord(recordData , record) ;

                record.set(record.data) ;
            
            }else{

                recordMap.set(id , record) ;

                if(record.isBindStore && record.store !== store){

                    continue ;
                }

                record.bindStore(store) ;
            }

            result.push(record) ;
        }
    }

    return result ;
 }

 function removeRecord(recordData , record){

    let index = recordData.indexOf(record) ;

    if(index !== -1){

        recordData[index] = undefined ;
    }
 }

 function resetRecordData(recordData){

    let index ;

    while((index = recordData.indexOf(undefined)) !== -1){

        removeIndex(recordData , index) ;
    }
 }


            var_class_1561952820472 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820472 ;
                }

            } ;
        }
        

                    return new var_class_1561952820472(store);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559024344382" signature="<void> src::data.store.base()"  name="src::data.store.base" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get">
        <![CDATA[(() =>{

                let get,create,assign,createProxy,isMemoryProxy,createReader,createRecordset;

                let var_init_locked_1561952820492;

                let var_class_1561952820492;

                

                return function(){

                    
        if(!var_init_locked_1561952820492){

            get = include('data.model.get');
create = include('data.model.create');
assign = include('object.assign');
createProxy = include('data.proxy.create');
isMemoryProxy = include('is.proxy.memory');
createReader = include('data.reader.json');
createRecordset = include('data.recordset');

            var_init_locked_1561952820492 = true ;
        }
        

                    
        if(!var_class_1561952820492){

            
/**
 * 
 * 数据存储器
 * 
 * @import get from data.model.get
 * 
 * @import create from data.model.create
 * 
 * @import assign from object.assign
 * 
 * @import createProxy from data.proxy.create
 * 
 * @import isMemoryProxy from is.proxy.memory
 * 
 * @import createReader from data.reader.json
 * 
 * @import createRecordset from data.recordset
 * 
 * @class
 * 
 */

class main{

    createRecord(data){

        let records = this.reader(data) ;

        if(records.length){

            return records[0] ;
        }
    }

    onProxyRead(proxy , records){

        let me = this,
        {
            recordset
        } = me;

        recordset.add(records) ;

        me.fireEvent('load' , records) ;
    }
}

 

            var_class_1561952820492 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820492 ;
                }

            } ;
        }
        

                    return var_class_1561952820492;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559026999048" signature="<void> src::object.link(<mixed> dest , <mixed> source , <array> names)" entry-type="mixed mixed array"  name="src::object.link" imports="src::is.type src::is.function">
        <![CDATA[(() =>{

                let isFunction;

                let var_init_locked_1561952820513;

                

                

                function main(dest , source , names){

        /**
 * 
 * 将对象的方法或者属性进行链接
 * 
 * @import is.function
 * 
 * @param {mixed} dest 目标对象
 * 
 * @param {mixed} source 来源对象
 * 
 * @param {array} names 字段名称集合
 * 
 */

 for(let name of names){

    let value = source[name] ;

    if(isFunction(value)){

        dest[name] = (...args) => source[name](...args) ;
    
    }else{

        Object.defineProperty(dest , name , {

            set:value => source[name] = value,

            get:() => source[name]

        }) ;
    }
 }

    }

                return function(dest , source , names){

                    
        if(!var_init_locked_1561952820513){

            isFunction = include('is.function');

            var_init_locked_1561952820513 = true ;
        }
        

                    

                    return main.call(this , dest , source , names) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559026647287" signature="<void> src::data.store.constructor({<object> proxy , <mixed> data , <boolean> autoLoad , <array> fields , <data.Model> model})"  name="src::data.store.constructor" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::class.define src::data.model.create src::object.link">
        <![CDATA[(() =>{

                let create,get,assign,createProxy,isMemoryProxy,createReader,createRecordset,link;

                let var_init_locked_1561952820517;

                

                

                function main({proxy , data , autoLoad , fields , model}){

        
/**
 * 
 * 初始化数据存储器
 * 
 * @import create from data.model.create
 * 
 * @import get from data.model.get
 * 
 * @import assign from object.assign
 * 
 * @import createProxy from data.proxy.create
 * 
 * @import isMemoryProxy from is.proxy.memory
 * 
 * @import createReader from data.reader.json
 * 
 * @import createRecordset from data.recordset
 * 
 * @import link from object.link
 * 
 * @param {object} config 配置
 * 
 * @param {object} [config.proxy = {}] 数据代理
 * 
 * @param {mixed} [config.data] 初始数据
 * 
 * @param {boolean} [config.autoLoad = false] 自动加载
 * 
 * @param {array} [config.fields] 数据字段定义
 * 
 * @param {data.Model} [config.model] 数据模型
 * 
 * 
 */

if(fields){

    model = create({
        fields
    }) ;

}

if(model){

    model = get(model) ;

}

let me = this ;

me.fireEventDataCacheCount = 1 ;

(me.proxy = createProxy(assign({
    type:'memory',
    model,
    reader:{
        type:'json'
    }
} , proxy))).addListeners({
    read:'onProxyRead',
    scope:me
}) ;

let recordset = me.recordset = createRecordset(me) ;

if(data){

    let {
        proxy
    } = me ;

    if(isMemoryProxy(proxy)){

        proxy.read(data)
    }

}else if(autoLoad){

    me.load() ;
}

me.reader = createReader({
    model
}) ;

link(me , recordset , [
    'indexOf',
    'findRecords',
    'getById'
]) ;

    }

                return function({proxy = {} , data , autoLoad = false , fields , model}){

                    
        if(!var_init_locked_1561952820517){

            create = include('data.model.create');
get = include('data.model.get');
assign = include('object.assign');
createProxy = include('data.proxy.create');
isMemoryProxy = include('is.proxy.memory');
createReader = include('data.reader.json');
createRecordset = include('data.recordset');
link = include('object.link');

            var_init_locked_1561952820517 = true ;
        }
        

                    

                    return main.call(this , {proxy , data , autoLoad , fields , model}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559018673717" signature="<void> src::data.store.add(<mixed> records , <boolean> isFireEvent)" entry-type="mixed boolean"  name="src::data.store.add">
        <![CDATA[(() =>{

                

                

                

                

                function main(records , isFireEvent){

        /**
 * 
 * 插入记录
 * 
 * @param {mixed} records 数据记录
 * 
 * @param {boolean} [isFireEvent = true] 是否触发插入事件
 * 
 */

let me = this,
 {
   recordset
 } = me ;

 records = recordset.add(records) ;

 if(records.length){

    if(isFireEvent){

      me.fireEvent('add' , records) ;
    }
 }

 return records ;

    }

                return function(records , isFireEvent = true){

                    

                    

                    return main.call(this , records , isFireEvent) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559013013943" signature="<void> src::data.store.insert(<number> index , <mixed> records , <boolean> isFireEvent)" entry-type="number mixed boolean"  name="src::data.store.insert">
        <![CDATA[(() =>{

                

                

                

                

                function main(index , records , isFireEvent){

        
/**
 * 
 * 插入记录
 * 
 * @param {number} index 插入位置
 * 
 * @param {mixed} records 数据记录
 * 
 * @param {boolean} [isFireEvent = true] 是否触发插入事件
 * 
 */

 let me = this,
 {
   recordset
 } = me ;

 records = recordset.insert(index , records) ;

 if(records.length){

    if(isFireEvent){

      me.fireEvent('insert' , index , records) ;
    }
 }

 return records ;

    }

                return function(index , records , isFireEvent = true){

                    

                    

                    return main.call(this , index , records , isFireEvent) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559013373128" signature="<void> src::data.store.remove(<mixed> records , <boolean> isFireEvent)" entry-type="mixed boolean"  name="src::data.store.remove" imports="src::array.remove.index src::array.remove src::is.type src::is.array src::is.empty src::is.string src::array.from">
        <![CDATA[(() =>{

                let remove,from;

                let var_init_locked_1561952820538;

                

                

                function main(records , isFireEvent){

        
/**
 * 
 * 删除记录
 * 
 * @import remove from array.remove
 * 
 * @import from from array.from
 * 
 * @param {mixed} records 数据记录
 * 
 * @param {boolean} [isFireEvent = true] 是否触发删除事件
 * 
 */

 let me = this,
 {
    recordset
 } = me;

 records = recordset.remove(records) ;

 if(records.length && isFireEvent){

   me.fireEvent('remove' , records) ;
 }

 return records ;






    }

                return function(records , isFireEvent = true){

                    
        if(!var_init_locked_1561952820538){

            remove = include('array.remove');
from = include('array.from');

            var_init_locked_1561952820538 = true ;
        }
        

                    

                    return main.call(this , records , isFireEvent) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559024384827" signature="<void> src::data.store.load(<mixed> options , <boolean> isClear)" entry-type="mixed boolean"  name="src::data.store.load">
        <![CDATA[(() =>{

                

                

                

                

                function main(options , isClear){

        
/**
 * 
 * 加载数据
 * 
 * @param {mixed} options 数据或者配置
 * 
 * @param {boolean} [isClear = true] 是否在加载前清除
 * 
 */

let me = this,
{
    proxy
} = me;

if(isClear){

    me.clear() ;
}

proxy.read(options) ;

    }

                return function(options , isClear = true){

                    

                    

                    return main.call(this , options , isClear) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559024331104" signature="<void> src::data.store.clear()"  name="src::data.store.clear">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 清理
 * 
 * 
 */

let me = this,
    {
        recordset
    } = me ;

recordset.clear() ;

me.fireEvent('clear') ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559027085061" signature="<void> src::data.store()"  name="src::data.store" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear">
        <![CDATA[(() =>{

                let mixin_1561952819455__1,extend,constructor,method_add,method_insert,method_remove,method_load,method_clear;

                let var_init_locked_1561952820546;

                let var_class_1561952820546;

                

                return function(){

                    
        if(!var_init_locked_1561952820546){

            mixin_1561952819455__1 = include('mixin.observable');
extend = include('src::data.store.base')();
constructor = include('src::data.store.constructor');
method_add = include('src::data.store.add');
method_insert = include('src::data.store.insert');
method_remove = include('src::data.store.remove');
method_load = include('src::data.store.load');
method_clear = include('src::data.store.clear');

            var_init_locked_1561952820546 = true ;
        }
        

                    
        if(!var_class_1561952820546){

            class main extends mixins({extend , mixins:[include('mixin.observable')]}){

            

            

            constructor(...args){

            super(...args) ;

            constructor.apply(this , args) ;

        }

            add(...args){

            return method_add.apply(this , args) ;

        }
insert(...args){

            return method_insert.apply(this , args) ;

        }
remove(...args){

            return method_remove.apply(this , args) ;

        }
load(...args){

            return method_load.apply(this , args) ;

        }
clear(...args){

            return method_clear.apply(this , args) ;

        }

            

        }

            var_class_1561952820546 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820546 ;
                }

            } ;
        }
        

                    return var_class_1561952820546;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1558503141801" signature="<mixed> src::data.store.create(<mixed> store)" entry-type="mixed"  name="src::data.store.create" imports="src::is.type src::is.string src::is.object.simple src::is.class src::is.function src::class.create src::is.defined src::class.create.option src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store">
        <![CDATA[(() =>{

                let create,srcDataStore;

                let var_init_locked_1561952820562;

                

                

                function main(store){

        
/**
 * 
 * 创建新的数据存储器
 * 
 * @import create from class.create.option
 * 
 * @import ....store
 * 
 * @param {mixed} [store] 数据代理
 * 
 * @return {mixed} 数据代理 
 * 
 */

return create('data.store' , store) ;



    }

                return function(store){

                    
        if(!var_init_locked_1561952820562){

            create = include('class.create.option');
srcDataStore = include('src::data.store');

            var_init_locked_1561952820562 = true ;
        }
        

                    

                    return main.call(this , store) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559091906575" signature="<object> src::mixin.region.xy.anchor.get(<string> anchor)" entry-type="string"  name="src::mixin.region.xy.anchor.get">
        <![CDATA[(() =>{

                

                

                

                

                function main(anchor){

        
/**
 * 
 * 计算当前区域锚点坐标
 * 
 * @param {string} [anchor = 'tl'] 锚点信息
 * 
 * @return {object} 坐标值 
 * 
 */

let {
    x,
    y,
    width,
    height
} = this ;

switch(anchor){

   case 'c':

        return {
            x:x + width / 2,
            y:y + height / 2
        } ;

   case 'tl':

       return {
           x,
           y
       } ;

   case 'tr':

       return {
           x:x + width,
           y
       } ;

   case 'br':

       return {
           x:x + width,
           y:y + height
       } ;

   case 'bl':

       return {
           x,
           y:y + height
       } ;

   case 'r':

       return {
           x:x + width,
           y:y + height / 2
       } ;

   case 'l':

       return {
           x,
           y:y + height / 2
       } ;

   case 't':

       return {
           x:x + width / 2,
           y 
       } ;

   case 'b':

       return {
           x:x + width / 2,
           y:y + height
       }
}

    }

                return function(anchor = 'tl'){

                    

                    

                    return main.call(this , anchor) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559092063351" signature="<object> src::mixin.region.xy.anchor.set(<object> xy , <string> anchor)" entry-type="object string"  name="src::mixin.region.xy.anchor.set" imports="src::is.defined">
        <![CDATA[(() =>{

                let isDefined;

                let var_init_locked_1561952820584;

                

                

                function main(xy , anchor){

        
/**
 * 
 * 计算当前区域锚点坐标
 * 
 * @import is.defined
 * 
 * @param {object} xy 设置坐标
 * 
 * @param {string} [anchor = 'tl'] 锚点信息
 * 
 * @return {object} 坐标值 
 * 
 */

let {
    x:originX,
    y:originY,
    width,
    height
} = this,
{
    x,
    y
} = xy,
defaultXY = {};

if(!isDefined(x)){

    defaultXY.x = originX ;
}

if(!isDefined(y)){

    defaultXY.y = originY ;
}

const {
    assign
} = Object ;

switch(anchor){

   case 'c':

        return assign({
            x:x - width / 2,
            y:y - height / 2
        } , defaultXY) ;

   case 'tl':

       return assign({
           x,
           y
       } , defaultXY) ;

    case 'tr':

        return assign({
            x:x - width,
            y
        } , defaultXY) ;
    
    case 'br':
    
        return assign({
            x:x - width,
            y:y - height
        } , defaultXY) ;
    
    case 'bl':
    
        return assign({
            x,
            y:y - height
        } , defaultXY) ;

   case 'r':

       return assign({
           x:x - width,
           y:y - height / 2
       } , defaultXY) ;

   case 'l':

       return assign({
           x,
           y:y - height / 2
       } , defaultXY) ;

   case 't':

       return assign({
           x:x - width / 2,
           y 
       } , defaultXY) ;

   case 'b':

       return assign({
           x:x - width / 2,
           y:y - height
       } , defaultXY) ;
}

    }

                return function(xy , anchor = 'tl'){

                    
        if(!var_init_locked_1561952820584){

            isDefined = include('is.defined');

            var_init_locked_1561952820584 = true ;
        }
        

                    

                    return main.call(this , xy , anchor) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559038760300" signature="<void> src::mixin.region(<mixed> extend)"  name="src::mixin.region" imports="src::mixin.region.xy.anchor.get src::is.defined src::mixin.region.xy.anchor.set">
        <![CDATA[(() =>{

                let method_getAnchorXY,method_setAnchorXY;

                let var_init_locked_1561952820586;

                

                

                function main(extend){

                        return class extends extend{

                            
                
                            
                
                            
                
                            getAnchorXY(...args){

            return method_getAnchorXY.apply(this , args) ;

        }
setAnchorXY(...args){

            return method_setAnchorXY.apply(this , args) ;

        }
                
                            
                
                        }

                }

                return function(extend){

                    
        if(!var_init_locked_1561952820586){

            method_getAnchorXY = include('src::mixin.region.xy.anchor.get');
method_setAnchorXY = include('src::mixin.region.xy.anchor.set');

            var_init_locked_1561952820586 = true ;
        }
        

                    

                    return main.call(this , extend) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302686" signature="<boolean> src::is.object.empty(<mixed> data)" entry-type="mixed"  name="src::is.object.empty">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 判断对象是否没有字段
 * 
 * @param {mixed} data 检测数据
 * 
 * @return {boolean} 如果没有字段则返回 true , 否则返回 false
 * 
 */

 return Object.keys(data).length === 0 ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561094972544" signature="<void> src::data.model.node.tree.base(<mixed> data)" entry-type="mixed"  name="src::data.model.node.tree.base" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::mixin.region.xy.anchor.get src::mixin.region.xy.anchor.set src::mixin.region src::is.object.empty">
        <![CDATA[(() =>{

                let Model,insert,remove,region,isEmpty;

                let var_init_locked_1561952820600;

                let var_class_1561952820600;

                

                return function(){

                    
        if(!var_init_locked_1561952820600){

            Model = include('data.model')();
insert = include('array.insert');
remove = include('array.remove');
region = include('mixin.region');
isEmpty = include('is.object.empty');

            var_init_locked_1561952820600 = true ;
        }
        

                    
        if(!var_class_1561952820600){

            
/**
 * 
 * 树型数据模型
 * 
 * @import Model from data.model value
 * 
 * @import insert from array.insert
 * 
 * @import remove from array.remove
 * 
 * @import region from mixin.region
 * 
 * @import isEmpty from is.object.empty
 * 
 * @param {mixed} data 数据
 * 
 * @class
 * 
 */

 class main extends Model{

    syncSize({
        width,
        height
    }){

        return !isEmpty(this.set({
            width,
            height
        })) ;
    }

    static get fieldConfigurations(){

        return [
            'id',
            'parentId',
            {
                name:'expanded',
                persistent:true,
                defaultValue:false
            },{
                name:'width',
                persistent:true,
                defaultValue:0
            },{
                name:'height',
                persistent:true,
                defaultValue:0
            },{
                name:'x',
                persistent:true,
                defaultValue:0
            },{
                name:'y',
                persistent:true,
                defaultValue:0
            },{
                name:'selected',
                persistent:true,
                defaultValue:false
            },{
                name:'hidden',
                persistent:true,
                defaultValue:true
            },
            {
                name:'leaf',
                defaultValue:false
            }
        ];
    }

    get hidden(){

        return this.get('hidden') ;
    }

    /**
     * 
     * 获得横坐标
     * 
     * @return {number}
     * 
     */
    get x(){

        return this.get('x') ;
    }

    /**
     * 
     * 获得纵坐标
     * 
     * @return {number}
     * 
     */
    get y(){

        return this.get('y');
    }

    get width(){

       return this.get('width') ;
    }

    /**
     * 
     * 获得高度
     * 
     * @return {number}
     * 
     */
    get height(){

        return this.get('height') ;
    }

    /**
     * 
     * 判断当前节点是否为根节点
     * 
     * @return {boolean} 如果为根节点则返回 true , 否则返回 false
     * 
     */
    get isRoot(){

        return !this.parentNode ;
    }

    /**
     * 
     * 判断当前节点是否为叶子节点
     * 
     * @return {boolean} 如果为叶子节点则返回 true , 否则返回 false
     * 
     */
    get isLeaf(){

        return this.get('leaf') ;
    }

    /**
     * 
     * 判断当前节点是否加载
     * 
     * @return {boolean}
     * 
     */
    get synchronized(){

        let {
            isLeaf,
            children
        } = this ;

        return !isLeaf && children.length !== 0 ;
    }

    syncChildNodes(nodes){

        let me = this ;

        if(!me.synchronized){

            if(nodes.length === 0){

                me.set('leaf' , true) ;
            
            }else{

                me.suspendEvents() ;
    
                nodes = me.appendChild(nodes) ;

                me.resumeEvents() ;
            }

            me.fireEvent('syncchildnodes' , nodes) ;
        }
    }

    /**
     * 
     * 返回上一个兄弟节点
     * 
     * @return {data.model.node.Tree} 父节点
     * 
     */
    get previousSiblingNode(){

        return getSiblingNode.call(this , 'previous') ;
    }
    /**
     * 
     * 返回下一个兄弟节点
     * 
     * @return {data.model.node.Tree} 父节点
     * 
     */
    get nextSiblingNode(){

        return getSiblingNode.call(this , 'next') ;
    }

    /**
     * 
     * 表达当前节点是否展开
     * 
     * @return {boolean} 节点展开标志
     * 
     */
    get expanded(){

        return this.get('expanded') ;
    }
    /**
     * 
     * 返回父节点
     * 
     * @return {data.model.node.Tree} 父节点
     * 
     */
    get parentNode(){

        let me = this,
        {
            store
        } = me ;

        return store.getById(me.get('parentId')) ;
    }
    /**
     * 
     * 返回子节点集合
     * 
     * @return {data.model.node.Tree[]} 子节点集合
     * 
     */
    get childNodes(){

       let me = this,
       {
           expanded
       } = me ;

       if(!expanded){

            return [] ;
       }

       return me.children ;
    }

    /**
     * 
     * 返回第一个子节点
     * 
     * @return {data.model.node.Tree} 节点
     * 
     */
    get firstChildNode(){

       return getChildNode.call(this , 'first') ;
    }

    get firstDescendantNodes(){

        return getDescendantNodes.call(this , 'firstChildNode') ;
    }

    /**
     * 
     * 返回最后一个子节点
     * 
     * @return {data.model.node.Tree} 节点
     * 
     */
    get lastChildNode(){

        return getChildNode.call(this , 'last') ;
    }

    get lastDescendantNodes(){

        return getDescendantNodes.call(this , 'lastChildNode') ;
    }


    get selected(){

        return this.get('selected') ;
    }

    /**
     * 
     * 删除
     * 
     * @param {data.model.node.Tree} node 节点
     * 
     */
    removeChild(node){

        let {
             store,
             childNodes
        } = this ;

        store.removeNodes(node) ;
        
        remove(childNodes , node) ;
    }

    /**
     * 
     * 显示
     * 
     */
    show(){

        doHidden.call(this , false) ;
        
    }

    /**
     * 
     * 隐藏
     * 
     */
    hide(){

        doHidden.call(this , true) ;
    }
 }

 function doHidden(value){

    let me = this,
        {
            childNodes,
            store
        } = me,
        {
            selectedNode
        } = store;

    if(selectedNode === me){

        let {
            parentNode
        } = me ;

        while(parentNode){

            if(parentNode.hidden === false){

                parentNode.select() ;

                break ;
            
            }else{

                parentNode = parentNode.parentNode ;
            }
        }
    }

    if(value){

        me.set({
            hidden:true,
            x:0,
            y:0
        }) ;

    }else{

        me.set('hidden' , value) ;
    }

    for(let childNode of childNodes){

        childNode[value ? 'hide' : 'show']() ;
    }
 }

 function getSiblingNode(property){

    let me = this,
        {
            parentNode
        } = me ;

    if(parentNode){

        let {
            childNodes
        } = parentNode,
        index = childNodes.indexOf(me);

        switch(property){

            case 'next':

                index ++ ;

                break ;

            case 'previous':

                index -- ;
        }

        return childNodes[index] ;
    }
 }

 function getDescendantNodes(property){

    let nodes = [],
        node = this;

    while(true){

        let childNode = node[property] ;

        if(childNode){

            nodes.push(childNode) ;

            node = childNode ;
        
        }else{

            break ;
        }

    }

    return nodes ;
 }

 function getChildNode(property){

    let {
        children
    } = this ;

    if(children.length){

        switch(property){

            case 'first':

                return children[0] ;

            case 'last':

                return children[children.length - 1] ;
        }
    }
 }

            var_class_1561952820600 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820600 ;
                }

            } ;
        }
        

                    return var_class_1561952820600;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559127432406" signature="<void> src::math.region({<number> x , <number> y , <number> width , <number> height})"  name="src::math.region" imports="src::mixin.region.xy.anchor.get src::is.defined src::mixin.region.xy.anchor.set src::mixin.region">
        <![CDATA[(() =>{

                let region;

                let var_init_locked_1561952820622;

                let var_class_1561952820622;

                

                return function({x , y , width , height}){

                    
        if(!var_init_locked_1561952820622){

            region = include('mixin.region');

            var_init_locked_1561952820622 = true ;
        }
        

                    
        if(!var_class_1561952820622){

            
/**
 * 
 * 区域
 * 
 * @import region from mixin.region
 * 
 * @param {object} config 配置
 * 
 * @param {number} [config.x] 横坐标
 * 
 * @param {number} [config.y] 纵坐标
 * 
 * @param {number} [config.width] 宽度
 * 
 * @param {number} [config.height] 高度
 * 
 */

 class main extends mixins({
    mixins:[
      region
    ]
 }){

    constructor({
       x,
       y,
       width,
       height
    }){

      super() ;
       
      let me = this ;

      me.x = x ;

      me.y = y ;

      me.width = width ;

      me.height = height ;

    }

    get right(){

       return this.getAnchorXY('r').x ;
    }

    get bottom(){

      return this.getAnchorXY('b').y ;
    }

    contains(region){

      let me = this;
 
      return (region.x >= me.x && (region.right || region.x) <= me.right && region.y >= me.y && (region.bottom || region.y) <= me.bottom);
   }

   containTo(region){

      let me = this ;

      if(me.contains(region)){

         return {
            x:0,
            y:0
         };
      }

      let {
         x:currentX,
         y:currentY,
         right:currentRight,
         bottom:currentBottom
      } = me,
      offsetX = 0,
      offsetY = 0;

      let {
         x:regionX,
         y:regionY,
         right:regionRight,
         bottom:regionBottom
      } = region;

      if(regionX < currentX){

         offsetX = regionX - currentX ;
      }

      if(regionRight > currentRight){

         offsetX = regionRight - currentRight ;
      }

      if(regionY < currentY){

         offsetY = regionY - currentY ;
      }

      if(regionBottom > currentBottom){

         offsetY = regionBottom - currentBottom ;
      }

      return {
         x:offsetX,
         y:offsetY
      } ;
   }
 }

 

            var_class_1561952820622 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820622 ;
                }

            } ;
        }
        

                    return new var_class_1561952820622({x , y , width , height});
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559126527953" signature="<math.Region> src::data.model.node.tree.region()"  name="src::data.model.node.tree.region" imports="src::mixin.region.xy.anchor.get src::is.defined src::mixin.region.xy.anchor.set src::mixin.region src::math.region">
        <![CDATA[(() =>{

                let createRegion;

                let var_init_locked_1561952820625;

                

                

                function main(){

        
/**
 * 
 * 获得当前节点区域对象
 * 
 * @import createRegion from math.region
 * 
 * @return {math.Region} 节点区域 
 * 
 */

 let {
    x,
    y,
    width,
    height
 } = this ;

 return createRegion({
     x,
     y,
     width,
     height
 }) ;

    }

                return function(){

                    
        if(!var_init_locked_1561952820625){

            createRegion = include('math.region');

            var_init_locked_1561952820625 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559179619558" signature="<number> src::data.model.node.tree.depth()"  name="src::data.model.node.tree.depth">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 当前节点深度
 * 
 * @return {number} 深度
 * 
 */

let node = this,
parentNode,
depth = 0;

while(parentNode = node.parentNode){

    node = parentNode ;

    depth ++ ;
}

return depth ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559181780942" signature="<number> src::data.model.node.tree.height.scope.child()"  name="src::data.model.node.tree.height.scope.child">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 所有子节点的总范围高度
 * 
 * @return {number} 总高度 
 * 
 */

let countHeight = 0,
    {
        childNodes,
        store
    } = this ;

if(childNodes.length){

    for(let {
        scopeHeight
    } of childNodes){

        countHeight += scopeHeight ;
    }

    countHeight += store.marginBottom * (childNodes.length - 1);

    return countHeight ;
}

return 0 ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559180564430" signature="<number> src::data.model.node.tree.height.scope()"  name="src::data.model.node.tree.height.scope">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 获取当前节点的范围高度
 * 
 * @return {number} 高度 
 * 
 */

let me = this,
{
    height,
    childCountScopeHeight,
} = me ;

return Math.max(height , childCountScopeHeight) ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559182031634" signature="<array> src::data.model.node.tree.nodes.child()"  name="src::data.model.node.tree.nodes.child">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 获取真实的子节点集合
 * 
 * @return {array} 子节点集合 
 * 
 */

let {
    store,
    id
} = this ;

return store.findRecords('parentId' , id) ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559182401545" signature="<array> src::data.model.node.tree.nodes.descendant()"  name="src::data.model.node.tree.nodes.descendant" imports="src::data.model.node.tree.nodes.child">
        <![CDATA[(() =>{

                let getChildNodes;

                let var_init_locked_1561952820640;

                let var_current_scope_1561952820640;

                

                function main(){

        
/**
 * 
 * 获得所有子孙节点
 * 
 * @import getChildNodes from ..child scoped
 * 
 * @return {array} 节点集合 
 * 
 */

let me = this,
    childNodes = getChildNodes(),
    result = [];

for(let childNode of childNodes){

    result.push(childNode) ;

    result.push(...childNode.descendantNodes) ;

}

return result ;

    }

                return function(){

                    

                    
        if(!var_current_scope_1561952820640 !== this){

            getChildNodes = include('src::data.model.node.tree.nodes.child').bind(this);

            var_current_scope_1561952820640 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561109859422" signature="<data.model.node.Tree> src::data.model.node.tree.node.leaf.last()"  name="src::data.model.node.tree.node.leaf.last">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 最后一个叶子节点
 * 
 * @return {data.model.node.Tree} 树型节点 
 * 
 */

let me = this,
{
    isLeaf
} = me ;

if(isLeaf){

    return me ;
}

let node = me.lastChildNode;

while(node && !node.isLeaf && node.synchronized){

    node = node.lastChildNode ;
}

return node ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559278888162" signature="<array> src::data.model.node.tree.nodes.leaf()"  name="src::data.model.node.tree.nodes.leaf">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 当前节点下的所有叶子节点
 * 
 * @return {array} 节点集合 
 * 
 */

 let me = this,
 {
    childNodes
 } = me;

 if(childNodes.length){

    let leafNodes = [] ;

    for(let childNode of childNodes){

        leafNodes.push(...childNode.leafNodes) ;
    }

    return leafNodes ;

 }

 return [
    me
] ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559185942444" signature="<void> src::data.model.node.tree.layout()"  name="src::data.model.node.tree.layout">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 布局
 * 
 * 
 */

let me = this,
    {
        childNodes
    } = me;

if(childNodes.length === 0){

    return ;
}

let {
    childCountScopeHeight,
    store
} = me,
{
    marginBottom,
    marginRight
} = store,
{
    y:centerY
} = me.getAnchorXY('c'),
{
    x
} = me.getAnchorXY('r');

let startY = centerY - childCountScopeHeight / 2 ;

x += marginRight;

for(let childNode of childNodes){

    let {
        scopeHeight
    } = childNode ;

    childNode.set('x' , x) ;

    childNode.set(childNode.setAnchorXY({
        y:startY + scopeHeight / 2
    } , 'c')) ;

    startY += scopeHeight + marginBottom;

    childNode.layout() ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561095058258" signature="async <void> src::data.model.node.tree.expand()"  name="src::data.model.node.tree.expand">
        <![CDATA[(() =>{

                

                

                

                

                async function main(){

        
/**
 * 
 * 展开节点 
 * 
 */

let me = this,
{
    expanded
} = me ;

if(!expanded){

    let {
        store,
        synchronized
    } = this ;

    if(!synchronized){

        me.syncChildNodes(await store.syncChildNodes(me)) ;
    }

    me.set('expanded' , true) ;

    let {
        childNodes
    } = me ;

    for(let childNode of childNodes){

        childNode.show() ;
    }

    me.fireEvent('expand' , synchronized) ;
}

    }

                return async function(){

                    

                    

                    return await main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561093771380" signature="<void> src::data.model.node.tree.collapse(<boolean> deep)" entry-type="boolean"  name="src::data.model.node.tree.collapse">
        <![CDATA[(() =>{

                

                

                

                

                function main(deep){

        
/**
 * 
 * 收起
 * 
 * @param {boolean} [deep = false] 是否级联收起节点，默认为 false
 * 
 */

let me = this,
{
    expanded,
    childNodes
} = me ;

if(expanded){

    for(let childNode of childNodes){

        if(deep){

            childNode.suspendEvents() ;

            childNode.collapse(deep) ;

            childNode.resumeEvents() ;
        }

        childNode.hide() ;
    }

    me.set('expanded' , false) ;

    me.fireEvent('collapse') ;
}

    }

                return function(deep = false){

                    

                    

                    return main.call(this , deep) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559199240426" signature="<void> src::data.model.node.tree.select()"  name="src::data.model.node.tree.select">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 选定
 * 
 * 
 */

let me = this,
    {
        selected,
        store,
        hidden
    } = me;

if(!selected && !hidden){

    let {
        selectedNode
    } = store ;

    if(selectedNode){

        selectedNode.deselect() ;
    }

    me.set('selected' , true) ;

    store.selectedNode = me ;

    me.fireEvent('select') ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559199257569" signature="<void> src::data.model.node.tree.deselect()"  name="src::data.model.node.tree.deselect">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 取消选定
 * 
 * 
 */

let me = this,
    {
        selected,
        hidden,
        store
    } = me;

if(selected && !hidden){

    me.set('selected' , false) ;

    me.fireEvent('deselect') ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559127572894" signature="<void> src::data.model.node.tree.select.vertical(<string> direction)" entry-type="string"  name="src::data.model.node.tree.select.vertical">
        <![CDATA[(() =>{

                

                

                

                

                function main(direction){

        
/**
 * 
 * 垂直方向选择
 * 
 * @param {string} direction 方向 
 * 
 */

 let methodName,
     increment;

 switch(direction){

    case 'up':

        methodName = 'previousSiblingNode' ;

        increment = -1 ;

        break ;

    case 'down':

        methodName = 'nextSiblingNode' ;

        increment = 1 ;
 }

 let me = this,
    node = me[methodName] ;

if(node){

    node.select() ;

    return node ;

}else{

    let {
        depth,
        store
    } = me,
    nodes = store.rootNode.getDepthNodes(depth),
    node = nodes[nodes.indexOf(me) + increment] ;

    if(node){

        node.select() ;

        return node ;
    }

}


    }

                return function(direction){

                    

                    

                    return main.call(this , direction) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559179464281" signature="<void> src::data.model.node.tree.select.up()"  name="src::data.model.node.tree.select.up" imports="src::data.model.node.tree.select.vertical">
        <![CDATA[(() =>{

                let select;

                let var_init_locked_1561952820667;

                let var_current_scope_1561952820667;

                

                function main(){

        /**
 * 
 * 向上选择一个节点
 * 
 * @import select from ..vertical scoped
 * 
 */

return select('up') ;

    }

                return function(){

                    

                    
        if(!var_current_scope_1561952820667 !== this){

            select = include('src::data.model.node.tree.select.vertical').bind(this);

            var_current_scope_1561952820667 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559179457518" signature="<void> src::data.model.node.tree.select.down()"  name="src::data.model.node.tree.select.down" imports="src::data.model.node.tree.select.vertical">
        <![CDATA[(() =>{

                let select;

                let var_init_locked_1561952820669;

                let var_current_scope_1561952820669;

                

                function main(){

        /**
 * 
 * 向下选择一个节点
 * 
 * @import select from ..vertical scoped
 * 
 */

 return select('down') ;

 

    }

                return function(){

                    

                    
        if(!var_current_scope_1561952820669 !== this){

            select = include('src::data.model.node.tree.select.vertical').bind(this);

            var_current_scope_1561952820669 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559179282854" signature="<void> src::data.model.node.tree.select.left()"  name="src::data.model.node.tree.select.left">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 向左选择一个节点
 * 
 */

let {
    parentNode
} = this ;

if(parentNode){

    parentNode.select() ;

    return parentNode ;
}

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302762" signature="<number> src::math.point.distance(<object> point1 , <object> point2)" entry-type="object object"  name="src::math.point.distance">
        <![CDATA[(() =>{

                

                

                

                

                function main(point1 , point2){

        /**
 * 
 * 计算两点之间的距离
 * 
 * @param {object} point1 第一个点的位置
 * 
 * @param {object} point2 第二个点的位置
 * 
 * @return {number} 两点之间的距离
 * 
 */

const {
    pow,
    sqrt
} = Math ;

return sqrt(pow(point1.x - point2.x , 2) + pow(point1.y - point2.y , 2) , 2);

    }

                return function(point1 , point2){

                    

                    

                    return main.call(this , point1 , point2) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559179288579" signature="<void> src::data.model.node.tree.select.right()"  name="src::data.model.node.tree.select.right" imports="src::math.point.distance">
        <![CDATA[(() =>{

                let getDistance;

                let var_init_locked_1561952820681;

                

                

                function main(){

        
/**
 * 
 * 向右移动一格
 * 
 * @import getDistance from math.point.distance
 * 
 */

let me = this,
{
    firstChildNode
} = me ;

if(firstChildNode){

    firstChildNode.select() ;

    return firstChildNode ;

}else{

    let {
        depth,
        store
    } = me,
    nodes = store.rootNode.getDepthNodes(depth + 1),
    minDistance,
    nearestNode;

    for(let node of nodes){

        let distance = getDistance(me , node) ;

        if(!minDistance){

            minDistance = distance ;

            nearestNode = node ;

            continue ;
        }

        if(minDistance > distance){

            minDistance = distance ;

            nearestNode = node ;
        }
    }

    if(nearestNode){

        nearestNode.select() ;

        return nearestNode ;
    }
}

    }

                return function(){

                    
        if(!var_init_locked_1561952820681){

            getDistance = include('math.point.distance');

            var_init_locked_1561952820681 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561021459735" signature="<array> src::data.model.node.tree.nodes.depth(<number> depth)" entry-type="number"  name="src::data.model.node.tree.nodes.depth">
        <![CDATA[(() =>{

                

                

                

                

                function main(depth){

        
/**
 * 
 * 获得指定深度的节点集合
 * 
 * @param {number} depth 深度
 * 
 * @return {array} 节点集合 
 * 
 */

let nodes = [],
{
    children
} = this;

if(depth === 1){

    return children ;
}

depth -- ;

for(let childNode of children){

    nodes.push(childNode , ...childNode.getDepthNodes(depth)) ;
}

return nodes ;

    }

                return function(depth){

                    

                    

                    return main.call(this , depth) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561111215025" signature="<void> src::data.model.node.tree.append(<mixed> node)" entry-type="mixed"  name="src::data.model.node.tree.append">
        <![CDATA[(() =>{

                

                

                

                

                function main(node){

        
/**
 * 
 * 添加节点
 * 
 * @param {mixed} node 节点信息
 * 
 */

let me = this,
{
    store,
    expanded,
    hidden
} = me,
nodes = store.insert(store.indexOf(me.lastLeafNode || me) + 1 , node) ;

me.set('leaf' , false) ;

if(expanded && !hidden){

    for(let node of nodes){

        node.show() ;
    }
}

me.fireEvent('append' , nodes) ;

return nodes ;


    }

                return function(node){

                    

                    

                    return main.call(this , node) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561111264946" signature="<void> src::data.model.node.tree.insert.after(<mixed> node , <data.model.tree.Node> existNode)" entry-type="mixed data.model.tree.Node"  name="src::data.model.node.tree.insert.after">
        <![CDATA[(() =>{

                

                

                

                

                function main(node , existNode){

        
/**
 * 
 * 在指定节点之前插入
 * 
 * @param {mixed} node 插入节点
 * 
 * @param {data.model.tree.Node} existNode 节点
 * 
 */

 let me = this,
 {
    store,
    children,
    expanded,
    hidden
  } = me;

  if(children.includes(existNode)){

    let nodes = store.insert(store.indexOf(existNode) + 1 , node) ;

    me.set('leaf' , false) ;

    if(expanded && !hidden){

        for(let node of nodes){
    
            node.show() ;
        }
    }

    me.fireEvent('insertafter' , nodes , existNode) ;
  }

    }

                return function(node , existNode){

                    

                    

                    return main.call(this , node , existNode) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561111396682" signature="<void> src::data.model.node.tree.remove(<data.model.node.Tree> node)" entry-type="data.model.node.Tree"  name="src::data.model.node.tree.remove" imports="src::array.remove.index src::array.remove">
        <![CDATA[(() =>{

                let remove;

                let var_init_locked_1561952820702;

                

                

                function main(node){

        
/**
 * 
 * 删除子节点
 * 
 * @import remove from array.remove
 * 
 * @param {data.model.node.Tree} node 树型节点
 * 
 */

let me = this,
{
    store,
    children
} = me ;

if(children.includes(node)){

    store.remove(node) ;

    remove(children , node) ;

    if(children.length === 0){

        me.set('leaf' , true) ;
    }
}

    }

                return function(node){

                    
        if(!var_init_locked_1561952820702){

            remove = include('array.remove');

            var_init_locked_1561952820702 = true ;
        }
        

                    

                    return main.call(this , node) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559277673399" signature="<void> src::data.model.node.tree()"  name="src::data.model.node.tree" imports="src::mixin.region.xy.anchor.get src::is.defined src::mixin.region.xy.anchor.set src::mixin.region src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::is.class.from src::is.data.model.class src::data.model.get src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.object.empty src::data.model.node.tree.base src::math.region src::data.model.node.tree.region src::data.model.node.tree.depth src::data.model.node.tree.height.scope.child src::data.model.node.tree.height.scope src::data.model.node.tree.nodes.child src::data.model.node.tree.nodes.descendant src::data.model.node.tree.node.leaf.last src::data.model.node.tree.nodes.leaf src::data.model.node.tree.layout src::data.model.node.tree.expand src::data.model.node.tree.collapse src::data.model.node.tree.select src::data.model.node.tree.deselect src::data.model.node.tree.select.vertical src::data.model.node.tree.select.up src::data.model.node.tree.select.down src::data.model.node.tree.select.left src::math.point.distance src::data.model.node.tree.select.right src::data.model.node.tree.nodes.depth src::data.model.node.tree.append src::data.model.node.tree.insert.after src::data.model.node.tree.remove">
        <![CDATA[(() =>{

                let mixin_1561952819519__1,extend,get_region,get_depth,get_childCountScopeHeight,get_scopeHeight,get_descendantNodes,get_children,get_lastLeafNode,get_leafNodes,method_layout,method_expand,method_collapse,method_select,method_deselect,method_selectUp,method_selectDown,method_selectLeft,method_selectRight,method_getDepthNodes,method_appendChild,method_insertAfter,method_removeChild;

                let var_init_locked_1561952820707;

                let var_class_1561952820707;

                

                return function(){

                    
        if(!var_init_locked_1561952820707){

            mixin_1561952819519__1 = include('mixin.region');
extend = include('src::data.model.node.tree.base')();
get_region = include('src::data.model.node.tree.region');
get_depth = include('src::data.model.node.tree.depth');
get_childCountScopeHeight = include('src::data.model.node.tree.height.scope.child');
get_scopeHeight = include('src::data.model.node.tree.height.scope');
get_descendantNodes = include('src::data.model.node.tree.nodes.descendant');
get_children = include('src::data.model.node.tree.nodes.child');
get_lastLeafNode = include('src::data.model.node.tree.node.leaf.last');
get_leafNodes = include('src::data.model.node.tree.nodes.leaf');
method_layout = include('src::data.model.node.tree.layout');
method_expand = include('src::data.model.node.tree.expand');
method_collapse = include('src::data.model.node.tree.collapse');
method_select = include('src::data.model.node.tree.select');
method_deselect = include('src::data.model.node.tree.deselect');
method_selectUp = include('src::data.model.node.tree.select.up');
method_selectDown = include('src::data.model.node.tree.select.down');
method_selectLeft = include('src::data.model.node.tree.select.left');
method_selectRight = include('src::data.model.node.tree.select.right');
method_getDepthNodes = include('src::data.model.node.tree.nodes.depth');
method_appendChild = include('src::data.model.node.tree.append');
method_insertAfter = include('src::data.model.node.tree.insert.after');
method_removeChild = include('src::data.model.node.tree.remove');

            var_init_locked_1561952820707 = true ;
        }
        

                    
        if(!var_class_1561952820707){

            class main extends mixins({extend , mixins:[include('mixin.region')]}){

            

            

            

            layout(...args){

            return method_layout.apply(this , args) ;

        }
expand(...args){

            return method_expand.apply(this , args) ;

        }
collapse(...args){

            return method_collapse.apply(this , args) ;

        }
select(...args){

            return method_select.apply(this , args) ;

        }
deselect(...args){

            return method_deselect.apply(this , args) ;

        }
selectUp(...args){

            return method_selectUp.apply(this , args) ;

        }
selectDown(...args){

            return method_selectDown.apply(this , args) ;

        }
selectLeft(...args){

            return method_selectLeft.apply(this , args) ;

        }
selectRight(...args){

            return method_selectRight.apply(this , args) ;

        }
getDepthNodes(...args){

            return method_getDepthNodes.apply(this , args) ;

        }
appendChild(...args){

            return method_appendChild.apply(this , args) ;

        }
insertAfter(...args){

            return method_insertAfter.apply(this , args) ;

        }
removeChild(...args){

            return method_removeChild.apply(this , args) ;

        }

            get region(){

                return get_region.call(this) ;
    
            }
get depth(){

                return get_depth.call(this) ;
    
            }
get childCountScopeHeight(){

                return get_childCountScopeHeight.call(this) ;
    
            }
get scopeHeight(){

                return get_scopeHeight.call(this) ;
    
            }
get descendantNodes(){

                return get_descendantNodes.call(this) ;
    
            }
get children(){

                return get_children.call(this) ;
    
            }
get lastLeafNode(){

                return get_lastLeafNode.call(this) ;
    
            }
get leafNodes(){

                return get_leafNodes.call(this) ;
    
            }

        }

            var_class_1561952820707 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820707 ;
                }

            } ;
        }
        

                    return var_class_1561952820707;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561094536604" signature="<void> src::data.store.tree.base()"  name="src::data.store.tree.base" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::mixin.region.xy.anchor.get src::mixin.region.xy.anchor.set src::mixin.region src::is.object.empty src::data.model.node.tree.base src::math.region src::data.model.node.tree.region src::data.model.node.tree.depth src::data.model.node.tree.height.scope.child src::data.model.node.tree.height.scope src::data.model.node.tree.nodes.child src::data.model.node.tree.nodes.descendant src::data.model.node.tree.node.leaf.last src::data.model.node.tree.nodes.leaf src::data.model.node.tree.layout src::data.model.node.tree.expand src::data.model.node.tree.collapse src::data.model.node.tree.select src::data.model.node.tree.deselect src::data.model.node.tree.select.vertical src::data.model.node.tree.select.up src::data.model.node.tree.select.down src::data.model.node.tree.select.left src::math.point.distance src::data.model.node.tree.select.right src::data.model.node.tree.nodes.depth src::data.model.node.tree.append src::data.model.node.tree.insert.after src::data.model.node.tree.remove src::data.model.node.tree">
        <![CDATA[(() =>{

                let Store,model,define,from;

                let var_init_locked_1561952820732;

                let var_class_1561952820732;

                

                return function(){

                    
        if(!var_init_locked_1561952820732){

            Store = include('data.store')();
model = include('data.model.node.tree')();
define = include('class.define');
from = include('array.from');

            var_init_locked_1561952820732 = true ;
        }
        

                    
        if(!var_class_1561952820732){

            
/**
 * 
 * 树型数据存储器
 * 
 * @import Store from data.store value
 * 
 * @import model from data.model.node.tree value
 * 
 * @import define from class.define
 * 
 * @import from from array.from
 * 
 * @class
 * 
 * 
 */

 class main extends Store{

    constructor({
        fields,
        margin = {},
        padding = 0,
        lineOffsetX = 0, 
        rootConfig,
        syncChildNodes,
        ...options
    }){

        if(fields){

            model = define(class extends model{

                static get fieldConfigurations(){

                    return [
                        ...super.fieldConfigurations,
                        ...fields
                    ];
                }
            }) ;

        }

        super({
            ...options,
            model
        }) ;

        let me = this ;

        me.rootConfig = rootConfig || {} ;

        me.syncChildNodes = syncChildNodes || (() => []) ;

        let {
            bottom:marginBottom = 0,
            right:marginRight = 0
        } = margin ;

        me.marginBottom = marginBottom ;

        me.marginRight = marginRight ;

        me.padding = padding ;

        me.lineOffsetX = lineOffsetX ;

        me.addListeners({
            load:{
                fn:'onLoad',
                once:true,
                getOldFireEventData:'last'
            },
            expand:'onExpand',
            collapse:'onCollapse',
            scope:me
        }) ;
    }

    onExpand(){

        this.layout() ;

    }

    onCollapse(){

        this.layout() ;
    }

    onLoad(store , nodes){

        for(let node of nodes){

            if(!node.isBindStore){

                continue ;
            }

            let {
                parentNode
            } = node ;

            if(!parentNode){
                
                let me = this,
                {
                    rootConfig
                } = me ;

                me.rootNode = node ;

                node.set(rootConfig) ;

                doReorder(node) ;

                node.suspendEvents() ;

                node.show() ;

                node.select() ;

                node.expand() ;

                node.resumeEvents() ;

                break ;
            }
        }
    }
 }


 function doReorder(node){

    let {
        store,
        childNodes
    } = node ;

    store.insert(store.indexOf(node) + 1 , childNodes) ;

 }

            var_class_1561952820732 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820732 ;
                }

            } ;
        }
        

                    return var_class_1561952820732;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559281372822" signature="<mixed> src::data.store.tree.region()"  name="src::data.store.tree.region">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 返回当前脑图区域信息
 * 
 * @return {mixed} 返回说明 
 * 
 */

let {
    rootNode,
    padding
} = this,
{
    x,
    y,
    firstDescendantNodes,
    lastDescendantNodes,
    leafNodes
} = rootNode;

if(x > 0){

    x = 0 ;
}

for(let node of firstDescendantNodes){

    let {
        x:nodeX,
        y:nodeY
    } = node.getAnchorXY('tr') ;

    if(y > nodeY){

        y = nodeY ;
    }
}

if(y > 0){

    y = 0 ;
}

let bottom = y ;

for(let node of lastDescendantNodes){

    let {
        y:nodeY
    } = node.getAnchorXY('br') ;

    if(bottom < nodeY){

        bottom = nodeY ;
    }
}

let right = rootNode.getAnchorXY('r').x ;

for(let leafNode of leafNodes){

    let {
        x
    } = leafNode.getAnchorXY('c') ;

    if(right < x){

        right = x ;
    }
}

return {
    left:Math.abs(x) + padding,
    top:Math.abs(y) + padding,
    bottom:padding,
    right:padding,
    width:right - Math.max(x , 0),
    height:bottom - Math.max(y , 0)
} ;

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559195440033" signature="<void> src::data.store.tree.insert(<number> index , <mixed> nodes)" entry-type="number mixed"  name="src::data.store.tree.insert">
        <![CDATA[(() =>{

                

                

                

                

                function main(index , nodes){

        
/**
 * 
 * 插入节点
 * 
 * @param {number} index 插入位置
 * 
 * @param {mixed} nodes 节点数据
 * 
 * 
 */

let me = this,
{
    recordset
} = me,
insertNodes = [];

nodes = recordset.add(nodes) ;

for(let node of nodes){

    insertNodes.push(node) ;

    insertNodes.push(...node.descendantNodes) ;
}

recordset.insert(index , insertNodes) ;

me.fireEvent('insert' , insertNodes) ;

return nodes ;

    }

                return function(index , nodes){

                    

                    

                    return main.call(this , index , nodes) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559027601568" signature="<void> src::data.store.tree.remove(<mixed> nodes)" entry-type="mixed"  name="src::data.store.tree.remove" imports="src::is.type src::is.array src::is.empty src::is.string src::array.from">
        <![CDATA[(() =>{

                let from;

                let var_init_locked_1561952820761;

                

                

                function main(nodes){

        
/**
 * 
 * 去除节点
 * 
 * @import from from array.from
 * 
 * @param {mixed} nodes 节点数据
 * 
 */

nodes = from(nodes) ;

let me = this,
    {
        recordset
    } = me,
    removeNodes = [] ;

for(let node of nodes){

    let {
        descendantNodes
    } = node ;

    removeNodes.push(...recordset.remove([
        node,
        ...descendantNodes
    ])) ;
}

me.fireEvent('remove' , removeNodes) ;

    }

                return function(nodes){

                    
        if(!var_init_locked_1561952820761){

            from = include('array.from');

            var_init_locked_1561952820761 = true ;
        }
        

                    

                    return main.call(this , nodes) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561094955772" signature="<void> src::data.store.tree.layout()"  name="src::data.store.tree.layout">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 布局
 * 
 */

 function main(){

    let me = this,
        {
            rootNode,
            recordset
        } = me,
        records = recordset.findRecords('hidden' , false);

    if(rootNode){

        rootNode.layout() ;

        let {
            region,
            marginRight,
            lineOffsetX
        } = me ;

        me.fireEvent('layout' , records , lines(records , region , marginRight , lineOffsetX)) ;
    }
 }

 function lines(nodes , {
    left,
    top
} , marginRight , lineOffsetX){

   let lines = [],
       halfMargin = marginRight / 2 ;

   for(let node of nodes){

        if(node.hidden){

            continue ;
        }

       let {
           x:startX,
           y:startY
       } = node.getAnchorXY('r'),
       {
           childNodes
       } = node;

       startX += lineOffsetX ;

       for(let childNode of childNodes){

           let {
               x:endX,
               y:endY
           } = childNode.getAnchorXY('l') ;

           lines.push([
               startX + left,
               startY + top,
               startX + halfMargin + left,
               startY + top,
               endX - halfMargin + left,
               endY + top,
               endX + left,
               endY + top
           ]) ;
       }
   }

   return lines ;
}

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559029908791" signature="<void> src::data.store.tree(<mixed> options)"  name="src::data.store.tree" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::mixin.observable.constructor src::mixin.observable.fire.data.last src::mixin.observable.listeners.clear.all src::mixin.observable.listeners.clear src::is.object.simple src::is.type src::is.function src::mixin.observable.listeners.add src::mixin.observable.listeners.remove src::is.string src::function.empty src::function.get src::mixin.observable.listener.add src::mixin.observable.listener.remove src::mixin.observable.listener.has src::is.array src::mixin.observable.fire src::mixin.observable.events.suspend src::mixin.observable.events.resume src::mixin.observable src::is.class src::class.create src::is.defined src::class.create.option src::object.proxy src::is.object src::object.key.join src::object.value.get src::is.empty src::array.from src::data.reader.json src::data.proxy.constructor src::is.promise src::data.proxy.read src::data.proxy src::data.proxy.memory src::data.proxy.create src::is.proxy.memory src::object.keys src::string.split src::object.value.set src::object.assign.if src::data.model.base src::data.model.fields src::data.model.store.bind.is src::id.generate src::data.model.id src::data.model.store.bind src::data.model.store.unbind src::data.model.has src::data.model.value.set src::data.model.value.get src::data.model.load src::data.model.destroy src::data.model src::is.class.from src::is.data.model.class src::data.model.get src::class.define src::data.model.create src::object.assign src::array.remove.index src::array.remove src::array.insert src::is.data.model src::array.clone src::data.recordset src::data.store.base src::object.link src::data.store.constructor src::data.store.add src::data.store.insert src::data.store.remove src::data.store.load src::data.store.clear src::data.store src::mixin.region.xy.anchor.get src::mixin.region.xy.anchor.set src::mixin.region src::is.object.empty src::data.model.node.tree.base src::math.region src::data.model.node.tree.region src::data.model.node.tree.depth src::data.model.node.tree.height.scope.child src::data.model.node.tree.height.scope src::data.model.node.tree.nodes.child src::data.model.node.tree.nodes.descendant src::data.model.node.tree.node.leaf.last src::data.model.node.tree.nodes.leaf src::data.model.node.tree.layout src::data.model.node.tree.expand src::data.model.node.tree.collapse src::data.model.node.tree.select src::data.model.node.tree.deselect src::data.model.node.tree.select.vertical src::data.model.node.tree.select.up src::data.model.node.tree.select.down src::data.model.node.tree.select.left src::math.point.distance src::data.model.node.tree.select.right src::data.model.node.tree.nodes.depth src::data.model.node.tree.append src::data.model.node.tree.insert.after src::data.model.node.tree.remove src::data.model.node.tree src::data.store.tree.base src::data.store.tree.region src::data.store.tree.insert src::data.store.tree.remove src::data.store.tree.layout">
        <![CDATA[(() =>{

                let extend,get_region,method_insert,method_remove,method_layout;

                let var_init_locked_1561952820767;

                let var_class_1561952820767;

                

                return function(options){

                    
        if(!var_init_locked_1561952820767){

            extend = include('src::data.store.tree.base')();
get_region = include('src::data.store.tree.region');
method_insert = include('src::data.store.tree.insert');
method_remove = include('src::data.store.tree.remove');
method_layout = include('src::data.store.tree.layout');

            var_init_locked_1561952820767 = true ;
        }
        

                    
        if(!var_class_1561952820767){

            class main extends mixins({extend , mixins:[]}){

            

            

            

            insert(...args){

            return method_insert.apply(this , args) ;

        }
remove(...args){

            return method_remove.apply(this , args) ;

        }
layout(...args){

            return method_layout.apply(this , args) ;

        }

            get region(){

                return get_region.call(this) ;
    
            }

        }

            var_class_1561952820767 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820767 ;
                }

            } ;
        }
        

                    return new var_class_1561952820767(options);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302054" signature="<void> src::canvas.draw.line.bezierCurve(<canvas.Context> context , {<array> points , <object> ...styles})"  name="src::canvas.draw.line.bezierCurve" imports="src::is.object.simple src::object.keys src::is.type src::is.object src::is.array src::is.empty src::string.split src::object.value.set src::is.defined src::object.key.join src::object.value.get src::object.assign">
        <![CDATA[(() =>{

                let assign;

                let var_init_locked_1561952820797;

                

                

                function main(context , {points , ...styles}){

        
/**
 * 
 * 绘制贝赛尔曲线
 * 
 * @import assign from object.assign
 * 
 * @param {canvas.Context} context 画板的上下文对象
 * 
 * @param {object} [config = {}] 画线配置
 * 
 * @param {array} [config.points = []] 画线点集合
 * 
 * @param {object} [...config.styles] 画线样式
 * 
 */

 if(points.length === 8){

    context.beginPath();

    assign(context , styles) ;

    context.moveTo(...points.slice(0 , 2));
    
    context.bezierCurveTo(...points.slice(2));
    
    context.stroke();
 }

    }

                return function(context , {points = [] , ...styles} = {}){

                    
        if(!var_init_locked_1561952820797){

            assign = include('object.assign');

            var_init_locked_1561952820797 = true ;
        }
        

                    

                    return main.call(this , context , {points , ...styles}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529945" signature="<number> src::math.degree2radian(<number> degree)" entry-type="number"  name="src::math.degree2radian">
        <![CDATA[(() =>{

                

                

                

                

                function main(degree){

        
/**
 * 
 * 将角度转换成弧度
 * 
 * @param {number} degree 角度
 * 
 * @return {number} 弧度 
 * 
 */

return Math.PI / 180 * degree ;


    }

                return function(degree){

                    

                    

                    return main.call(this , degree) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302046" signature="<void> src::canvas.draw.line.arc(<canvas.Context> context , {<number> x , <number> y , <number> r , <number> start , <number> end , <number> counterclockwise , <object> ...styles})"  name="src::canvas.draw.line.arc" imports="src::is.object.simple src::object.keys src::is.type src::is.object src::is.array src::is.empty src::string.split src::object.value.set src::is.defined src::object.key.join src::object.value.get src::object.assign src::math.degree2radian">
        <![CDATA[(() =>{

                let assign,degree2radian;

                let var_init_locked_1561952820804;

                

                

                function main(context , {x , y , r , start , end , counterclockwise , ...styles}){

        
/**
 * 
 * 绘制弧线
 * 
 * @import assign from object.assign
 * 
 * @import degree2radian from math.degree2radian
 * 
 * @param {canvas.Context} context 画板的上下文对象
 * 
 * @param {object} [config = {}] 画线配置
 * 
 * @param {number} config.x 圆中心点横坐标
 * 
 * @param {number} config.y 圆中心点纵坐标
 * 
 * @param {number} config.r 圆的半径
 * 
 * @param {number} [config.start = -90] 圆起始角度
 * 
 * @param {number} config.end 圆终止角度
 * 
 * @param {number} [config.counterclockwise = false] 如果为 false 则为顺时针，反之为逆时针
 * 
 * @param {object} [...config.styles] 画线样式
 * 
 */

context.beginPath();

assign(context , styles) ;

context.arc(x , y , r , degree2radian(start) , degree2radian(end) , counterclockwise) ;

context.stroke();

    }

                return function(context , {x , y , r , start = -90 , end , counterclockwise = false , ...styles} = {}){

                    
        if(!var_init_locked_1561952820804){

            assign = include('object.assign');
degree2radian = include('math.degree2radian');

            var_init_locked_1561952820804 = true ;
        }
        

                    

                    return main.call(this , context , {x , y , r , start , end , counterclockwise , ...styles}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529938" signature="<boolean> src::is.number(<mixed> data)" entry-type="mixed"  name="src::is.number" imports="src::is.type">
        <![CDATA[(() =>{

                let isType;

                let var_init_locked_1561952820809;

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为数值类型
 * 
 * @import is.type
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为数值类型则返回 true , 否则返回 false 
 * 
 */

return isType(data , 'number') && isFinite(data);

    }

                return function(data){

                    
        if(!var_init_locked_1561952820809){

            isType = include('is.type');

            var_init_locked_1561952820809 = true ;
        }
        

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557885936285" signature="<Date> src::date.get({<number> year , <number> month , <number> day , <number> hours , <number> minutes , <number> seconds})"  name="src::date.get" imports="src::is.defined">
        <![CDATA[(() =>{

                let isDefined;

                let var_init_locked_1561952820813;

                

                

                function main({year , month , day , hours , minutes , seconds}){

        
/**
 * 
 * 获得日期对象
 * 
 * @import is.defined
 * 
 * @param {object} [config = {}] 日期配置
 * 
 * @param {number} [config.year] 年份
 * 
 * @param {number} [config.month] 月份
 * 
 * @param {number} [config.day] 日
 * 
 * @param {number} [config.hours] 小时
 * 
 * @param {number} [config.minutes] 分钟
 * 
 * @param {number} [config.seconds] 秒
 * 
 * @return {Date} 日期对象 
 * 
 */

 let data = new Date() ;

 data.setMonth(0) ;

 data.setDate(1) ;

 data.setMilliseconds(0) ;

 if(isDefined(year)){

   data.setFullYear(year) ;
 }

 if(isDefined(month)){

   data.setMonth(month - 1) ;
 }

 if(isDefined(day)){

   data.setDate(day) ;
 
 }

 if(isDefined(hours)){

   data.setHours(hours) ;
 }

 if(isDefined(minutes)){

   data.setMinutes(minutes) ;
 }

 if(isDefined(seconds)){

   data.setSeconds(seconds) ;
 }

 return data ;



    }

                return function({year , month , day , hours , minutes , seconds} = {}){

                    
        if(!var_init_locked_1561952820813){

            isDefined = include('is.defined');

            var_init_locked_1561952820813 = true ;
        }
        

                    

                    return main.call(this , {year , month , day , hours , minutes , seconds}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557902435150" signature="<array> src::date.list(<Date> startDate , <Date> endDate , <number> count , <boolean> isNumber)" entry-type="Date Date number boolean"  name="src::date.list">
        <![CDATA[(() =>{

                

                

                

                

                function main(startDate , endDate , count , isNumber){

        
/**
 * 
 * 生成日期列表
 * 
 * @param {Date} startDate 起始日期
 * 
 * @param {Date} endDate 终止日期
 * 
 * @param {number} count 生成个数
 * 
 * @param {boolean} [isNumber = true] 是否以数字方式取代日期对象
 * 
 * @return {array} 日期列表
 * 
 */

let startTime = startDate.getTime(),
endTime = endDate.getTime(),
countTime = endTime - startTime ;

if(countTime % count !== 0){

   return [] ;
}

let time = countTime / count ;

if(time === 0){

   return [] ;
}

let result = [];

for(let i = startTime ; i <= endTime ; i += time){

   result.push(i) ;
}

if(!isNumber){

for(let i = 0 ; i < count ; i ++){

   result[i] = new Date(result[i]) ;
}
}

return result ;

    }

                return function(startDate , endDate , count , isNumber = true){

                    

                    

                    return main.call(this , startDate , endDate , count , isNumber) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557898516300" signature="<string> src::string.format(<string> format , <mixed> ...args)" entry-type="string mixed"  name="src::string.format">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 字符串格式化
 * 
 * @param {string} format 格式字符串
 * 
 * @param {mixed} [...args] 格式参数
 * 
 * @return {string} 格式化后的字符串 
 * 
 */

 let formatRe = /\{(\d+)\}/g ;

 function main(format , ...args){

    return format.replace(formatRe , (match , index) => args[index]) ;
 }

                return function(format , ...args){

                    

                    

                    return main.call(this , format , ...args) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557899514665" signature="<Date> src::date.parse(<string> data , <Regex> regex , <string> format)" entry-type="string Regex string"  name="src::date.parse" imports="src::string.format">
        <![CDATA[(() =>{

                let doFormat;

                let var_init_locked_1561952820822;

                

                

                function main(data , regex , format){

        /**
 * 
 * 将字符串转换成日期
 * 
 * @import doFormat from string.format
 * 
 * @param {string} data 字符串
 * 
 * @param {Regex} regex 正则表达式
 * 
 * @param {string} format 日期格式
 * 
 * @return {Date} 日期对象 
 * 
 */

 let args = data.match(regex) ;

 if(args){

    return new Date(doFormat(format , ...args)) ;
 }

 

    }

                return function(data , regex , format){

                    
        if(!var_init_locked_1561952820822){

            doFormat = include('string.format');

            var_init_locked_1561952820822 = true ;
        }
        

                    

                    return main.call(this , data , regex , format) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302752" signature="<Map> src::map.set.multi(<Map> map , <array> ...values)" entry-type="Map array"  name="src::map.set.multi" imports="src::is.type src::is.array">
        <![CDATA[(() =>{

                let isArray;

                let var_init_locked_1561952820826;

                

                

                function main(map , ...values){

        
/**
 * 
 * 设置复合值
 * 
 * @import is.array
 * 
 * @param {Map} map Map 对象
 * 
 * @param {array} [...values] 包含多维键，以及相应值
 * 
 * @return {Map} 返回 Map 对象引用
 * 
 */

let {
    length
} = values;

if(length >= 2){

    let key = values.slice(0 , length - 1),
        value = values[length - 1],
        oldValues = map.get(...key);

    if(isArray(oldValues)){

        if(!oldValues.includes(value)){

            oldValues.push(value) ;
        }
        
    }else{

        oldValues = [
            value
        ] ;
    }

    map.set(...key , oldValues) ;
}

return map ;

 

    }

                return function(map , ...values){

                    
        if(!var_init_locked_1561952820826){

            isArray = include('is.array');

            var_init_locked_1561952820826 = true ;
        }
        

                    

                    return main.call(this , map , ...values) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513303426" signature="<array> src::week.days(<number> startDay)" entry-type="number"  name="src::week.days">
        <![CDATA[(() =>{

                

                

                

                

                function main(startDay){

        
/**
 * 
 * 获得一周周期排列
 * 
 * @param {number} [startDay = 0] 确定起始周几
 * 
 * @return {array} 一周周期排列数字集合
 * 
 */

 let result = [
     startDay
 ];

while(result.length < 7){

    startDay ++ ;

    if(startDay <= 6){

        result.push(startDay) ;
    
    }else{

        result.push(startDay = 0) ;
    }
}

return result ;


    }

                return function(startDay = 0){

                    

                    

                    return main.call(this , startDay) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529933" signature="<boolean> src::is.date(<mixed> data)" entry-type="mixed"  name="src::is.date">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        /**
 * 
 * 判定数据是否为日期类型
 * 
 * @param {mixed} data 检验数据
 * 
 * @return {boolean} 如果给定值为日期类型则返回 true , 否则返回 false 
 * 
 */


 return Object.prototype.toString.call(data) === '[object Date]' ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302574" signature="<Date> src::date.prev(<Date> date , <number> step)" entry-type="Date number"  name="src::date.prev" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date">
        <![CDATA[(() =>{

                let get,getProperty,isDate;

                let var_init_locked_1561952820834;

                

                

                function main(date , step){

        /**
 * 
 * 基于当前日历向前移一次
 * 
 * @import get from date.get
 * 
 * @import getProperty from date.get.properties
 * 
 * @import is.date
 * 
 * @param {Date} date 基准日期
 * 
 * @param {number} [step = 1] 步长
 * 
 * @return {Date} 移过的日期 
 * 
 */

if(isDate(date)){

   date = getProperty(date , [
      'year',
      'month',
      'day'
   ]) ;
 }

 let {
   day,
   ...other
 } = date ;

 day -= step ;

 return get({
   day,
   ...other
 }) ;

    }

                return function(date , step = 1){

                    
        if(!var_init_locked_1561952820834){

            get = include('date.get');
getProperty = include('date.get.properties');
isDate = include('is.date');

            var_init_locked_1561952820834 = true ;
        }
        

                    

                    return main.call(this , date , step) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302561" signature="<Date> src::date.next(<Date|object> date , <number> step)" entry-type="Date number"  name="src::date.next" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date">
        <![CDATA[(() =>{

                let get,getProperty,isDate;

                let var_init_locked_1561952820839;

                

                

                function main(date , step){

        /**
 * 
 * 基于当前日历向后移一次
 * 
 * @import get from date.get
 * 
 * @import getProperty from date.get.properties
 * 
 * @import is.date
 * 
 * @param {Date | object} date 基准日期
 * 
 * @param {number} [step = 1] 步长
 * 
 * @return {Date} 移过的日期 
 * 
 */

 if(isDate(date)){

   date = getProperty(date , [
     'year',
     'month',
     'day'
   ]) ;
 }

 let {
   day,
   ...other
 } = date ;

 day += step ;

 return get({
   day,
   ...other
 }) ;

    }

                return function(date , step = 1){

                    
        if(!var_init_locked_1561952820839){

            get = include('date.get');
getProperty = include('date.get.properties');
isDate = include('is.date');

            var_init_locked_1561952820839 = true ;
        }
        

                    

                    return main.call(this , date , step) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302862" signature="<Date> src::month.date.last(<number> year , <number> month)" entry-type="number number"  name="src::month.date.last" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev">
        <![CDATA[(() =>{

                let get,prev,getLastDate;

                let var_init_locked_1561952820843;

                

                

                function main(year , month){

        
/**
 * 
 * 指定月份的最后日期
 * 
 * @import get from date.get
 * 
 * @import prev from date.prev
 * 
 * @import getLastDate from ..last
 * 
 * @param {number} year 年份
 * 
 * @param {number} month 月份
 * 
 * @return {Date} 日期对象 
 * 
 */

 let date = get({
      year,
      month,
      day:31
   }) ;

 if(month < 1 || month > 12){

    return getLastDate(date.getFullYear() , date.getMonth() + 1) ;
 }

 while(date.getMonth() + 1 !== month){

    date = prev(date) ;
 }

 return date ;


    }

                return function(year , month){

                    
        if(!var_init_locked_1561952820843){

            get = include('date.get');
prev = include('date.prev');
getLastDate = include('src::month.date.last');

            var_init_locked_1561952820843 = true ;
        }
        

                    

                    return main.call(this , year , month) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301888" signature="<array> src::calendar.month(<number> year , <number> month , {<number> row , <number> weekStartDay , <number> day , <boolean> ignoreNotCurrentMonthLastRow})"  name="src::calendar.month" imports="src::is.defined src::date.get src::week.days src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::date.next src::month.date.last">
        <![CDATA[(() =>{

                let get,getDays,prev,next,getLastDate;

                let var_init_locked_1561952820850;

                

                

                function main(year , month , {row , weekStartDay , day , ignoreNotCurrentMonthLastRow}){

        
/**
 * 
 * 显示以月份显示的日历数据
 * 
 * @import get from date.get
 * 
 * @import getDays from week.days
 * 
 * @import prev from date.prev
 * 
 * @import next from date.next
 * 
 * @import getLastDate from month.date.last
 * 
 * @param {number} year 年份
 * 
 * @param {number} month 月份
 * 
 * @param {object} [config = {}] 日历构造配置
 * 
 * @param {number} [config.row = 6] 日历显示行数
 * 
 * @param {number} [config.weekStartDay = 1] 每周从周几进行显示
 * 
 * @param {number} [config.day] 指定日期所在周作为日历的第一周
 * 
 * @param {boolean} [config.ignoreNotCurrentMonthLastRow = true] 是否忽略不是本月的尾行
 * 
 * @return {array} 一组日历数据 
 * 
 */


if(!day){

    day = 1 ;

}

let lastDay = getLastDate(year , month).getDate() ;

if(day > lastDay){

    day = lastDay ;
}

let date = get({
    year,
    month,
    day
}) ;

let days = getDays(weekStartDay),
    prevCount = days.indexOf(date.getDay()),
    nextCount = 6 - prevCount,
    result = [
        date
    ];

let currentDate = date ;

while(prevCount -- > 0){

    result.unshift(date = prev(date)) ;
}

date = currentDate ;

while(nextCount -- > 0){

    result.push(date = next(date)) ;
}

let count = (row - 1) ;

while(count -- > 0){

    date = next(date) ;

    if(ignoreNotCurrentMonthLastRow && date.getMonth() + 1 !== month){

        break ;
    }

    result.push(date) ;

    for(let i = 0 ; i < 6 ; i ++){

        result.push(date = next(date)) ;
    }

    
}

return result ;





    }

                return function(year , month , {row = 6 , weekStartDay = 1 , day , ignoreNotCurrentMonthLastRow = true} = {}){

                    
        if(!var_init_locked_1561952820850){

            get = include('date.get');
getDays = include('week.days');
prev = include('date.prev');
next = include('date.next');
getLastDate = include('month.date.last');

            var_init_locked_1561952820850 = true ;
        }
        

                    

                    return main.call(this , year , month , {row , weekStartDay , day , ignoreNotCurrentMonthLastRow}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301937" signature="<void> src::calendar.month.view.deselect()"  name="src::calendar.month.view.deselect">
        <![CDATA[(() =>{

                

                

                

                

                function main(){

        
/**
 * 
 * 清除当前日历所有选定
 * 
 */

 let me = this,
 {
    proxy,
    dates,
    selectedDate
 } = me ;

 if(selectedDate){

    selectedDate.selected = false ;

    proxy.call('deselect' , dates.indexOf(selectedDate) , selectedDate) ;
    
 }

 delete me.selectedDate ;
 

    }

                return function(){

                    

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301955" signature="<void> src::calendar.month.view.select(<number> year , <number> month , <number> day)" entry-type="number number number"  name="src::calendar.month.view.select" imports="src::calendar.month.view.deselect src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::month.date.last">
        <![CDATA[(() =>{

                let deselect,getLastDate,get;

                let var_init_locked_1561952820858;

                let var_current_scope_1561952820858;

                

                function main(year , month , day){

        
/**
 * 
 * 选定
 * 
 * @import deselect from ..deselect scoped
 * 
 * @import getLastDate from month.date.last
 * 
 * @import get from date.get.properties
 * 
 * @param {number} year 选定年份
 * 
 * @param {number} month 选定月份
 * 
 * @param {number} day 选定日期
 * 
 */

 let me = this,
 {
    proxy,
    selectedDate,
    dates
 } = me;

 deselect() ;

 let {
    day:lastDay
 } = get(getLastDate(year , month) , [
    'day'
 ]) ;

 if(day > lastDay){

   day = lastDay ;
 }

 let count = -1 ;

 for(let date of dates){

   let {
      year:itemYear,
      month:itemMonth,
      day:itemDay
   } = date ;

   count ++ ;

   if(itemYear === year && itemMonth === month && itemDay === day){

      me.selectedDate = date ;

      date.selected = true ;

      proxy.call('select' , count , date) ;

      break ;
   }
 }

 





    }

                return function(year , month , day){

                    
        if(!var_init_locked_1561952820858){

            getLastDate = include('month.date.last');
get = include('date.get.properties');

            var_init_locked_1561952820858 = true ;
        }
        

                    
        if(!var_current_scope_1561952820858 !== this){

            deselect = include('src::calendar.month.view.deselect').bind(this);

            var_current_scope_1561952820858 = this ;
        }
        

                    return main.call(this , year , month , day) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301988" signature="<void> src::calendar.month.view.selectMonth(<number> year , <number> month)" entry-type="number number"  name="src::calendar.month.view.selectMonth" imports="src::is.defined src::date.get src::week.days src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select">
        <![CDATA[(() =>{

                let getDates,deselect,select,getProperty;

                let var_init_locked_1561952820864;

                let var_current_scope_1561952820864;

                

                function main(year , month){

        
/**
 * 
 * 选定月份
 * 
 * @import getDates from ......month
 * 
 * @import deselect from ..deselect scoped
 * 
 * @import select from ..select scoped
 * 
 * @import getProperty from date.get.properties
 * 
 * @param {number} year 年份
 * 
 * @param {number} month 月份
 * 
 */

 let me = this,
 {
    selectedDate,
    weekStartDay,
    viewConfig
 } = me;

 deselect() ;

 let fields = [
        'year',
        'month',
        'day'
     ],
     dates = me.dates = getDates(year , month , {
        ...viewConfig,
        weekStartDay
     }).map(date =>{

        let {
            year:itemYear,
            month:itemMonth,
            day
        } = getProperty(date , fields),
        activate = year === itemYear && month === itemMonth;
        
        return {
            activate,
            year:itemYear,
            month:itemMonth,
            day,
            selected:false,
            key:date.getTime()
        } ;

    }) ;

me.year = year ;

me.month = month ;

me.proxy.call('load' , year , month , dates) ;

if(selectedDate){

    let {
        day
    } = selectedDate,
    {
        year,
        month
    } = me;

    select(year , month , day) ;
}

    }

                return function(year , month){

                    
        if(!var_init_locked_1561952820864){

            getDates = include('src::calendar.month');
getProperty = include('date.get.properties');

            var_init_locked_1561952820864 = true ;
        }
        

                    
        if(!var_current_scope_1561952820864 !== this){

            deselect = include('src::calendar.month.view.deselect').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820864 = this ;
        }
        

                    return main.call(this , year , month) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301904" signature="<void> src::calendar.month.view.constructor(<mixed> target , {<object> selectedDate , <number> weekStartDay , <object> viewConfig})"  name="src::calendar.month.view.constructor" imports="src::object.proxy src::is.defined src::date.get src::week.days src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth">
        <![CDATA[(() =>{

                let getProxy,selectMonth,select,getProperty;

                let var_init_locked_1561952820870;

                let var_current_scope_1561952820870;

                

                function main(target , {selectedDate , weekStartDay , viewConfig}){

        
/**
 * 
 * 构建一个月基日历
 * 
 * @import getProxy from object.proxy
 * 
 * @import selectMonth from ..selectMonth scoped
 * 
 * @import select from ..select scoped
 * 
 * @import getProperty from date.get.properties
 * 
 * @param {mixed} target 可提供日历显示的套件
 * 
 * @param {object} [config = {}] 初始化配置
 * 
 * @param {object} [config.selectedDate] 初始化选择日期
 * 
 * @param {number} [config.weekStartDay = 0] 默认从星期天进行计算
 * 
 * @param {object} [config.viewConfig = {}] 日历视图设置
 * 
 */

 let me = this ;

 me.viewConfig = viewConfig ;

 me.weekStartDay = weekStartDay ;

 me.proxy = getProxy(target) ;

 me.selectedDates = [] ;

 me.dates = [] ;

 if(!selectedDate){

    selectedDate = getProperty(new Date() , [
        'year',
        'month',
        'day'
    ]) ;
 }

 let {
     year,
     month,
     day
 } = selectedDate ;

 selectMonth(year , month) ;

 select(year , month , day) ;

    }

                return function(target , {selectedDate , weekStartDay = 0 , viewConfig = {}} = {}){

                    
        if(!var_init_locked_1561952820870){

            getProxy = include('object.proxy');
getProperty = include('date.get.properties');

            var_init_locked_1561952820870 = true ;
        }
        

                    
        if(!var_current_scope_1561952820870 !== this){

            selectMonth = include('src::calendar.month.view.selectMonth').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820870 = this ;
        }
        

                    return main.call(this , target , {selectedDate , weekStartDay , viewConfig}) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302697" signature="<boolean> src::is.week.day.first(<Date> date , <number> weekStartDay)" entry-type="Date number"  name="src::is.week.day.first">
        <![CDATA[(() =>{

                

                

                

                

                function main(date , weekStartDay){

        
/**
 * 
 * 判断指定日期是否为一周的第一天
 * 
 * @param {Date} date 校验日期
 * 
 * @param {number} [weekStartDay = 1] 确认一周是从周几算起
 *  
 * @return {boolean} 如果是第一天的话，则返回 true , 否则返回 false
 * 
 */

 return date.getDay() === weekStartDay ;

 

    }

                return function(date , weekStartDay = 1){

                    

                    

                    return main.call(this , date , weekStartDay) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302884" signature="<Date> src::month.prev(<Date|object> date)" entry-type="Date"  name="src::month.prev" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date">
        <![CDATA[(() =>{

                let get,getProperty,isDate;

                let var_init_locked_1561952820880;

                

                

                function main(date){

        /**
 * 
 * 获得指定月份的上一个月份
 * 
 * @import get from date.get
 * 
 * @import getProperty from date.get.properties
 * 
 * @import is.date
 * 
 * @param {Date | object} date 包括月份的日期对象 
 * 
 * @return {Date} 上一个月份 
 * 
 */

 if(isDate(date)){

    date = getProperty(date , [
        'year',
        'month'
    ]) ;
 }

 let {
    month,
    ...other
 } = date ;

 month -- ;

 console.log(month , get({
   month,
  ...other
}).toJSON()) ;

 return get({
     month,
    ...other
 }) ;

    }

                return function(date){

                    
        if(!var_init_locked_1561952820880){

            get = include('date.get');
getProperty = include('date.get.properties');
isDate = include('is.date');

            var_init_locked_1561952820880 = true ;
        }
        

                    

                    return main.call(this , date) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302008" signature="<void> src::calendar.month.view.selectPrevMonth()"  name="src::calendar.month.view.selectPrevMonth" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::month.prev src::week.days src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth">
        <![CDATA[(() =>{

                let prev,getProperty,selectMonth;

                let var_init_locked_1561952820884;

                let var_current_scope_1561952820884;

                

                function main(){

        
/**
 * 
 * 向上移动月份
 * 
 * @import prev from month.prev
 * 
 * @import getProperty from date.get.properties
 * 
 * @import selectMonth from ..selectMonth scoped
 * 
 */

let me = this,
{
    year,
    month
} = me,
{
   year:selectedYear,
   month:selectedMonth
} = getProperty(prev({
    year,
    month
}) , [
    'year',
    'month'
]) ;

selectMonth(selectedYear , selectedMonth) ;

    }

                return function(){

                    
        if(!var_init_locked_1561952820884){

            prev = include('month.prev');
getProperty = include('date.get.properties');

            var_init_locked_1561952820884 = true ;
        }
        

                    
        if(!var_current_scope_1561952820884 !== this){

            selectMonth = include('src::calendar.month.view.selectMonth').bind(this);

            var_current_scope_1561952820884 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301977" signature="<void> src::calendar.month.view.selectLeft()"  name="src::calendar.month.view.selectLeft" imports="src::is.week.day.first src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::month.prev src::week.days src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth src::calendar.month.view.selectPrevMonth">
        <![CDATA[(() =>{

                let isFirst,get,prevMonth,prevDate,getProperty,select;

                let var_init_locked_1561952820891;

                let var_current_scope_1561952820891;

                

                function main(){

        /**
 * 
 * 向左移一个格
 * 
 * @import isFirst from is.week.day.first
 * 
 * @import get from date.get
 * 
 * @import prevMonth from ..selectPrevMonth scoped
 * 
 * @import prevDate from date.prev
 * 
 * @import getProperty from date.get.properties
 * 
 * @import select from ..select scoped
 * 
 */

 let {
    selectedDate,
    weekStartDay,
    month
 } = this ;

 if(selectedDate){

    let date = get(selectedDate) ;

    if(isFirst(date , weekStartDay)){

        prevMonth() ;
    
    }else{

        date = prevDate(date) ;

        let {
            year:prevYearValue,
            month:prevMonthValue,
            day
        } = getProperty(date , [
            'year',
            'month',
            'day'
        ]) ;

        if(prevMonthValue !== month){

            prevMonth() ;
        }

        select(prevYearValue , prevMonthValue , day) ;
    }
 }

    }

                return function(){

                    
        if(!var_init_locked_1561952820891){

            isFirst = include('is.week.day.first');
get = include('date.get');
prevDate = include('date.prev');
getProperty = include('date.get.properties');

            var_init_locked_1561952820891 = true ;
        }
        

                    
        if(!var_current_scope_1561952820891 !== this){

            prevMonth = include('src::calendar.month.view.selectPrevMonth').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820891 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302702" signature="<boolean> src::is.week.day.last(<Date> date , <number> weekStartDay)" entry-type="Date number"  name="src::is.week.day.last" imports="src::week.days">
        <![CDATA[(() =>{

                let getDays;

                let var_init_locked_1561952820897;

                

                

                function main(date , weekStartDay){

        
/**
 * 
 * 判断指定日期是否为一周的最后一天
 * 
 * @import getDays from week.days
 * 
 * @param {Date} date 校验日期
 * 
 * @param {number} [weekStartDay = 1] 确认一周是从周几算起
 *  
 * @return {boolean} 如果是最后一天的话，则返回 true , 否则返回 false
 * 
 */

let days = getDays(weekStartDay) ;

return date.getDay() === days[days.length - 1] ;

    }

                return function(date , weekStartDay = 1){

                    
        if(!var_init_locked_1561952820897){

            getDays = include('week.days');

            var_init_locked_1561952820897 = true ;
        }
        

                    

                    return main.call(this , date , weekStartDay) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302882" signature="<Date> src::month.next(<Date|object> date)" entry-type="Date"  name="src::month.next" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date">
        <![CDATA[(() =>{

                let get,getProperty,isDate;

                let var_init_locked_1561952820900;

                

                

                function main(date){

        /**
 * 
 * 获得指定月份的下一个月份
 * 
 * @import get from date.get
 * 
 * @import getProperty from date.get.properties
 * 
 * @import is.date
 * 
 * @param {Date | object} date 包括月份的日期对象 
 * 
 * @return {Date} 下一个月份 
 * 
 */

 if(isDate(date)){

    date = getProperty(date , [
        'year',
        'month'
    ]) ;
 }

 let {
    month,
    ...other
 } = date ;

 month ++ ;

 return get({
     month,
    ...other
 }) ;

    }

                return function(date){

                    
        if(!var_init_locked_1561952820900){

            get = include('date.get');
getProperty = include('date.get.properties');
isDate = include('is.date');

            var_init_locked_1561952820900 = true ;
        }
        

                    

                    return main.call(this , date) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301996" signature="<void> src::calendar.month.view.selectNextMonth()"  name="src::calendar.month.view.selectNextMonth" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::month.next src::week.days src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth">
        <![CDATA[(() =>{

                let next,getProperty,selectMonth;

                let var_init_locked_1561952820904;

                let var_current_scope_1561952820904;

                

                function main(){

        
/**
 * 
 * 向下移动月份
 * 
 * @import next from month.next
 * 
 * @import getProperty from date.get.properties
 * 
 * @import selectMonth from ..selectMonth scoped
 * 
 */

 let me = this,
 {
     year,
     month
 } = me,
 {
    year:selectedYear,
    month:selectedMonth
 } = getProperty(next({
     year,
     month
 }) , [
     'year',
     'month'
 ]) ;


 selectMonth(selectedYear , selectedMonth) ;

    }

                return function(){

                    
        if(!var_init_locked_1561952820904){

            next = include('month.next');
getProperty = include('date.get.properties');

            var_init_locked_1561952820904 = true ;
        }
        

                    
        if(!var_current_scope_1561952820904 !== this){

            selectMonth = include('src::calendar.month.view.selectMonth').bind(this);

            var_current_scope_1561952820904 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302019" signature="<void> src::calendar.month.view.selectRight()"  name="src::calendar.month.view.selectRight" imports="src::week.days src::is.week.day.last src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::month.next src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth src::calendar.month.view.selectNextMonth">
        <![CDATA[(() =>{

                let isLast,get,nextMonth,nextDate,getProperty,select;

                let var_init_locked_1561952820911;

                let var_current_scope_1561952820911;

                

                function main(){

        
/**
 * 
 * 向右移一个格日期
 * 
 * @import isLast from is.week.day.last
 * 
 * @import get from date.get
 * 
 * @import nextMonth from ..selectNextMonth scoped
 * 
 * @import nextDate from date.next
 * 
 * @import getProperty from date.get.properties
 * 
 * @import select from ..select scoped
 * 
 */


let {
    selectedDate,
    weekStartDay,
    month
 } = this ;

 if(selectedDate){

    let date = get(selectedDate) ;

    if(isLast(date , weekStartDay)){

        nextMonth() ;
    
    }else{

        date = nextDate(date) ;

        let {
            year:nextYearValue,
            month:nextMonthValue,
            day
        } = getProperty(date , [
            'year',
            'month',
            'day'
        ]) ;

        if(nextMonthValue !== month){

            nextMonth() ;
        }

        select(nextYearValue , nextMonthValue , day) ;
    }
 }

    }

                return function(){

                    
        if(!var_init_locked_1561952820911){

            isLast = include('is.week.day.last');
get = include('date.get');
nextDate = include('date.next');
getProperty = include('date.get.properties');

            var_init_locked_1561952820911 = true ;
        }
        

                    
        if(!var_current_scope_1561952820911 !== this){

            nextMonth = include('src::calendar.month.view.selectNextMonth').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820911 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302856" signature="<Date> src::month.date.first(<number> year , <number> month)" entry-type="number number"  name="src::month.date.first" imports="src::is.defined src::date.get">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952820917;

                

                

                function main(year , month){

        
/**
 * 
 * 指定月份的第一个日期
 * 
 * @import get from date.get
 * 
 * @param {number} year 年份
 * 
 * @param {number} month 月份
 * 
 * @return {Date} 日期对象 
 * 
 */

 return get({
      year,
      month,
      day:1
   }) ;

    }

                return function(year , month){

                    
        if(!var_init_locked_1561952820917){

            get = include('date.get');

            var_init_locked_1561952820917 = true ;
        }
        

                    

                    return main.call(this , year , month) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302874" signature="<Date[]> src::month.dates.week.first(<number> year , <number> month , <number> weekStartDay)" entry-type="number number number"  name="src::month.dates.week.first" imports="src::week.days src::is.defined src::date.get src::month.date.first src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.next">
        <![CDATA[(() =>{

                let getDays,getFirstDate,next;

                let var_init_locked_1561952820921;

                

                

                function main(year , month , weekStartDay){

        
/**
 * 
 * 获得指定月份的第一周的所有日期
 * 
 * @import getDays from week.days
 * 
 * @import getFirstDate from month.date.first
 * 
 * @import next from date.next
 * 
 * @param {number} year 年份 
 * 
 * @param {number} month 月份
 * 
 * @param {number} [weekStartDay = 1] 确定一周从周几进行计算
 * 
 * @return {Date[]} 一周里所有的日期 
 * 
 */

let days = getDays(weekStartDay),
    date = getFirstDate(year , month),
    firstIndex = days.indexOf(date.getDay()),
    result = [
        date
    ];

for(let i = firstIndex + 1; i < 7 ; i ++){

    result.push(date = next(date)) ;
}

return result ;



 

    }

                return function(year , month , weekStartDay = 1){

                    
        if(!var_init_locked_1561952820921){

            getDays = include('week.days');
getFirstDate = include('month.date.first');
next = include('date.next');

            var_init_locked_1561952820921 = true ;
        }
        

                    

                    return main.call(this , year , month , weekStartDay) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302578" signature="<Date> src::date.prev.week(<Date|object> date)" entry-type="Date"  name="src::date.prev.week" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev">
        <![CDATA[(() =>{

                let prev;

                let var_init_locked_1561952820925;

                

                

                function main(date){

        
/**
 * 
 * 基于当前日期的上一周的日期
 * 
 * @import prev from date.prev
 * 
 * @param {Date | object} date 基准日期
 * 
 * @return {Date} 移过的日期 
 * 
 */

return prev(date , 7) ;


    }

                return function(date){

                    
        if(!var_init_locked_1561952820925){

            prev = include('date.prev');

            var_init_locked_1561952820925 = true ;
        }
        

                    

                    return main.call(this , date) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302032" signature="<void> src::calendar.month.view.selectUp()"  name="src::calendar.month.view.selectUp" imports="src::week.days src::is.defined src::date.get src::month.date.first src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.next src::month.dates.week.first src::array.dates.includes src::month.prev src::date.prev src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth src::calendar.month.view.selectPrevMonth src::date.prev.week">
        <![CDATA[(() =>{

                let getFirstWeekDates,includes,get,prevMonth,prevDate,getProperty,select;

                let var_init_locked_1561952820930;

                let var_current_scope_1561952820930;

                

                function main(){

        
/**
 * 
 * 向上移一格日期
 * 
 * @import getFirstWeekDates from month.dates.week.first
 * 
 * @import includes from array.dates.includes
 * 
 * @import get from date.get
 * 
 * @import prevMonth from ..selectPrevMonth scoped
 * 
 * @import prevDate from date.prev.week
 * 
 * @import getProperty from date.get.properties
 * 
 * @import select from ..select scoped
 * 
 */

let {
    selectedDate,
    weekStartDay,
    year,
    month
 } = this ;

 if(selectedDate){

    let dates = getFirstWeekDates(year , month , weekStartDay),
        date = get(selectedDate) ;

    if(includes(dates , date)){

        prevMonth() ;
    
    }else{

        date = prevDate(date) ;

        let {
            year:prevYearValue,
            month:prevMonthValue,
            day
        } = getProperty(date , [
            'year',
            'month',
            'day'
        ]) ;

        if(prevMonthValue !== month){

            prevMonth() ;
        }

        select(prevYearValue , prevMonthValue , day) ;
    }
 }

    }

                return function(){

                    
        if(!var_init_locked_1561952820930){

            getFirstWeekDates = include('month.dates.week.first');
includes = include('array.dates.includes');
get = include('date.get');
prevDate = include('date.prev.week');
getProperty = include('date.get.properties');

            var_init_locked_1561952820930 = true ;
        }
        

                    
        if(!var_current_scope_1561952820930 !== this){

            prevMonth = include('src::calendar.month.view.selectPrevMonth').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820930 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302878" signature="<Date[]> src::month.dates.week.last(<number> year , <number> month , <number> weekStartDay)" entry-type="number number number"  name="src::month.dates.week.last" imports="src::week.days src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::month.date.last">
        <![CDATA[(() =>{

                let getDays,getLastDate,prev;

                let var_init_locked_1561952820938;

                

                

                function main(year , month , weekStartDay){

        
/**
 * 
 * 获得指定月份的最后一周的所有日期
 * 
 * @import getDays from week.days
 * 
 * @import getLastDate from month.date.last
 * 
 * @import prev from date.prev
 * 
 * @param {number} year 年份 
 * 
 * @param {number} month 月份
 * 
 * @param {number} [weekStartDay = 1] 确定一周从周几进行计算
 * 
 * @return {Date[]} 一周里所有的日期 
 * 
 */

let days = getDays(weekStartDay),
    date = getLastDate(year , month),
    lastIndex = days.indexOf(date.getDay()),
    result = [
        date
    ];

for(let i = lastIndex - 1; i >= 0 ; i --){

    result.push(date = prev(date)) ;
}

return result ;

    }

                return function(year , month , weekStartDay = 1){

                    
        if(!var_init_locked_1561952820938){

            getDays = include('week.days');
getLastDate = include('month.date.last');
prev = include('date.prev');

            var_init_locked_1561952820938 = true ;
        }
        

                    

                    return main.call(this , year , month , weekStartDay) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302570" signature="<Date> src::date.next.week(<Date|object> date)" entry-type="Date"  name="src::date.next.week" imports="src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.next">
        <![CDATA[(() =>{

                let next;

                let var_init_locked_1561952820943;

                

                

                function main(date){

        
/**
 * 
 * 基于当前日期的下一周的日期
 * 
 * @import next from date.next
 * 
 * @param {Date | object} date 基准日期
 * 
 * @return {Date} 移过的日期 
 * 
 */

 return next(date , 7) ;

 

    }

                return function(date){

                    
        if(!var_init_locked_1561952820943){

            next = include('date.next');

            var_init_locked_1561952820943 = true ;
        }
        

                    

                    return main.call(this , date) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301965" signature="<void> src::calendar.month.view.selectDown()"  name="src::calendar.month.view.selectDown" imports="src::week.days src::is.defined src::date.get src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::month.date.last src::month.dates.week.last src::array.dates.includes src::month.next src::date.next src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth src::calendar.month.view.selectNextMonth src::date.next.week">
        <![CDATA[(() =>{

                let getLastWeekDates,includes,get,nextMonth,nextDate,getProperty,select;

                let var_init_locked_1561952820949;

                let var_current_scope_1561952820949;

                

                function main(){

        
/**
 * 
 * 向下移一格日期
 * 
 * @import getLastWeekDates from month.dates.week.last
 * 
 * @import includes from array.dates.includes
 * 
 * @import get from date.get
 * 
 * @import nextMonth from ..selectNextMonth scoped
 * 
 * @import nextDate from date.next.week
 * 
 * @import getProperty from date.get.properties
 * 
 * @import select from ..select scoped
 * 
 */

 let {
    selectedDate,
    weekStartDay,
    year,
    month
 } = this ;

 if(selectedDate){

    let dates = getLastWeekDates(year , month , weekStartDay),
        date = get(selectedDate) ;

    if(includes(dates , date)){

        nextMonth() ;
    
    }else{

        date = nextDate(date) ;

        let {
            year:nextYearValue,
            month:nextMonthValue,
            day
        } = getProperty(date , [
            'year',
            'month',
            'day'
        ]) ;

        if(nextMonthValue !== month){

            nextMonth() ;
        }

        select(nextYearValue , nextMonthValue , day) ;
    }
 }



    }

                return function(){

                    
        if(!var_init_locked_1561952820949){

            getLastWeekDates = include('month.dates.week.last');
includes = include('array.dates.includes');
get = include('date.get');
nextDate = include('date.next.week');
getProperty = include('date.get.properties');

            var_init_locked_1561952820949 = true ;
        }
        

                    
        if(!var_current_scope_1561952820949 !== this){

            nextMonth = include('src::calendar.month.view.selectNextMonth').bind(this);
select = include('src::calendar.month.view.select').bind(this);

            var_current_scope_1561952820949 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301894" signature="<void> src::calendar.month.view(<mixed> target , <mixed> config)" entry-type=" "  name="src::calendar.month.view" imports="src::class.empty src::object.proxy src::is.defined src::date.get src::week.days src::is.type src::is.array src::is.empty src::is.string src::array.from src::date.get.properties src::is.date src::date.prev src::date.next src::month.date.last src::calendar.month src::calendar.month.view.deselect src::calendar.month.view.select src::calendar.month.view.selectMonth src::calendar.month.view.constructor src::is.week.day.first src::month.prev src::calendar.month.view.selectPrevMonth src::calendar.month.view.selectLeft src::is.week.day.last src::month.next src::calendar.month.view.selectNextMonth src::calendar.month.view.selectRight src::month.date.first src::month.dates.week.first src::array.dates.includes src::date.prev.week src::calendar.month.view.selectUp src::month.dates.week.last src::date.next.week src::calendar.month.view.selectDown">
        <![CDATA[(() =>{

                let extend,constructor,method_select,method_selectLeft,method_selectRight,method_selectUp,method_selectDown,method_selectMonth,method_selectNextMonth,method_selectPrevMonth,method_deselect;

                let var_init_locked_1561952820958;

                let var_class_1561952820958;

                

                return function(target , config){

                    
        if(!var_init_locked_1561952820958){

            extend = include('class.empty')();
constructor = include('src::calendar.month.view.constructor');
method_select = include('src::calendar.month.view.select');
method_selectLeft = include('src::calendar.month.view.selectLeft');
method_selectRight = include('src::calendar.month.view.selectRight');
method_selectUp = include('src::calendar.month.view.selectUp');
method_selectDown = include('src::calendar.month.view.selectDown');
method_selectMonth = include('src::calendar.month.view.selectMonth');
method_selectNextMonth = include('src::calendar.month.view.selectNextMonth');
method_selectPrevMonth = include('src::calendar.month.view.selectPrevMonth');
method_deselect = include('src::calendar.month.view.deselect');

            var_init_locked_1561952820958 = true ;
        }
        

                    
        if(!var_class_1561952820958){

            class main {

            

            

            constructor(...args){

            

            constructor.apply(this , args) ;

        }

            select(...args){

            return method_select.apply(this , args) ;

        }
selectLeft(...args){

            return method_selectLeft.apply(this , args) ;

        }
selectRight(...args){

            return method_selectRight.apply(this , args) ;

        }
selectUp(...args){

            return method_selectUp.apply(this , args) ;

        }
selectDown(...args){

            return method_selectDown.apply(this , args) ;

        }
selectMonth(...args){

            return method_selectMonth.apply(this , args) ;

        }
selectNextMonth(...args){

            return method_selectNextMonth.apply(this , args) ;

        }
selectPrevMonth(...args){

            return method_selectPrevMonth.apply(this , args) ;

        }
deselect(...args){

            return method_deselect.apply(this , args) ;

        }

            

        }

            var_class_1561952820958 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952820958 ;
                }

            } ;
        }
        

                    return new var_class_1561952820958(target , config);
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301804" signature="<Map> src::browser.global.listener.map()"  name="src::browser.global.listener.map" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map">
        <![CDATA[(() =>{

                let map;

                let var_init_locked_1561952820968;

                

                let var_once_value_1561952820968;

                function main(){

        
/**
 * 
 * 维护全局事件监听对象
 * 
 * @import map value
 * 
 * @return {Map} 集合对象 
 * 
 * @once
 * 
 */

 return map ;

    }

                return function(){

                    
        if(!var_init_locked_1561952820968){

            map = include('map')();

            var_init_locked_1561952820968 = true ;
        }
        

                    

                    
        if(var_once_value_1561952820968){

            return var_once_value_1561952820968 ;

        }
        return var_once_value_1561952820968 = main.call(this ) ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301836" signature="<boolean> src::browser.selector.is(<HTMLElement> el , <string> selector)" entry-type="HTMLElement string"  name="src::browser.selector.is">
        <![CDATA[(() =>{

                

                

                

                

                function main(el , selector){

        
/**
 * 
 * 判断元素是否匹配选择器
 * 
 * @param {HTMLElement} el 元素
 * 
 * @param {string} selector 选择器字符串
 * 
 * @return {boolean} 如果元素匹配选择器则返回 true , 否则返回 false 
 * 
 */

 let {
    ownerDocument
 } = el ;

 let els = Array.from(ownerDocument.querySelectorAll(selector)) ;

 return els.includes(el) ;

    }

                return function(el , selector){

                    

                    

                    return main.call(this , el , selector) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301840" signature="<boolean> src::browser.selector.parent(<HTMLElement> el , <string> selector)" entry-type="HTMLElement string"  name="src::browser.selector.parent" imports="src::browser.selector.is">
        <![CDATA[(() =>{

                let is;

                let var_init_locked_1561952820975;

                

                

                function main(el , selector){

        
/**
 * 
 * 判断元素及其元素父祖级元素是否匹配选择器
 * 
 * @import is from ..is
 * 
 * @param {HTMLElement} el 元素
 * 
 * @param {string} selector 选择器
 * 
 * @return {boolean} 如果匹配则返回 true , 否则返回 false 
 * 
 */

 while(el){

    if(is(el , selector)){

        return el ;
    }

    el = el.parentElement ;
 }

    }

                return function(el , selector){

                    
        if(!var_init_locked_1561952820975){

            is = include('src::browser.selector.is');

            var_init_locked_1561952820975 = true ;
        }
        

                    

                    return main.call(this , el , selector) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560907540397" signature="<mixed> src::browser.global.listener.add(<string> event , <function> fn , <string> selector)" entry-type="string function string"  name="src::browser.global.listener.add" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map src::browser.selector.is src::browser.selector.parent">
        <![CDATA[(() =>{

                let getMap,is;

                let var_init_locked_1561952820978;

                

                

                function main(event , fn , selector){

        
/**
 * 
 * 监听全局事件
 * 
 * @import getMap from ..map
 * 
 * @import is from browser.selector.parent
 * 
 * @param {string} event 目标监听事件
 * 
 * @param {function} fn 目标监听回调
 * 
 * @param {string} selector 选择器
 * 
 * @return {mixed} 返回说明 
 * 
 */

let map = getMap(),
    listenerFn = e =>{

        let {
            target
        } = e ;

        if(selector){

            if(is(target , selector)){

                fn(e) ;
            }
            
        }else{

            fn(e) ;
        }
    };

map.set(event , fn , selector , listenerFn) ;

window.addEventListener(event , listenerFn) ;




    }

                return function(event , fn , selector){

                    
        if(!var_init_locked_1561952820978){

            getMap = include('src::browser.global.listener.map');
is = include('browser.selector.parent');

            var_init_locked_1561952820978 = true ;
        }
        

                    

                    return main.call(this , event , fn , selector) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560907550989" signature="<mixed> src::browser.global.listener.remove(<string> event , <function> fn , <string> selector)" entry-type="string function string"  name="src::browser.global.listener.remove" imports="src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map">
        <![CDATA[(() =>{

                let getMap;

                let var_init_locked_1561952820983;

                

                

                function main(event , fn , selector){

        
/**
 * 
 * 去除监听全局事件
 * 
 * @import getMap from ..map
 * 
 * @param {string} event 目标监听事件
 * 
 * @param {function} fn 目标监听回调
 * 
 * @param {string} selector 选择器
 * 
 * @return {mixed} 返回说明 
 * 
 */

 let map = getMap(),
    listenerFn = map.get(event , fn , selector);

if(listenerFn){

    window.removeEventListener(event , listenerFn) ;
}

    }

                return function(event , fn , selector){

                    
        if(!var_init_locked_1561952820983){

            getMap = include('src::browser.global.listener.map');

            var_init_locked_1561952820983 = true ;
        }
        

                    

                    return main.call(this , event , fn , selector) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559115972842" signature="<mixed> src::browser.event.key(<Event> event)" entry-type="Event"  name="src::browser.event.key">
        <![CDATA[(() =>{

                

                

                

                

                
/**
 * 
 * 得到对应代码的值键值 
 * 
 * @param {Event} event 键事件对象
 * 
 * @return {mixed} 键值 
 * 
 */

 const KEY_CODES = {
    39:'DIRECTION::RIGHT',
    37:'DIRECTION::LEFT',
    38:'DIRECTION::UP',
    40:'DIRECTION::DOWN',
    13:'ENTER',
    46:'DELETE',
    9:'TAB',
    107:'+',
    187:'=',
    27:'ESC'
 };

 function main({
    shiftKey,
    keyCode
 }){

    return {
       shift:shiftKey,
       key:KEY_CODES[keyCode],
       code:keyCode
    } ;
 }

                return function(event){

                    

                    

                    return main.call(this , event) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560845081876" signature="<mixed> src::browser.event.single(<Event> e , <string> name)" entry-type="Event string"  name="src::browser.event.single">
        <![CDATA[(() =>{

                

                

                

                

                function main(e , name){

        
/**
 * 
 * 获得单一的事件对象
 * 
 * @param {Event} e 事件对象
 * 
 * @param {string} name 事件名称
 *  
 * @return {mixed} 事件对象 
 * 
 */

 let touches ;

 switch(name){

    case 'start':
    case 'move':

        touches = e.touches ;

        break ;

    case 'end':

        touches = e.changedTouches ;
 }

 if(touches){

    return touches[0] ;
 }

 return e ;

    }

                return function(e , name){

                    

                    

                    return main.call(this , e , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560845110363" signature="<void> src::browser.event.touches(<Event> e)" entry-type="Event"  name="src::browser.event.touches">
        <![CDATA[(() =>{

                

                

                

                

                function main(e){

        
/**
 * 
 * 如果当前设备处于多点触控时返回，其它时候返回空
 * 
 * @param {Event} e 事件对象
 * 
 */

let touches ;

switch(name){

   case 'start':
   case 'move':

       touches = e.touches ;

       break ;

   case 'end':

       touches = e.changedTouches
}

if(touches && touches.length > 1){

    return touches ;
}

    }

                return function(e){

                    

                    

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560847628821" signature="<void> src::browser.event.stop(<Event> e)" entry-type="Event"  name="src::browser.event.stop">
        <![CDATA[(() =>{

                

                

                

                

                function main(e){

        
/**
 * 
 * 停止事件
 * 
 * @param {Event} e 事件对象
 * 
 */

 e.preventDefault() ;

 e.stopPropagation() ;

    }

                return function(e){

                    

                    

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301873" signature="<boolean> src::is.browser.support.pointer()"  name="src::is.browser.support.pointer">
        <![CDATA[(() =>{

                

                

                

                let var_once_value_1561952820997;

                function main(){

        
/**
 * 
 * 判断当前浏览器是否支持点触
 * 
 * @return {boolean} 如果是支持点触则返回 true ，否则返回 false 
 * 
 * @once
 * 
 */

return window.hasOwnProperty('onpointerdown');

    }

                return function(){

                    

                    

                    
        if(var_once_value_1561952820997){

            return var_once_value_1561952820997 ;

        }
        return var_once_value_1561952820997 = main.call(this ) ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301879" signature="<boolean> src::is.browser.support.touch()"  name="src::is.browser.support.touch">
        <![CDATA[(() =>{

                

                

                

                let var_once_value_1561952820999;

                function main(){

        
/**
 * 
 * 判断当前浏览器是否支持触摸
 * 
 * @return {boolean} 如果是支持触摸则返回 true ，否则返回 false 
 * 
 * @once
 * 
 */

return window.hasOwnProperty('ontouchstart');

    }

                return function(){

                    

                    

                    
        if(var_once_value_1561952820999){

            return var_once_value_1561952820999 ;

        }
        return var_once_value_1561952820999 = main.call(this ) ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560844375434" signature="<mixed> src::browser.event.name.single(<string> name)" entry-type="string"  name="src::browser.event.name.single" imports="src::is.browser.support.pointer src::is.browser.support.touch">
        <![CDATA[(() =>{

                let isSupportPointer,isSupportTouch;

                let var_init_locked_1561952821002;

                

                

                function main(name){

        
/**
 * 
 * 获取事件名称
 * 
 * @import isSupportPointer from is.browser.support.pointer
 * 
 * @import isSupportTouch from is.browser.support.touch
 * 
 * @param {string} name 事件名称
 * 
 * @return {mixed} 返回说明 
 * 
 */

 if(isSupportPointer()){

    switch(name){

        case 'start':

            name = 'down' ;

            break ;

        case 'end':

            name = 'up' ;
    }

    return `pointer${name}` ;

 }else if(isSupportTouch()){

    return `touch${name}` ;
 }

 return `mouse${name}` ;



    }

                return function(name){

                    
        if(!var_init_locked_1561952821002){

            isSupportPointer = include('is.browser.support.pointer');
isSupportTouch = include('is.browser.support.touch');

            var_init_locked_1561952821002 = true ;
        }
        

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529985" signature="<string> src::os.name()"  name="src::os.name">
        <![CDATA[(() =>{

                

                

                

                let var_once_value_1561952821007;

                function main(){

        /**
 * 
 * 返回当前操作系统的名称
 * 
 * @once
 * 
 * @return {string} 操作系统的名称
 * 
 */

const NAMES = {
    iphone: 'iOS',
    android:'Android',
    mac: 'MacOS',
    win: 'Windows',
    linux: 'Linux',
    other: 'Other'
};

let userAgent = navigator.userAgent.toLowerCase(),
    name = NAMES[(userAgent.match(/mac|win|linux/) || ['other'])[0]];

switch(name){

    case 'MacOS':
    case 'Linux':

        {

            let name = NAMES[(userAgent.match(/iphone|android/) || ['other'])[0]] ;

            if(name !== 'Other'){

                return name ;
            }
        }
}

return name ;

    }

                return function(){

                    

                    

                    
        if(var_once_value_1561952821007){

            return var_once_value_1561952821007 ;

        }
        return var_once_value_1561952821007 = main.call(this ) ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529855" signature="<number> src::browser.scale()"  name="src::browser.scale" imports="src::os.name">
        <![CDATA[(() =>{

                let osName;

                let var_init_locked_1561952821009;

                

                

                function main(){

        /**
 * 
 * 获得当前浏览器的缩放比率
 * 
 * @import os.name
 * 
 * @return {number} 缩放比率
 * 
 */

switch(osName){

    case 'iOS':
    case 'Android':

        return 1 ;
}

return window.devicePixelRatio ;


    }

                return function(){

                    
        if(!var_init_locked_1561952821009){

            osName = include('os.name');

            var_init_locked_1561952821009 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513301443" signature="<void> config::event.tap(<string> key)"  name="config::event.tap" imports="src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get">
        <![CDATA[(() =>{

                let get;

                let var_init_locked_1561952821012;

                

                

                const config = {
    "timeout":100,
    "moveDistance":8
};
                function main(key){

                    return get(config , key) ;

                }
                

                return function(key){

                    
        if(!var_init_locked_1561952821012){

            get = include('object.value.get');

            var_init_locked_1561952821012 = true ;
        }
        

                    

                    return main.call(this , key) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560925628704" signature="<void> src::browser.event.gesture.tap.move(<Event> e)" entry-type="Event"  name="src::browser.event.gesture.tap.move" imports="src::browser.event.single src::math.point.distance src::os.name src::browser.scale src::browser.event.stop src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get config::event.tap">
        <![CDATA[(() =>{

                let getEvent,getDistance,getScale,stop,moveDistance;

                let var_init_locked_1561952821016;

                

                

                function main(e){

        
/**
 * 
 * 移动事件监听
 * 
 *  @import getEvent from browser.event.single
 * 
 * @import getDistance from math.point.distance
 * 
 * @import getScale from browser.scale
 * 
 * @import stop from browser.event.stop
 * 
 * @config moveDistance from event.tap...moveDistance
 * 
 * @param {Event} e 事件对象
 * 
 */

stop(e) ;

let me = this,
    {
        pageX,
        pageY
    } = getEvent(e , 'move'),
    {
        startPoint,
        el
    } = me;

if(Math.round(getDistance({
    x:pageX,
    y:pageY
} , startPoint)) * getScale() >= moveDistance){

    dispatch(el , 'gesture:tapcancel') ;

    disabled() ;
}

    }

                return function(e){

                    
        if(!var_init_locked_1561952821016){

            getEvent = include('browser.event.single');
getDistance = include('math.point.distance');
getScale = include('browser.scale');
stop = include('browser.event.stop');
moveDistance = config('event.tap' , 'moveDistance');

            var_init_locked_1561952821016 = true ;
        }
        

                    

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560843725891" signature="<void> src::browser.event.gesture.tap.disabled()"  name="src::browser.event.gesture.tap.disabled" imports="src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map src::browser.global.listener.remove">
        <![CDATA[(() =>{

                let getName,un;

                let var_init_locked_1561952821021;

                

                

                function main(){

        
/**
 * 
 * 取消监听全局事件
 * 
 * @import getName from browser.event.name.single
 * 
 * @import un from browser.global.listener.remove
 * 
 */

 let me = this,
 {
    onStart,
    onMove,
    onEnd
 } = me;

 un(getName('start') , onStart) ;

 un(getName('move') ,  onMove) ;

 un(getName('end') ,  onEnd) ;

 delete me.onStart ;

 delete me.onMove ;

 delete me.onEnd ;

 

    }

                return function(){

                    
        if(!var_init_locked_1561952821021){

            getName = include('browser.event.name.single');
un = include('browser.global.listener.remove');

            var_init_locked_1561952821021 = true ;
        }
        

                    

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560845292953" signature="<void> src::browser.event.dispatch(<HTMLElement> el , <string> name , <mixed> detail)" entry-type="HTMLElement string mixed"  name="src::browser.event.dispatch">
        <![CDATA[(() =>{

                

                

                

                

                function main(el , name , detail){

        
/**
 * 
 * 触发一个自定义事件
 * 
 * @param {HTMLElement} el 元素
 * 
 * @param {string} name 自定义事件名称
 * 
 * @param {mixed} detail 基于自定义事件传递的数据
 * 
 */

el.dispatchEvent(new CustomEvent(name , {
   bubbles:true,
   detail
})) ;


    }

                return function(el , name , detail){

                    

                    

                    return main.call(this , el , name , detail) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560922946038" signature="<void> src::browser.event.gesture.tap.end(<Event> e)" entry-type="Event"  name="src::browser.event.gesture.tap.end" imports="src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map src::browser.global.listener.remove src::browser.event.gesture.tap.disabled src::browser.event.stop src::browser.event.dispatch">
        <![CDATA[(() =>{

                let disabled,stop,dispatch;

                let var_init_locked_1561952821030;

                let var_current_scope_1561952821030;

                

                function main(e){

        
/**
 * 
 * 结束事件监听
 * 
 * @import disabled from ..disabled scoped
 * 
 * @import stop from browser.event.stop
 * 
 * @import dispatch from browser.event.dispatch scoped
 * 
 * @param {Event} e 事件对象
 * 
 */

 stop(e) ;

 dispatch(this.el , 'gesture:tap') ;

 disabled() ;

 

    }

                return function(e){

                    
        if(!var_init_locked_1561952821030){

            stop = include('browser.event.stop');

            var_init_locked_1561952821030 = true ;
        }
        

                    
        if(!var_current_scope_1561952821030 !== this){

            disabled = include('src::browser.event.gesture.tap.disabled').bind(this);
dispatch = include('browser.event.dispatch').bind(this);

            var_current_scope_1561952821030 = this ;
        }
        

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560920064785" signature="<void> src::browser.event.gesture.tap.start.global(<Event> e)" entry-type="Event"  name="src::browser.event.gesture.tap.start.global" imports="src::browser.event.touches src::browser.event.stop src::browser.event.gesture.tap.start.disabled">
        <![CDATA[(() =>{

                let getTouchEvents,stop,disabled;

                let var_init_locked_1561952821036;

                let var_current_scope_1561952821036;

                

                function main(e){

        
/**
 * 
 * 全局监听按下事件
 * 
 * @import getTouchEvents from browser.event.touches
 * 
 * @import stop from browser.event.stop
 * 
 * @import disabled from ..disabled scoped
 * 
 * @param {Event} e 事件
 * 
 */

 stop(e) ;

 if(getTouchEvents(e)){

    disabled() ;
 }
 

    }

                return function(e){

                    
        if(!var_init_locked_1561952821036){

            getTouchEvents = include('browser.event.touches');
stop = include('browser.event.stop');

            var_init_locked_1561952821036 = true ;
        }
        

                    
        if(!var_current_scope_1561952821036 !== this){

            disabled = include('src::browser.event.gesture.tap.start.disabled').bind(this);

            var_current_scope_1561952821036 = this ;
        }
        

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560908512631" signature="<void> src::browser.event.gesture.tap.enabled()"  name="src::browser.event.gesture.tap.enabled" imports="src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single src::browser.event.single src::math.point.distance src::os.name src::browser.scale src::browser.event.stop src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get config::event.tap src::browser.event.gesture.tap.move src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map src::browser.global.listener.remove src::browser.event.gesture.tap.disabled src::browser.event.dispatch src::browser.event.gesture.tap.end src::browser.event.touches src::browser.event.gesture.tap.start.disabled src::browser.event.gesture.tap.start.global src::browser.selector.is src::browser.selector.parent src::browser.global.listener.add">
        <![CDATA[(() =>{

                let getName,onMove,onEnd,onStart,on;

                let var_init_locked_1561952821039;

                let var_current_scope_1561952821039;

                

                function main(){

        
/**
 * 
 * 监听全局事件
 * 
 * @import getName from browser.event.name.single
 * 
 * @import onMove from ..move scoped
 * 
 * @import onEnd from ..end scoped
 * 
 * @import onStart from ..start.global scoped
 * 
 * @import on from browser.global.listener.add
 * 
 */

 let me = this ;

 on(getName('start') , me.onStart = onStart) ;

 on(getName('move') ,  me.onMove = onMove) ;

 on(getName('end') , me.onEnd = onEnd) ;

 

    }

                return function(){

                    
        if(!var_init_locked_1561952821039){

            getName = include('browser.event.name.single');
on = include('browser.global.listener.add');

            var_init_locked_1561952821039 = true ;
        }
        

                    
        if(!var_current_scope_1561952821039 !== this){

            onMove = include('src::browser.event.gesture.tap.move').bind(this);
onEnd = include('src::browser.event.gesture.tap.end').bind(this);
onStart = include('src::browser.event.gesture.tap.start.global').bind(this);

            var_current_scope_1561952821039 = this ;
        }
        

                    return main.call(this ) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560923863794" signature="<void> src::browser.event.gesture.tap.start(<Event> e)" entry-type="Event"  name="src::browser.event.gesture.tap.start" imports="src::browser.event.single src::browser.event.touches src::browser.event.stop src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single src::math.point.distance src::os.name src::browser.scale src::is.type src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get config::event.tap src::browser.event.gesture.tap.move src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map src::browser.global.listener.map src::browser.global.listener.remove src::browser.event.gesture.tap.disabled src::browser.event.dispatch src::browser.event.gesture.tap.end src::browser.event.gesture.tap.start.disabled src::browser.event.gesture.tap.start.global src::browser.selector.is src::browser.selector.parent src::browser.global.listener.add src::browser.event.gesture.tap.enabled">
        <![CDATA[(() =>{

                let getEvent,getTouchEvents,stop,enabled,disabled,timeout;

                let var_init_locked_1561952821049;

                let var_current_scope_1561952821049;

                

                function main(e){

        
/**
 * 
 * 开始事件监听
 * 
 * @import getEvent from browser.event.single
 * 
 * @import getTouchEvents from browser.event.touches
 * 
 * @import stop from browser.event.stop
 * 
 * @import enabled from ..enabled scoped
 * 
 * @import disabled from ..disabled scoped
 * 
 * @config timeout from event.tap...timeout
 * 
 * @param {Event} e 事件对象
 * 
 */

stop(e) ;

if(getTouchEvents(e)){

   return ;

}

let me = this,
{
   pageX:x,
   pageY:y
} = getEvent(e , 'start') ;

me.startPoint = {
   x,
   y
} ;

enabled() ;


    }

                return function(e){

                    
        if(!var_init_locked_1561952821049){

            getEvent = include('browser.event.single');
getTouchEvents = include('browser.event.touches');
stop = include('browser.event.stop');
timeout = config('event.tap' , 'timeout');

            var_init_locked_1561952821049 = true ;
        }
        

                    
        if(!var_current_scope_1561952821049 !== this){

            enabled = include('src::browser.event.gesture.tap.enabled').bind(this);
disabled = include('src::browser.event.gesture.tap.disabled').bind(this);

            var_current_scope_1561952821049 = this ;
        }
        

                    return main.call(this , e) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560907352039" signature="<string> src::browser.event.gesture.name.start(<string> name)" entry-type="string"  name="src::browser.event.gesture.name.start" imports="src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single">
        <![CDATA[(() =>{

                let getName;

                let var_init_locked_1561952821061;

                

                

                function main(name){

        
/**
 * 
 * 获得手势事件启动的原生事件
 * 
 * @import getName from browser.event.name.single
 * 
 * @param {string} name 手势事件名称
 * 
 * @return {string} 原生事件名称 
 * 
 */

switch(name){

    case 'tap':

        return getName('start') ;
}

    }

                return function(name){

                    
        if(!var_init_locked_1561952821061){

            getName = include('browser.event.name.single');

            var_init_locked_1561952821061 = true ;
        }
        

                    

                    return main.call(this , name) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1560908299618" signature="<void> src::browser.event.gesture.manager.dom()"  name="src::browser.event.gesture.manager.dom" imports="src::is.browser.support.pointer src::is.browser.support.touch src::browser.event.name.single src::browser.event.gesture.name.start src::class.empty src::map.constructor src::map.size.get src::map.find src::map.set src::map.get src::map.has src::map.delete src::map.forEach src::map.clear src::map">
        <![CDATA[(() =>{

                let getName,createMap;

                let var_init_locked_1561952821064;

                let var_class_1561952821064;

                let var_once_value_1561952821064;

                return function(){

                    
        if(!var_init_locked_1561952821064){

            getName = include('src::browser.event.gesture.name.start');
createMap = include('map');

            var_init_locked_1561952821064 = true ;
        }
        

                    
        if(!var_class_1561952821064){

            
/**
 * 
 * 传统事件管理
 * 
 * @import getName from ....name.start
 * 
 * @import createMap from map
 * 
 * @once
 * 
 */

 class main {

    constructor(){

        let me = this ;

        me.events = createMap() ;

    }

    install(el , name){

        let {
            events
        } = this,
        listener = include(`browser.event.gesture.${name}.start`).bind({
            el
        });

        el.addEventListener(getName(name) , listener) ;

        events.set(el , name , listener) ;
    }

    uninstall(el , name){

        let {
            events
        } = this,
        listener = events.get(el , name);

        if(listener){

            el.removeEventListener(getName(name) , listener) ;

            events.delete(el , name) ;
        }

        
    }
 }

            var_class_1561952821064 = class extends main{

                static get ZBEE_CLASS(){

                    return true ;
                }

                get ZBEE_CURRENT_CLASS(){

                    return var_class_1561952821064 ;
                }

            } ;
        }
        

                    
        if(var_once_value_1561952821064){

            return var_once_value_1561952821064 ;

        }

        return var_once_value_1561952821064 = new var_class_1561952821064() ;
        
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1557899641262" signature="<number> src::number.format(<mixed> data , <number> count)" entry-type="mixed number"  name="src::number.format">
        <![CDATA[(() =>{

                

                

                

                

                function main(data , count){

        
/**
 * 
 * 格式化数字
 * 
 * @param {mixed} data 数值
 * 
 * @param {number} count 数值
 * 
 * @return {number} 格式化后的数字字符串 
 * 
 */

 return String(data).padStart(count , '0') ;

    }

                return function(data , count){

                    

                    

                    return main.call(this , data , count) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561361316211" signature="<object> src::miniprogram.page(<object> config)" entry-type="object"  name="src::miniprogram.page" imports="src::function.empty src::is.type src::is.function src::is.array src::is.object src::is.defined src::object.key.join src::object.value.get">
        <![CDATA[(() =>{

                let empty,isFunction,get;

                let var_init_locked_1561952821071;

                

                

                
/**
 * 
 * 针对小程序的页面进行封装
 * 
 * @import empty from function.empty value
 * 
 * @import is.function
 * 
 * @import get from object.value.get
 * 
 * @param {object} config 页面设置
 * 
 * @return {object} 封装后的页面配置 
 * 
 */

 function main({

    onLoad = empty,

    computed = {},

    ...options

 }){

    return {

        onLoad(query){
    
            let me = this,
            {
                setData:nativeSetData,
                data
            } = me ;

            data = new Data(me , {
                computed,
                setData:nativeSetData
            }) ;

            me.setData = values => nativeSetData.call(me , values , () => refresh.call(data)) ;

            onLoad.call(me , query) ;
        },

        ...options
     } ;
 }

 class Data{

    constructor(program , {
        computed,
        setData
    }){

        let me = this ;

        me.program = program ;

        me.computed = computed ;

        me.setData = setData ;
    }

    get(name){

        let me = this,{
            computed,
            program
        } = me,
        {
            data
        } = program ;

        if(computed.hasOwnProperty(name)){
    
            return computed[name](me) ;
        }
    
        return get(data , name) ;
        
    }
 }

 function refresh(){

    let me = this,
    {
        program,
        setData,
        computed
    } = me,
    {
        data
    } = program,
    names = Object.keys(computed);

    for(let name of names){

        data[name] = computed[name](me) ;
    }

    setData.call(program , data) ;
 }
 

                return function(config){

                    
        if(!var_init_locked_1561952821071){

            empty = include('function.empty')();
isFunction = include('is.function');
get = include('object.value.get');

            var_init_locked_1561952821071 = true ;
        }
        

                    

                    return main.call(this , config) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1561605982021" signature="<string> src::json.stringify(<mixed> data)" entry-type="mixed"  name="src::json.stringify">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * JSON 序列化
 * 
 * @param {mixed} data 数据
 * 
 * @return {string} JSON 序列化后的字符串 
 * 
 */

  try{
  
    return JSON.stringify(data);
  
  }catch(e){
  
  }

  return false;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529935" signature="<boolean> src::is.directory(<string> path)" entry-type="string"  name="src::is.directory">
        <![CDATA[(() =>{

                

                

                

                

                function main(path){

        
/**
 * 
 * 判断路径是否为文件夹路径
 * 
 * @param {string} path 路径
 * 
 * @return {boolean} 路径是文件夹则返回 true , 否则返回 false
 * 
 */

const {
    existsSync,
    statSync
} = require('fs') ;

return existsSync(path) && statSync(path).isDirectory() ;


    }

                return function(path){

                    

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529936" signature="<boolean> src::is.file(<string> path)" entry-type="string"  name="src::is.file">
        <![CDATA[(() =>{

                

                

                

                

                function main(path){

        
/**
 * 
 * 判断路径是否为文件路径
 * 
 * @param {string} path 路径
 * 
 * @return {boolean} 路径是文件则返回 true , 否则返回 false
 * 
 */

const {
    existsSync,
    statSync
} = require('fs') ;

return existsSync(path) && statSync(path).isFile() ;


    }

                return function(path){

                    

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1559527326421" signature="<array> src::directory.readAllFilePaths(<string> path , <RegExp> testRe)" entry-type="string RegExp"  name="src::directory.readAllFilePaths" imports="src::is.directory src::is.file">
        <![CDATA[(() =>{

                let isDirectory,isFile;

                let var_init_locked_1561952821083;

                

                

                
/**
 * 
 * 读取目录下所有文件的路径
 * 
 * @import is.directory
 * 
 * @import is.file
 * 
 * @param {string} path 文件夹目录路径
 * 
 * @param {RegExp} [testRe] 路径匹配正则表达式
 * 
 * @return {array} 多个文件路径
 * 
 */

function main(path , testRe){

    if(isDirectory(path)){

        const {
            readdirSync
        } = require('fs'),
        {
            join
        } = require('path');
    
        let names = readdirSync(path),
            paths = [];
    
        for(let name of names){
    
            let targetPath = join(path , name) ;
    
            if(isFile(targetPath)){

                if(testRe && !testRe.test(targetPath)){

                    continue ;
                }
    
                paths.push(targetPath) ;
            
            }else{
    
                paths.push(...main(targetPath , testRe)) ;
            }
        }
    
        return paths ;
    }
    
    return [] ;
}

                return function(path , testRe){

                    
        if(!var_init_locked_1561952821083){

            isDirectory = include('is.directory');
isFile = include('is.file');

            var_init_locked_1561952821083 = true ;
        }
        

                    

                    return main.call(this , path , testRe) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1537062026717" signature="<ArrayBuffer> src::file.read(<string> path)" entry-type="string"  name="src::file.read" imports="src::is.file">
        <![CDATA[(() =>{

                let isFile;

                let var_init_locked_1561952821086;

                

                

                function main(path){

        /**
 * 
 * 读取文件
 * 
 * @import is.file
 * 
 * @param {string} path 文本文件路径
 * 
 * @return {ArrayBuffer} 原生文件内容
 * 
 */

const {
    readFileSync
} = require('fs') ;

if(isFile(path)){

    return readFileSync(path) ;
}

    }

                return function(path){

                    
        if(!var_init_locked_1561952821086){

            isFile = include('is.file');

            var_init_locked_1561952821086 = true ;
        }
        

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1537062026718" signature="<string> src::file.read.text(<string> path)" entry-type="string"  name="src::file.read.text" imports="src::is.file src::file.read">
        <![CDATA[(() =>{

                let read;

                let var_init_locked_1561952821088;

                

                

                function main(path){

        /**
 * 
 * 读取文本文件
 * 
 * @import read from file.read
 * 
 * @param {string} path 文本文件路径
 * 
 * @return {string} 文本文件内容
 * 
 */

let data = read(path) ;

if(data){

    return data.toString('utf8') ;
}

    }

                return function(path){

                    
        if(!var_init_locked_1561952821088){

            read = include('file.read');

            var_init_locked_1561952821088 = true ;
        }
        

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302711" signature="<mixed> src::json.parse(<string> data)" entry-type="string"  name="src::json.parse">
        <![CDATA[(() =>{

                

                

                

                

                function main(data){

        
/**
 * 
 * 将 JSON 字符串转换成 JSON 数据
 * 
 * @param {string} data JSON 字符串
 * 
 * @return {mixed} JSON 数据 
 * 
 */

try{

    return JSON.parse(data) ;

 }catch(err){


 }

 return {} ;

    }

                return function(data){

                    

                    

                    return main.call(this , data) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1556513302606" signature="<mixed> src::file.read.json(<string> path)" entry-type="string"  name="src::file.read.json" imports="src::is.file src::file.read src::file.read.text src::json.parse">
        <![CDATA[(() =>{

                let read,parse;

                let var_init_locked_1561952821093;

                

                

                function main(path){

        
/**
 * 
 * 读取 JSON 文件
 * 
 * @import read from file.read.text
 * 
 * @import parse from json.parse
 * 
 * @param {string} path JSON文件存储路径
 * 
 * @return {mixed} JSON数据 
 * 
 */

let data = read(path) ;

if(data){

    return parse(data) ;
}


    }

                return function(path){

                    
        if(!var_init_locked_1561952821093){

            read = include('file.read.text');
parse = include('json.parse');

            var_init_locked_1561952821093 = true ;
        }
        

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529894" signature="<mixed> src::directory.create(<string> path)" entry-type="string"  name="src::directory.create" imports="src::is.directory">
        <![CDATA[(() =>{

                let isDirectory;

                let var_init_locked_1561952821096;

                

                

                
/**
 * 
 * 创建不存在的目录，如果存在的话，则维持现状
 * 
 * @import is.directory
 * 
 * @param {string} path 目录路径
 * 
 * @return {mixed} 返回说明 
 * 
 * @scoped
 * 
 */

const {
    mkdirSync
} = require('fs'),
folderRe = /(?:^\/)|(?:[^\/\\]+(?:[\/\\]|$))/g;

function main(path){

    let folderNames = path.match(folderRe),
        folderPath = '';

    for(let folderName of folderNames){

        folderPath += folderName ;

        if(folderName !== '/' && !isDirectory(folderPath)){

            mkdirSync(folderPath) ;
        }
    }
}


                return function(path){

                    
        if(!var_init_locked_1561952821096){

            isDirectory = include('is.directory');

            var_init_locked_1561952821096 = true ;
        }
        

                    

                    return main.call(this , path) ;
                } ;

            })()]]>
    </class>

    <class standard="yes" motify="1535178529906" signature="<void> src::file.write(<string> path , <mixed> data)" entry-type="string mixed"  name="src::file.write" imports="src::is.directory src::directory.create">
        <![CDATA[(() =>{

                let create;

                let var_init_locked_1561952821098;

                

                

                function main(path , data){

        
/**
 * 
 * 保存文件
 * 
 * @import create from directory.create
 * 
 * @param {string} path 保存文件路径
 * 
 * @param {mixed} data 保存文件内容
 * 
 * 
 */

const {
    writeFileSync
} = require('fs'),
{
    dirname
} = require('path');

create(dirname(path)) ;

writeFileSync(path , data) ;

    }

                return function(path , data){

                    
        if(!var_init_locked_1561952821098){

            create = include('directory.create');

            var_init_locked_1561952821098 = true ;
        }
        

                    

                    return main.call(this , path , data) ;
                } ;

            })()]]>
    </class>


</classes>